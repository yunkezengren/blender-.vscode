# Blender 电子表格系统 - 列和行渲染管道

## 目录
- [1. 渲染管道概述](#1-渲染管道概述)
- [2. SpreadsheetDrawer 类](#2-spreadsheetdrawer-类)
  - [2.1. 核心属性](#21-核心属性)
  - [2.2. 虚函数接口](#22-虚函数接口)
- [3. 绘制流程](#3-绘制流程)
  - [3.1. 主绘制函数](#31-主绘制函数)
  - [3.2. 背景绘制](#32-背景绘制)
  - [3.3. 内容绘制](#33-内容绘制)
- [4. 单元格绘制](#4-单元格绘制)
  - [4.1. 顶部行（列标题）](#41-顶部行列标题)
  - [4.2. 左侧列（行索引）](#42-左侧列行索引)
  - [4.3. 内容单元格](#43-内容单元格)
- [5. 坐标系统](#5-坐标系统)
  - [5.1. 屏幕坐标](#51-屏幕坐标)
  - [5.2. 视图坐标](#52-视图坐标)
  - [5.3. 滚动偏移](#53-滚动偏移)
- [6. 可见性裁剪](#6-可见性裁剪)
  - [6.1. 可见行计算](#61-可见行计算)
  - [6.2. 可见列计算](#62-可见列计算)
  - [6.3. Scissor 测试](#63-scissor-测试)
- [7. 样式和主题](#7-样式和主题)
  - [7.1. 颜色主题](#71-颜色主题)
  - [7.2. 交替行背景](#72-交替行背景)
  - [7.3. 分隔线](#73-分隔线)
- [8. 文本渲染](#8-文本渲染)
  - [8.1. 文本测量](#81-文本测量)
  - [8.2. 文本格式化](#82-文本格式化)
  - [8.3. 文本裁剪](#83-文本裁剪)
- [9. 重排序可视化](#9-重排序可视化)
  - [9.1. 源列高亮](#91-源列高亮)
  - [9.2. 目标位置指示](#92-目标位置指示)
  - [9.3. 拖拽预览](#93-拖拽预览)
- [10. 性能优化](#10-性能优化)
  - [10.1. 批量绘制](#101-批量绘制)
  - [10.2. 脏区域优化](#102-脏区域优化)
  - [10.3. 缓存策略](#103-缓存策略)

---

## 1. 渲染管道概述

电子表格的渲染管道采用分层绘制策略，从背景到前景依次绘制：

```mermaid
graph TB
    subgraph "渲染管道"
        Clear[清空背景]
        AltRow[交替行背景]
        IndexBg[索引列背景]
        TopBg[顶部行背景]
        ReorderSrc[重排序源列]
        SepLines[分隔线]
        LeftCol[左侧列内容]
        TopRow[顶部行内容]
        Cells[内容单元格]
        ReorderDst[重排序目标指示]
        Scroll[滚动条]
    end

    Clear --> AltRow
    AltRow --> IndexBg
    IndexBg --> TopBg
    TopBg --> ReorderSrc
    ReorderSrc --> SepLines
    SepLines --> LeftCol
    LeftCol --> TopRow
    TopRow --> Cells
    Cells --> ReorderDst
    ReorderDst --> Scroll
```

**绘制顺序**：
1. **背景层**：交替行、索引列背景、顶部行背景
2. **装饰层**：重排序源、分隔线
3. **内容层**：左侧列、顶部行、单元格
4. **交互层**：重排序目标指示
5. **UI层**：滚动条

---

## 2. SpreadsheetDrawer 类

### 2.1. 核心属性

**定义位置**: `spreadsheet_draw.cc:30-35`

```cpp
class SpreadsheetDrawer {
 protected:
  int left_column_width;    // 左侧列宽度（行索引）
  int top_row_height;       // 顶部行高度（列标题）
  int row_height;           // 单行高度
  int tot_rows;             // 总行数
  int tot_columns;          // 总列数

 public:
  SpreadsheetDrawer();
  virtual ~SpreadsheetDrawer();

  // 虚函数：子类可重写
  virtual void draw_top_row_cell(int column_index, const CellDrawParams ¶ms) const;
  virtual void draw_left_column_cell(int row_index, const CellDrawParams ¶ms) const;
  virtual void draw_content_cell(int row_index, int column_index, const CellDrawParams ¶ms) const;
  virtual int column_width(int column_index) const;
};
```

#### 2.1.1. 默认值

```cpp
SpreadsheetDrawer::SpreadsheetDrawer()
{
  left_column_width = UI_UNIT_X * 2;      // 2个UI单位
  top_row_height = UI_UNIT_Y * 1.1f;      // 1.1个UI单位
  row_height = UI_UNIT_Y;                 // 1个UI单位
}
```

### 2.2. 虚函数接口

#### 2.2.1. 绘制函数

```cpp
// 顶部行（列标题）绘制 - 默认为空
void SpreadsheetDrawer::draw_top_row_cell(int column_index,
                                          const CellDrawParams ¶ms) const
{
}

// 左侧列（行索引）绘制 - 默认为空
void SpreadsheetDrawer::draw_left_column_cell(int row_index,
                                              const CellDrawParams ¶ms) const
{
}

// 内容单元格绘制 - 默认为空
void SpreadsheetDrawer::draw_content_cell(int row_index,
                                          int column_index,
                                          const CellDrawParams ¶ms) const
{
}
```

#### 2.2.2. 列宽获取

```cpp
int SpreadsheetDrawer::column_width(int column_index) const
{
  return 5 * UI_UNIT_X;  // 默认5个UI单位
}
```

---

## 3. 绘制流程

### 3.1. 主绘制函数

**定义位置**: `spreadsheet_draw.cc:345-389`

```cpp
void draw_spreadsheet_in_region(const bContext *C,
                                ARegion *region,
                                const SpreadsheetDrawer &drawer)
{
  SpaceSpreadsheet &sspreadsheet = *CTX_wm_space_spreadsheet(C);

  // 1. 更新View2D总区域大小
  update_view2d_tot_rect(drawer, region, drawer.tot_rows);

  // 2. 清空背景
  ui::theme::frame_buffer_clear(TH_BACK);

  // 3. 获取滚动偏移
  View2D *v2d = ®ion->v2d;
  const int scroll_offset_y = v2d->cur.ymax;  // 垂直
  const int scroll_offset_x = v2d->cur.xmin;  // 水平

  // 4. 检查是否在重排序
  bool is_reordering_columns = sspreadsheet.runtime->reorder_column_visualization_data.has_value();

  // 5. GPU绘制准备
  GPUVertFormat *format = immVertexFormat();
  uint pos = GPU_vertformat_attr_add(format, "pos", blender::gpu::VertAttrType::SFLOAT_32_32);
  immBindBuiltinProgram(GPU_SHADER_3D_UNIFORM_COLOR);

  // 6. 绘制背景层
  draw_index_column_background(pos, region, drawer);
  draw_alternating_row_overlay(pos, scroll_offset_y, region, drawer);
  draw_top_row_background(pos, region, drawer);
  if (is_reordering_columns) {
    draw_column_reorder_source(pos, *region, sspreadsheet, scroll_offset_x);
  }
  draw_separator_lines(pos, scroll_offset_x, region, drawer);

  immUnbindProgram();

  // 7. 绘制内容层
  draw_left_column_content(scroll_offset_y, C, region, drawer);
  draw_top_row_content(C, region, drawer, scroll_offset_x);
  draw_cell_contents(C, region, drawer, scroll_offset_x, scroll_offset_y);

  // 8. 绘制交互层
  if (is_reordering_columns) {
    draw_column_reorder_destination(*region, sspreadsheet, drawer, scroll_offset_x);
  }

  // 9. 绘制滚动条
  rcti scroller_mask;
  BLI_rcti_init(&scroller_mask,
                drawer.left_column_width,
                region->winx,
                0,
                region->winy - drawer.top_row_height);
  ui::view2d_scrollers_draw(v2d, &scroller_mask);
}
```

### 3.2. 背景绘制

#### 3.2.1. 索引列背景

```cpp
static void draw_index_column_background(const uint pos,
                                         const ARegion *region,
                                         const SpreadsheetDrawer &drawer)
{
  immUniformThemeColorShade(TH_BACK, 11);  // 比背景亮11
  immRectf(pos,
           0,                                          // xmin
           region->winy - drawer.top_row_height,       // ymin
           drawer.left_column_width,                   // xmax
           0);                                         // ymax
}
```

#### 3.2.2. 交替行背景

```cpp
static void draw_alternating_row_overlay(const uint pos,
                                         const int scroll_offset_y,
                                         const ARegion *region,
                                         const SpreadsheetDrawer &drawer)
{
  immUniformThemeColor(TH_ROW_ALTERNATE);
  GPU_blend(GPU_BLEND_ALPHA);

  BLI_assert(drawer.row_height > 0);
  const int row_pair_height = drawer.row_height * 2;

  // 计算起始Y位置
  const int row_top_y = region->winy - drawer.top_row_height - scroll_offset_y % row_pair_height;

  // 绘制交替行
  for (const int i : IndexRange(region->winy / row_pair_height + 1)) {
    int x_left = 0;
    int x_right = region->winx;
    int y_top = row_top_y - i * row_pair_height - drawer.row_height;
    int y_bottom = y_top - drawer.row_height;

    // 裁剪到内容区域
    y_top = std::min(y_top, region->winy - drawer.top_row_height);
    y_bottom = std::min(y_bottom, region->winy - drawer.top_row_height);

    immRectf(pos, x_left, y_top, x_right, y_bottom);
  }

  GPU_blend(GPU_BLEND_NONE);
}
```

#### 3.2.3. 顶部行背景

```cpp
static void draw_top_row_background(const uint pos,
                                    const ARegion *region,
                                    const SpreadsheetDrawer &drawer)
{
  immUniformThemeColorShade(TH_BACK, 11);
  immRectf(pos,
           0,
           region->winy,
           region->winx,
           region->winy - drawer.top_row_height);
}
```

#### 3.2.4. 分隔线

```cpp
static void draw_separator_lines(const uint pos,
                                 const int scroll_offset_x,
                                 const ARegion *region,
                                 const SpreadsheetDrawer &drawer)
{
  immUniformThemeColorShade(TH_BACK, -11);  // 比背景暗11

  // 准备绘制线条
  immBeginAtMost(GPU_PRIM_LINES, drawer.tot_columns * 2 + 4);

  // 左侧列分隔线
  immVertex2f(pos, drawer.left_column_width, region->winy);
  immVertex2f(pos, drawer.left_column_width, 0);

  // 顶部行分隔线
  immVertex2f(pos, 0, region->winy - drawer.top_row_height);
  immVertex2f(pos, region->winx, region->winy - drawer.top_row_height);

  // 列间分隔线
  int line_x = drawer.left_column_width - scroll_offset_x;
  for (const int column_index : IndexRange(drawer.tot_columns)) {
    const int column_width = drawer.column_width(column_index);
    line_x += column_width;

    if (line_x >= drawer.left_column_width) {
      immVertex2f(pos, line_x, region->winy);
      immVertex2f(pos, line_x, 0);
    }
  }

  immEnd();
}
```

### 3.3. 内容绘制

#### 3.3.1. 左侧列内容

```cpp
static void draw_left_column_content(const int scroll_offset_y,
                                     const bContext *C,
                                     ARegion *region,
                                     const SpreadsheetDrawer &drawer)
{
  // 1. 保存当前Scissor
  int old_scissor[4];
  GPU_scissor_get(old_scissor);

  // 2. 设置Scissor到左侧列区域
  GPU_scissor(0, 0, drawer.left_column_width, region->winy - drawer.top_row_height);

  // 3. 创建UI块
  ui::Block *left_column_block = block_begin(C, region, __func__, ui::EmbossType::None);

  // 4. 计算可见行范围
  int first_row, max_visible_rows;
  get_visible_rows(drawer, region, scroll_offset_y, &first_row, &max_visible_rows);

  // 5. 绘制每个可见行
  for (const int row_index : IndexRange(first_row, max_visible_rows)) {
    if (row_index >= drawer.tot_rows) {
      break;
    }

    CellDrawParams params;
    params.block = left_column_block;
    params.xmin = 0;
    params.ymin = region->winy - drawer.top_row_height - (row_index + 1) * drawer.row_height -
                  scroll_offset_y;
    params.width = drawer.left_column_width - CELL_RIGHT_PADDING;
    params.height = drawer.row_height;

    drawer.draw_left_column_cell(row_index, params);
  }

  // 6. 绘制UI块
  block_end(C, left_column_block);
  block_draw(C, left_column_block);

  // 7. 恢复Scissor
  GPU_scissor(UNPACK4(old_scissor));
}
```

#### 3.3.2. 顶部行内容

```cpp
static void draw_top_row_content(const bContext *C,
                                 ARegion *region,
                                 const SpreadsheetDrawer &drawer,
                                 const int scroll_offset_x)
{
  int old_scissor[4];
  GPU_scissor_get(old_scissor);

  // 设置Scissor到顶部行区域
  GPU_scissor(drawer.left_column_width + 1,
              region->winy - drawer.top_row_height,
              region->winx - drawer.left_column_width,
              drawer.top_row_height);

  ui::Block *first_row_block = block_begin(C, region, __func__, ui::EmbossType::None);

  int left_x = drawer.left_column_width - scroll_offset_x;
  for (const int column_index : IndexRange(drawer.tot_columns)) {
    const int column_width = drawer.column_width(column_index);
    const int right_x = left_x + column_width;

    CellDrawParams params;
    params.block = first_row_block;
    params.xmin = left_x;
    params.ymin = region->winy - drawer.top_row_height;
    params.width = column_width - CELL_RIGHT_PADDING;
    params.height = drawer.top_row_height;

    drawer.draw_top_row_cell(column_index, params);

    left_x = right_x;
  }

  block_end(C, first_row_block);
  block_draw(C, first_row_block);

  GPU_scissor(UNPACK4(old_scissor));
}
```

#### 3.3.3. 内容单元格

```cpp
static void draw_cell_contents(const bContext *C,
                               ARegion *region,
                               const SpreadsheetDrawer &drawer,
                               const int scroll_offset_x,
                               const int scroll_offset_y)
{
  int old_scissor[4];
  GPU_scissor_get(old_scissor);

  // 设置Scissor到内容区域
  GPU_scissor(drawer.left_column_width + 1,
              0,
              region->winx - drawer.left_column_width,
              region->winy - drawer.top_row_height);

  ui::Block *cells_block = block_begin(C, region, __func__, ui::EmbossType::None);

  // 计算可见行范围
  int first_row, max_visible_rows;
  get_visible_rows(drawer, region, scroll_offset_y, &first_row, &max_visible_rows);

  // 遍历可见列
  int left_x = drawer.left_column_width - scroll_offset_x;
  for (const int column_index : IndexRange(drawer.tot_columns)) {
    const int column_width = drawer.column_width(column_index);
    const int right_x = left_x + column_width;

    // 检查列是否在可见范围内
    if (right_x >= drawer.left_column_width && left_x <= region->winx) {
      // 遍历可见行
      for (const int row_index : IndexRange(first_row, max_visible_rows)) {
        if (row_index >= drawer.tot_rows) {
          break;
        }

        CellDrawParams params;
        params.block = cells_block;
        params.xmin = left_x;
        params.ymin = region->winy - drawer.top_row_height - (row_index + 1) * drawer.row_height -
                      scroll_offset_y;
        params.width = column_width - CELL_RIGHT_PADDING;
        params.height = drawer.row_height;

        drawer.draw_content_cell(row_index, column_index, params);
      }
    }

    left_x = right_x;
  }

  block_end(C, cells_block);
  block_draw(C, cells_block);

  GPU_scissor(UNPACK4(old_scissor));
}
```

---

## 4. 单元格绘制

### 4.1. CellDrawParams 结构

```cpp
struct CellDrawParams {
  ui::Block *block;      // UI块
  int xmin;              // 最小X坐标
  int ymin;              // 最小Y坐标
  int width;             // 宽度
  int height;            // 高度
};
```

### 4.2. 实际绘制实现

在 `spreadsheet_draw.cc` 的子类中：

```cpp
class ConcreteSpreadsheetDrawer : public SpreadsheetDrawer {
 public:
  void draw_top_row_cell(int column_index, const CellDrawParams ¶ms) const override
  {
    // 1. 获取列对象
    const SpreadsheetColumn *column = get_column(column_index);

    // 2. 创建UI按钮
    uiBut *but = uiDefBut(params.block,
                          UI_BTYPE_TEXT,
                          0,
                          column->display_name,
                          params.xmin,
                          params.ymin,
                          params.width,
                          params.height,
                          nullptr,
                          0,
                          0,
                          0,
                          0,
                          column->display_name);

    // 3. 设置样式
    UI_but_theme_color_set(but, TH_TEXT);
    UI_but_align_set(but, UI_ALIGN_CENTER);
  }

  void draw_left_column_cell(int row_index, const CellDrawParams ¶ms) const override
  {
    // 绘制行索引
    std::string index_str = std::to_string(row_index);

    uiBut *but = uiDefBut(params.block,
                          UI_BTYPE_TEXT,
                          0,
                          index_str.c_str(),
                          params.xmin,
                          params.ymin,
                          params.width,
                          params.height,
                          nullptr,
                          0,
                          0,
                          0,
                          0,
                          index_str.c_str());

    UI_but_theme_color_set(but, TH_TEXT);
    UI_but_align_set(but, UI_ALIGN_CENTER);
  }

  void draw_content_cell(int row_index,
                         int column_index,
                         const CellDrawParams ¶ms) const override
  {
    // 1. 获取列值
    const ColumnValues *values = get_column_values(column_index);
    if (!values) {
      return;
    }

    // 2. 获取单元格值
    GVArray data = values->data();
    if (row_index >= data.size()) {
      return;
    }

    // 3. 格式化值
    std::string formatted = format_cell_value(data[row_index], values->type(), values->display_hint());

    // 4. 绘制文本
    uiBut *but = uiDefBut(params.block,
                          UI_BTYPE_TEXT,
                          0,
                          formatted.c_str(),
                          params.xmin,
                          params.ymin,
                          params.width,
                          params.height,
                          nullptr,
                          0,
                          0,
                          0,
                          0,
                          formatted.c_str());

    UI_but_theme_color_set(but, TH_TEXT);
    UI_but_align_set(but, UI_ALIGN_LEFT);
  }
};
```

---

## 5. 坐标系统

### 5.1. 坐标类型

#### 5.1.1. 屏幕坐标 (Screen Coordinates)

```
原点：(0, 0) 在区域左下角
Y轴：向上为正
X轴：向右为正
```

#### 5.1.2. 视图坐标 (View Coordinates)

```
原点：由View2D.cur定义
Y轴：向上为正（cur.ymax）
X轴：向右为正（cur.xmin）
```

### 5.2. 坐标转换

#### 5.2.1. 屏幕到视图

```cpp
// 屏幕X -> 视图X
float view_x = ui::view2d_region_to_view_x(®ion.v2d, screen_x);

// 屏幕Y -> 视图Y
float view_y = ui::view2d_region_to_view_y(®ion.v2d, screen_y);
```

转换公式：
$$
\text{view\_x} = \text{screen\_x} - \text{cur}.xmin
$$

$$
\text{view\_y} = \text{screen\_y} - (\text{region\_height} - \text{cur}.ymax)
$$

#### 5.2.2. 视图到屏幕

```cpp
// 视图X -> 屏幕X
float screen_x = ui::view2d_view_to_region_x(®ion.v2d, view_x);

// 视图Y -> 屏幕Y
float screen_y = ui::view2d_view_to_region_y(®ion.v2d, view_y);
```

### 5.3. 滚动偏移

#### 5.3.1. 垂直滚动

```cpp
// 向下滚动时，cur.ymax 增大
const int scroll_offset_y = v2d->cur.ymax;

// 单元格Y位置计算
int cell_y = region->winy - drawer.top_row_height - (row_index + 1) * drawer.row_height - scroll_offset_y;
```

#### 5.3.2. 水平滚动

```cpp
// 向右滚动时，cur.xmin 减小（负值）
const int scroll_offset_x = v2d->cur.xmin;

// 单元格X位置计算
int cell_x = drawer.left_column_width - scroll_offset_x + column_offset;
```

---

## 6. 可见性裁剪

### 6.1. 可见行计算

```cpp
static void get_visible_rows(const SpreadsheetDrawer &drawer,
                             const ARegion *region,
                             const int scroll_offset_y,
                             int *r_first_row,
                             int *r_max_visible_rows)
{
  // 第一个可见行
  *r_first_row = -scroll_offset_y / drawer.row_height;

  // 最大可见行数
  *r_max_visible_rows = region->winy / drawer.row_height + 1;
}
```

**示例**：
- `scroll_offset_y = 150`
- `row_height = 20`
- `region->winy = 400`

```
first_row = -150 / 20 = -7.5 → 7 (向下取整)
max_rows = 400 / 20 + 1 = 21
可见行：7 到 27
```

### 6.2. 可见列计算

```cpp
// 在绘制循环中
int left_x = drawer.left_column_width - scroll_offset_x;
for (const int column_index : IndexRange(drawer.tot_columns)) {
  const int column_width = drawer.column_width(column_index);
  const int right_x = left_x + column_width;

  // 检查是否在可见范围内
  if (right_x >= drawer.left_column_width && left_x <= region->winx) {
    // 列可见，绘制
  }

  left_x = right_x;
}
```

### 6.3. Scissor 测试

Scissor 测试用于限制绘制区域，避免绘制到不可见区域：

```cpp
// 保存当前Scissor
int old_scissor[4];
GPU_scissor_get(old_scissor);

// 设置新Scissor
GPU_scissor(x, y, width, height);

// 绘制操作
draw_content();

// 恢复Scissor
GPU_scissor(UNPACK4(old_scissor));
```

#### 6.3.1. Scissor 区域设置

```cpp
// 左侧列
GPU_scissor(0, 0, drawer.left_column_width, region->winy - drawer.top_row_height);

// 顶部行
GPU_scissor(drawer.left_column_width + 1,
            region->winy - drawer.top_row_height,
            region->winx - drawer.left_column_width,
            drawer.top_row_height);

// 内容区域
GPU_scissor(drawer.left_column_width + 1,
            0,
            region->winx - drawer.left_column_width,
            region->winy - drawer.top_row_height);
```

---

## 7. 样式和主题

### 7.1. 颜色主题

Blender使用主题系统定义颜色：

```cpp
// TH_BACK - 背景色
immUniformThemeColorShade(TH_BACK, 11);  // 亮11
immUniformThemeColorShade(TH_BACK, -11); // 暗11

// TH_ROW_ALTERNATE - 交替行
immUniformThemeColor(TH_ROW_ALTERNATE);

// TH_TEXT - 文本色
UI_but_theme_color_set(but, TH_TEXT);
```

#### 7.1.1. 主题颜色表

| 主题ID | 用途 | 默认值 |
|--------|------|--------|
| `TH_BACK` | 背景 | 灰色 |
| `TH_ROW_ALTERNATE` | 交替行 | 浅灰 |
| `TH_TEXT` | 文本 | 黑色 |
| `TH_HEADER` | 标题 | 深灰 |

### 7.2. 交替行背景

```cpp
// 绘制逻辑
for (每两行) {
  绘制一行背景
  跳过一行
}
```

**视觉效果**：
```
行0: 白色背景
行1: 交替色背景
行2: 白色背景
行3: 交替色背景
```

### 7.3. 分隔线

```cpp
immUniformThemeColorShade(TH_BACK, -11);  // 暗色
immBeginAtMost(GPU_PRIM_LINES, count);

// 垂直线
immVertex2f(pos, x, y1);
immVertex2f(pos, x, y2);

// 水平线
immVertex2f(pos, x1, y);
immVertex2f(pos, x2, y);

immEnd();
```

---

## 8. 文本渲染

### 8.1. 文本测量

```cpp
float calculate_text_width(const std::string &text)
{
  // 使用UI系统测量
  return UI_text_width(text.c_str(), UI_FONTSTYLE_NORMAL, UI_UNIT_X);
}
```

### 8.2. 文本格式化

#### 8.2.1. 数值格式化

```cpp
std::string format_value(const void *value,
                         eSpreadsheetColumnValueType type,
                         ColumnValueDisplayHint hint)
{
  std::stringstream ss;

  switch (type) {
    case SPREADSHEET_VALUE_TYPE_INT32:
      ss << *static_cast<const int *>(value);
      break;

    case SPREADSHEET_VALUE_TYPE_FLOAT:
      ss << std::fixed << std::setprecision(3)
         << *static_cast<const float *>(value);
      break;

    case SPREADSHEET_VALUE_TYPE_FLOAT3: {
      const float3 &v = *static_cast<const float3 *>(value);
      ss << "(" << v.x << ", " << v.y << ", " << v.z << ")";
      break;
    }

    case SPREADSHEET_VALUE_TYPE_COLOR: {
      const ColorGeometry4f &c = *static_cast<const ColorGeometry4f *>(value);
      ss << "(" << c.r << ", " << c.g << ", " << c.b << ", " << c.a << ")";
      break;
    }

    case SPREADSHEET_VALUE_TYPE_BYTES:
      ss << format_bytes(*static_cast<const int64_t *>(value));
      break;

    default:
      ss << "Unknown";
  }

  return ss.str();
}
```

#### 8.2.2. 字节格式化

```cpp
std::string format_bytes(int64_t bytes)
{
  const char *units[] = {"B", "KB", "MB", "GB", "TB"};
  int unit_index = 0;
  double value = bytes;

  while (value >= 1024 && unit_index < 4) {
    value /= 1024;
    unit_index++;
  }

  std::stringstream ss;
  ss << std::fixed << std::setprecision(2) << value << " " << units[unit_index];
  return ss.str();
}
```

### 8.3. 文本裁剪

```cpp
// 在UI块中自动处理
uiBut *but = uiDefBut(...);
UI_but_drawflag_set(but, UI_BUT_TEXT_TRUNCATION);
```

---

## 9. 重排序可视化

### 9.1. 源列高亮

```cpp
static void draw_column_reorder_source(const uint pos,
                                       const ARegion ®ion,
                                       const SpaceSpreadsheet &sspreadsheet,
                                       const int scroll_offset_x)
{
  const ReorderColumnVisualizationData &data =
      *sspreadsheet.runtime->reorder_column_visualization_data;
  const SpreadsheetTable &table = *get_active_table(sspreadsheet);
  const SpreadsheetColumn &moving_column = *table.columns[data.old_index];

  rctf rect;
  rect.xmin = moving_column.runtime->left_x - scroll_offset_x;
  rect.xmax = moving_column.runtime->right_x - scroll_offset_x;
  rect.ymin = 0;
  rect.ymax = region.winy;

  immUniformThemeColorShadeAlpha(TH_BACK, -20, -128);
  GPU_blend(GPU_BLEND_ALPHA);
  immRectf(pos, rect.xmin, rect.ymin, rect.xmax, rect.ymax);
  GPU_blend(GPU_BLEND_NONE);
}
```

### 9.2. 目标位置指示

```cpp
static void draw_column_reorder_destination(const ARegion ®ion,
                                            const SpaceSpreadsheet &sspreadsheet,
                                            const SpreadsheetDrawer &drawer,
                                            const int scroll_offset_x)
{
  const ReorderColumnVisualizationData &data =
      *sspreadsheet.runtime->reorder_column_visualization_data;
  const SpreadsheetTable &table = *get_active_table(sspreadsheet);
  const SpreadsheetColumn &moving_column = *table.columns[data.old_index];
  const SpreadsheetColumn &insert_column = *table.columns[data.new_index];

  // 1. 绘制移动中的列
  {
    ColorTheme4f color;
    ui::theme::get_color_shade_4fv(TH_BACK, -20, color);
    color.a = 0.3f;

    rctf offset_column_rect;
    offset_column_rect.xmin = moving_column.runtime->left_x + data.current_offset_x_px -
                              scroll_offset_x;
    offset_column_rect.xmax = offset_column_rect.xmin +
                              moving_column.width * SPREADSHEET_WIDTH_UNIT;
    offset_column_rect.ymin = 0;
    offset_column_rect.ymax = region.winy;

    ui::draw_roundbox_4fv(&offset_column_rect, true, 0, color);
  }

  // 2. 绘制插入位置指示器
  {
    ColorTheme4f color;
    ui::theme::get_color_shade_4fv(TH_TEXT, 20, color);
    color.a = 0.6f;

    const int insert_column_x = data.new_index <= data.old_index ?
        insert_column.runtime->left_x : insert_column.runtime->right_x;

    const int width = UI_UNIT_X * 0.1f;

    rctf insert_rect;
    insert_rect.xmin = insert_column_x - width / 2 - scroll_offset_x;
    insert_rect.xmax = insert_rect.xmin + width;
    insert_rect.ymin = 0;
    insert_rect.ymax = region.winy;

    // 不要绘制在索引列上
    const int left_bound = drawer.left_column_width - width / 2;
    insert_rect.xmin = std::max<float>(insert_rect.xmin, left_bound);
    insert_rect.xmax = std::max<float>(insert_rect.xmax, left_bound);

    ui::draw_roundbox_4fv(&insert_rect, true, 0, color);
  }
}
```

---

## 10. 性能优化

### 10.1. 批量绘制

#### 10.1.1. GPU 立即模式

```cpp
// 准备绘制
GPUVertFormat *format = immVertexFormat();
uint pos = GPU_vertformat_attr_add(format, "pos", blender::gpu::VertAttrType::SFLOAT_32_32);
immBindBuiltinProgram(GPU_SHADER_3D_UNIFORM_COLOR);

// 批量绘制线条
immBegin(GPU_PRIM_LINES, line_count);
for (int i = 0; i < line_count; i++) {
  immVertex2f(pos, x1, y1);
  immVertex2f(pos, x2, y2);
}
immEnd();

immUnbindProgram();
```

#### 10.1.2. 减少状态切换

```cpp
// 坏的做法：每个单元格切换一次
for (...) {
  immUniformThemeColor(...);
  immRectf(...);
}

// 好的做法：批量绘制相同颜色
immUniformThemeColor(TH_BACK);
immBegin(GPU_PRIM_RECTS, count);
for (...) {
  immRectf(pos, x1, y1, x2, y2);
}
immEnd();
```

### 10.2. 脏区域优化

#### 10.2.1. 部分重绘

```cpp
// 标记区域需要重绘
ED_region_tag_redraw(region);

// 只重绘指定区域
ED_region_tag_redraw_area(region, x, y, width, height);
```

#### 10.2.2. 避免不必要重绘

```cpp
// 只在数据变化时重绘
void on_data_change() {
  if (data_has_changed) {
    ED_region_tag_redraw(region);
  }
}
```

### 10.3. 缓存策略

#### 10.3.1. 列宽缓存

```cpp
class ColumnValues {
 private:
  mutable std::optional<float> cached_width_;

 public:
  float fit_column_width_px(...) const {
    if (cached_width_.has_value()) {
      return cached_width_.value();
    }

    float width = calculate_width(...);
    cached_width_ = width;
    return width;
  }

  void invalidate_cache() {
    cached_width_.reset();
  }
};
```

#### 10.3.2. 文本测量缓存

```cpp
// 使用全局缓存
static std::unordered_map<std::string, float> text_width_cache;

float get_cached_text_width(const std::string &text)
{
  auto it = text_width_cache.find(text);
  if (it != text_width_cache.end()) {
    return it->second;
  }

  float width = calculate_text_width(text);
  text_width_cache[text] = width;
  return width;
}
```

---

## 总结

渲染管道的关键设计点：

1. **分层绘制**：背景 → 装饰 → 内容 → 交互 → UI
2. **坐标系统**：屏幕坐标和视图坐标的转换
3. **可见性裁剪**：Scissor测试和范围计算
4. **批量绘制**：减少GPU状态切换
5. **缓存优化**：避免重复计算
6. **主题系统**：统一的样式管理

这些机制确保了电子表格的高效渲染和良好的视觉效果。

---

**文档版本**: 1.0
**最后更新**: 2025-12-19
**适用版本**: Blender 4.3+
