# Blender 电子表格UI交互与事件处理

## 概述

UI交互与事件处理系统负责处理用户输入、界面更新和区域管理。

**SPDX-License-Identifier**: GPL-2.0-or-later
**基于代码**: Blender 4.3 开发版本

---

## 区域管理

### 区域类型

```cpp
/* 来源: space_spreadsheet.cc */
enum eRegionType {
  RGN_TYPE_WINDOW = 0,    /* 主窗口 - 显示电子表格数据 */
  RGN_TYPE_HEADER = 1,    /* 顶部标题栏 */
  RGN_TYPE_FOOTER = 2,    /* 底部状态栏 */
  RGN_TYPE_UI = 3,        /* 右侧面板 - 过滤器 */
  RGN_TYPE_TOOLS = 4,     /* 左侧面板 - 数据集 */
};
```

### 区域注册

```cpp
/* 来源: space_spreadsheet.cc */
void register_spacetype()
{
  std::unique_ptr<SpaceType> st = std::make_unique<SpaceType>();
  ARegionType *art;

  st->spaceid = SPACE_SPREADSHEET;
  STRNCPY_UTF8(st->name, "Spreadsheet");

  /* 注册空间函数 */
  st->create = spreadsheet_create;
  st->free = spreadsheet_free;
  st->init = spreadsheet_init;
  st->duplicate = spreadsheet_duplicate;
  st->operatortypes = spreadsheet_operatortypes;
  st->keymap = spreadsheet_keymap;
  st->id_remap = spreadsheet_id_remap;
  st->foreach_id = spreadsheet_foreach_id;
  st->blend_read_data = spreadsheet_blend_read_data;
  st->blend_write = spreadsheet_blend_write;

  /* 主窗口区域 */
  art = MEM_callocN<ARegionType>("spacetype spreadsheet region");
  art->regionid = RGN_TYPE_WINDOW;
  art->keymapflag = ED_KEYMAP_UI | ED_KEYMAP_VIEW2D | ED_KEYMAP_FRAMES;
  art->lock = REGION_DRAW_LOCK_ALL;
  art->init = spreadsheet_main_region_init;
  art->draw = spreadsheet_main_region_draw;
  art->listener = spreadsheet_main_region_listener;
  art->cursor = spreadsheet_cursor;
  art->event_cursor = true;
  BLI_addhead(&st->regiontypes, art);

  /* 头部区域 */
  art = MEM_callocN<ARegionType>("spacetype spreadsheet header region");
  art->regionid = RGN_TYPE_HEADER;
  art->prefsizey = HEADERY;
  art->keymapflag = ED_KEYMAP_UI | ED_KEYMAP_VIEW2D | ED_KEYMAP_HEADER | ED_KEYMAP_FRAMES;
  art->lock = REGION_DRAW_LOCK_ALL;
  art->init = spreadsheet_header_region_init;
  art->draw = spreadsheet_header_region_draw;
  art->free = spreadsheet_header_region_free;
  art->listener = spreadsheet_header_region_listener;
  BLI_addhead(&st->regiontypes, art);

  /* 底部区域 */
  art = MEM_callocN<ARegionType>("spacetype spreadsheet footer region");
  art->regionid = RGN_TYPE_FOOTER;
  art->prefsizey = HEADERY;
  art->keymapflag = ED_KEYMAP_UI | ED_KEYMAP_VIEW2D | ED_KEYMAP_HEADER | ED_KEYMAP_FRAMES;
  art->lock = REGION_DRAW_LOCK_ALL;
  art->init = spreadsheet_footer_region_init;
  art->draw = spreadsheet_footer_region_draw;
  art->free = spreadsheet_footer_region_free;
  art->listener = spreadsheet_footer_region_listener;
  BLI_addhead(&st->regiontypes, art);

  /* 右侧面板 */
  art = MEM_callocN<ARegionType>("spacetype spreadsheet right region");
  art->regionid = RGN_TYPE_UI;
  art->prefsizex = UI_SIDEBAR_PANEL_WIDTH;
  art->keymapflag = ED_KEYMAP_UI | ED_KEYMAP_FRAMES;
  art->lock = REGION_DRAW_LOCK_ALL;
  art->init = spreadsheet_sidebar_init;
  art->layout = ED_region_panels_layout;
  art->draw = ED_region_panels_draw;
  art->free = spreadsheet_right_region_free;
  art->listener = spreadsheet_right_region_listener;
  register_row_filter_panels(*art);
  BLI_addhead(&st->regiontypes, art);

  /* 左侧面板 */
  art = MEM_callocN<ARegionType>("spreadsheet dataset region");
  art->regionid = RGN_TYPE_TOOLS;
  art->prefsizex = 150 + V2D_SCROLL_WIDTH;
  art->keymapflag = ED_KEYMAP_UI | ED_KEYMAP_FRAMES;
  art->lock = REGION_DRAW_LOCK_ALL;
  art->init = ED_region_panels_init;
  art->draw = spreadsheet_dataset_region_draw;
  art->listener = spreadsheet_dataset_region_listener;
  spreadsheet_data_set_region_panels_register(*art);
  BLI_addhead(&st->regiontypes, art);

  BKE_spacetype_register(std::move(st));
}
```

---

## 主窗口区域处理

### 初始化

```cpp
/* 来源: space_spreadsheet.cc */
static void spreadsheet_main_region_init(wmWindowManager *wm, ARegion *region)
{
  /* 1. 配置 View2D */
  region->v2d.scroll = V2D_SCROLL_RIGHT | V2D_SCROLL_BOTTOM |
                       V2D_SCROLL_VERTICAL_HIDE | V2D_SCROLL_HORIZONTAL_HIDE;
  region->v2d.align = V2D_ALIGN_NO_NEG_X | V2D_ALIGN_NO_POS_Y;
  region->v2d.keepzoom = V2D_LOCKZOOM_X | V2D_LOCKZOOM_Y | V2D_LIMITZOOM | V2D_KEEPASPECT;
  region->v2d.keeptot = V2D_KEEPTOT_STRICT;
  region->v2d.minzoom = region->v2d.maxzoom = 1.0f;

  /* 2. 初始化 View2D */
  view2d_region_reinit(®ion->v2d, ui::V2D_COMMONVIEW_LIST, region->winx, region->winy);

  /* 3. 设置溢出指示器标志 */
  region->flag |= RGN_FLAG_INDICATE_OVERFLOW;

  /* 4. 添加键映射处理 */
  wmKeyMap *keymap = WM_keymap_ensure(
      wm->runtime->defaultconf, "View2D Buttons List", SPACE_EMPTY, RGN_TYPE_WINDOW);
  WM_event_add_keymap_handler(®ion->runtime->handlers, keymap);

  keymap = WM_keymap_ensure(
      wm->runtime->defaultconf, "Spreadsheet Generic", SPACE_SPREADSHEET, RGN_TYPE_WINDOW);
  WM_event_add_keymap_handler(®ion->runtime->handlers, keymap);
}
```

### 绘制流程

```cpp
/* 来源: space_spreadsheet.cc */
static void spreadsheet_main_region_draw(const bContext *C, ARegion *region)
{
  SpaceSpreadsheet *sspreadsheet = CTX_wm_space_spreadsheet(C);

  /* 1. 更新上下文 (自动切换到活跃对象) */
  spreadsheet_update_context(C);

  /* 2. 获取数据源 */
  std::unique_ptr<DataSource> data_source = get_data_source(*C);
  if (!data_source) {
    data_source = std::make_unique<DataSource>();
  }

  /* 3. 获取或创建活跃表格 */
  const SpreadsheetTableID *active_table_id = get_active_table_id(*sspreadsheet);
  SpreadsheetTable *table = spreadsheet_table_find(*sspreadsheet, *active_table_id);
  if (!table) {
    spreadsheet_table_remove_unused(*sspreadsheet);
    table = spreadsheet_table_new(spreadsheet_table_id_copy(*active_table_id));
    spreadsheet_table_add(*sspreadsheet, table);
  }

  /* 4. 移动到前面 (LRU) */
  if (table) {
    spreadsheet_table_move_to_front(*sspreadsheet, *table);
  }

  /* 5. 更新表格时间戳 */
  if (table->last_used < sspreadsheet->table_use_clock || sspreadsheet->table_use_clock == 0) {
    sspreadsheet->table_use_clock++;
    if (sspreadsheet->table_use_clock == 0) {
      for (SpreadsheetTable *table : Span(sspreadsheet->tables, sspreadsheet->num_tables)) {
        table->last_used = sspreadsheet->table_use_clock;
      }
    }
    table->last_used = sspreadsheet->table_use_clock;
  }

  /* 6. 更新可见列 */
  update_visible_columns(*table, *data_source);

  /* 7. 构建布局 */
  SpreadsheetLayout spreadsheet_layout;
  ResourceScope scope;

  const int tot_rows = data_source->tot_rows();
  spreadsheet_layout.index_column_width = get_index_column_width(tot_rows);

  int x = spreadsheet_layout.index_column_width;

  for (SpreadsheetColumn *column : Span{table->columns, table->num_columns}) {
    std::unique_ptr<ColumnValues> values_ptr = data_source->get_column_values(*column->id);
    if (!values_ptr) {
      continue;
    }
    const ColumnValues *values = scope.add(std::move(values_ptr));
    const eSpreadsheetColumnValueType column_type = values->type();

    /* 计算列宽 */
    if (column->width <= 0.0f ||
        !ELEM(column_type, column->data_type, SPREADSHEET_VALUE_TYPE_UNKNOWN))
    {
      column->width = values->fit_column_width_px(100) / SPREADSHEET_WIDTH_UNIT;
    }

    const int width_in_pixels = column->width * SPREADSHEET_WIDTH_UNIT;
    spreadsheet_layout.columns.append({values, width_in_pixels});

    /* 更新运行时位置 */
    column->runtime->left_x = x;
    x += width_in_pixels;
    column->runtime->right_x = x;

    spreadsheet_column_assign_runtime_data(column, column_type, values->name());
  }

  /* 8. 应用行过滤 */
  spreadsheet_layout.row_indices = spreadsheet_filter_rows(
      *sspreadsheet, spreadsheet_layout, *data_source, scope);

  /* 9. 更新运行时统计 */
  sspreadsheet->runtime->tot_columns = spreadsheet_layout.columns.size();
  sspreadsheet->runtime->tot_rows = tot_rows;
  sspreadsheet->runtime->visible_rows = spreadsheet_layout.row_indices.size();

  /* 10. 绘制 */
  std::unique_ptr<SpreadsheetDrawer> drawer = spreadsheet_drawer_from_layout(spreadsheet_layout);
  draw_spreadsheet_in_region(C, region, *drawer);

  /* 11. 更新运行时尺寸 */
  sspreadsheet->runtime->top_row_height = drawer->top_row_height;
  sspreadsheet->runtime->left_column_width = drawer->left_column_width;

  /* 12. 绘制溢出指示器 */
  rcti mask;
  ui::view2d_mask_from_win(®ion->v2d, &mask);
  mask.ymax -= sspreadsheet->runtime->top_row_height;
  ED_region_draw_overflow_indication(CTX_wm_area(C), region, &mask);

  /* 13. 标记其他区域重绘 */
  ARegion *footer = BKE_area_find_region_type(CTX_wm_area(C), RGN_TYPE_FOOTER);
  ED_region_tag_redraw(footer);
  ARegion *sidebar = BKE_area_find_region_type(CTX_wm_area(C), RGN_TYPE_UI);
  ED_region_tag_redraw(sidebar);
}
```

### 上下文更新

```cpp
/* 来源: space_spreadsheet.cc */
static void spreadsheet_update_context(const bContext *C)
{
  using blender::ed::viewer_path::ViewerPathForGeometryNodesViewer;

  SpaceSpreadsheet *sspreadsheet = CTX_wm_space_spreadsheet(C);
  Object *active_object = CTX_data_active_object(C);
  Object *context_object = blender::ed::viewer_path::parse_object_only(
      sspreadsheet->geometry_id.viewer_path);

  switch (eSpaceSpreadsheet_ObjectEvalState(sspreadsheet->geometry_id.object_eval_state)) {
    case SPREADSHEET_OBJECT_EVAL_STATE_ORIGINAL:
    case SPREADSHEET_OBJECT_EVAL_STATE_EVALUATED: {
      if (sspreadsheet->flag & SPREADSHEET_FLAG_PINNED) {
        if (context_object == nullptr) {
          /* 对象已不存在，清除固定 */
          sspreadsheet->flag &= ~SPREADSHEET_FLAG_PINNED;
        }
        /* 否则保持固定，不做任何操作 */
      }
      else {
        if (active_object != context_object) {
          /* 活跃对象已改变，查看新对象 */
          view_active_object(C, sspreadsheet);
        }
      }
      break;
    }

    case SPREADSHEET_OBJECT_EVAL_STATE_VIEWER_NODE: {
      WorkSpace *workspace = CTX_wm_workspace(C);
      if (sspreadsheet->flag & SPREADSHEET_FLAG_PINNED) {
        const std::optional<ViewerPathForGeometryNodesViewer> parsed_path =
            blender::ed::viewer_path::parse_geometry_nodes_viewer(
                sspreadsheet->geometry_id.viewer_path);
        if (parsed_path.has_value()) {
          if (blender::ed::viewer_path::exists_geometry_nodes_viewer(*parsed_path)) {
            /* 固定的路径仍然有效 */
            break;
          }
          /* 路径已不存在，清除固定 */
          sspreadsheet->flag &= ~SPREADSHEET_FLAG_PINNED;
        }
        else {
          /* 未知路径，清除固定 */
          sspreadsheet->flag &= ~SPREADSHEET_FLAG_PINNED;
        }
      }

      /* 从工作区更新查看器路径 */
      const std::optional<ViewerPathForGeometryNodesViewer> workspace_parsed_path =
          blender::ed::viewer_path::parse_geometry_nodes_viewer(workspace->viewer_path);
      if (workspace_parsed_path.has_value()) {
        if (BKE_viewer_path_equal(&sspreadsheet->geometry_id.viewer_path,
                                  &workspace->viewer_path,
                                  VIEWER_PATH_EQUAL_FLAG_CONSIDER_UI_NAME))
        {
          /* 没有变化 */
          break;
        }
        /* 从工作区更新路径 */
        BKE_viewer_path_clear(&sspreadsheet->geometry_id.viewer_path);
        BKE_viewer_path_copy(&sspreadsheet->geometry_id.viewer_path, &workspace->viewer_path);
      }
      else {
        /* 没有活跃的查看器节点，切换回评估后的活跃对象 */
        sspreadsheet->geometry_id.object_eval_state = SPREADSHEET_OBJECT_EVAL_STATE_EVALUATED;
        view_active_object(C, sspreadsheet);
      }
      break;
    }
  }
}
```

### 查看活跃对象

```cpp
/* 来源: space_spreadsheet.cc */
static void view_active_object(const bContext *C, SpaceSpreadsheet *sspreadsheet)
{
  BKE_viewer_path_clear(&sspreadsheet->geometry_id.viewer_path);

  Object *ob = CTX_data_active_object(C);
  if (ob == nullptr) {
    return;
  }

  IDViewerPathElem *id_elem = BKE_viewer_path_elem_new_id();
  id_elem->id = &ob->id;
  BLI_addtail(&sspreadsheet->geometry_id.viewer_path.path, id_elem);

  ED_area_tag_redraw(CTX_wm_area(C));
}
```

---

## 头部区域处理

### 初始化和绘制

```cpp
/* 来源: space_spreadsheet.cc */
static void spreadsheet_header_region_init(wmWindowManager * /*wm*/, ARegion *region)
{
  ED_region_header_init(region);
}

static void spreadsheet_header_region_draw(const bContext *C, ARegion *region)
{
  spreadsheet_update_context(C);
  ED_region_header(C, region);
}
```

### 监听器

```cpp
/* 来源: space_spreadsheet.cc */
static void spreadsheet_header_region_listener(const wmRegionListenerParams *params)
{
  ARegion *region = params->region;
  const wmNotifier *wmn = params->notifier;
  SpaceSpreadsheet *sspreadsheet = static_cast<SpaceSpreadsheet *>(params->area->spacedata.first);

  switch (wmn->category) {
    case NC_SCENE:
      switch (wmn->data) {
        case ND_MODE:
        case ND_OB_ACTIVE:
          ED_region_tag_redraw(region);
          break;
      }
      break;

    case NC_OBJECT:
      ED_region_tag_redraw(region);
      break;

    case NC_SPACE:
      if (wmn->data == ND_SPACE_SPREADSHEET) {
        ED_region_tag_redraw(region);
      }
      break;

    case NC_GEOM:
    case NC_GPENCIL:
      ED_region_tag_redraw(region);
      break;

    case NC_VIEWER_PATH:
      if (sspreadsheet->geometry_id.object_eval_state == SPREADSHEET_OBJECT_EVAL_STATE_VIEWER_NODE)
      {
        ED_region_tag_redraw(region);
      }
      break;
  }
}
```

---

## 底部区域处理

### 绘制状态信息

```cpp
/* 来源: space_spreadsheet.cc */
static void spreadsheet_footer_region_draw(const bContext *C, ARegion *region)
{
  SpaceSpreadsheet *sspreadsheet = CTX_wm_space_spreadsheet(C);
  SpaceSpreadsheet_Runtime *runtime = sspreadsheet->runtime;

  /* 1. 构建状态字符串 */
  std::stringstream ss;
  ss << IFACE_("Rows:") << " ";

  if (runtime->visible_rows != runtime->tot_rows) {
    char visible_rows_str[BLI_STR_FORMAT_INT32_GROUPED_SIZE];
    BLI_str_format_int_grouped(visible_rows_str, runtime->visible_rows);
    ss << visible_rows_str << " / ";
  }

  char tot_rows_str[BLI_STR_FORMAT_INT32_GROUPED_SIZE];
  BLI_str_format_int_grouped(tot_rows_str, runtime->tot_rows);
  ss << tot_rows_str << "   |   " << IFACE_("Columns:") << " " << runtime->tot_columns;

  std::string stats_str = ss.str();

  /* 2. 清除背景 */
  ui::theme::frame_buffer_clear(TH_BACK);

  /* 3. 创建 UI 块 */
  ui::Block *block = block_begin(C, region, __func__, ui::EmbossType::Emboss);
  const uiStyle *style = ui::style_get_dpi();

  /* 4. 创建布局 */
  ui::Layout &layout = ui::block_layout(block,
                                        ui::LayoutDirection::Horizontal,
                                        ui::LayoutType::Header,
                                        UI_HEADER_OFFSET,
                                        region->winy - (region->winy - UI_UNIT_Y) / 2.0f,
                                        region->winx,
                                        1,
                                        0,
                                        style);

  /* 5. 添加内容 */
  layout.separator_spacer();
  layout.alignment_set(ui::LayoutAlign::Right);
  layout.label(stats_str, ICON_NONE);

  /* 6. 结束并绘制 */
  ui::block_layout_resolve(block);
  block_align_end(block);
  block_end(C, block);
  block_draw(C, block);
}
```

---

## 数据集区域处理

### 面板注册

```cpp
/* 来源: spreadsheet_panels.cc */
void spreadsheet_data_set_region_panels_register(ARegionType ®ion_type)
{
  PanelType *panel_type = MEM_callocN<PanelType>(__func__);
  STRNCPY_UTF8(panel_type->idname, "SPREADSHEET_PT_data_set");
  STRNCPY_UTF8(panel_type->label, N_("Data Set"));
  STRNCPY_UTF8(panel_type->translation_context, BLT_I18NCONTEXT_DEFAULT_BPYRNA);
  panel_type->flag = PANEL_TYPE_NO_HEADER;
  panel_type->draw = spreadsheet_data_set_panel_draw;
  BLI_addtail(®ion_type.paneltypes, panel_type);
}
```

### 绘制数据集面板

```cpp
/* 来源: spreadsheet_dataset_draw.cc (概念) */
void spreadsheet_data_set_panel_draw(const bContext *C, Panel *panel)
{
  SpaceSpreadsheet *sspreadsheet = CTX_wm_space_spreadsheet(C);
  ui::Layout &layout = *panel->layout;

  /* 显示当前数据源信息 */
  const SpreadsheetTableID *table_id = get_active_table_id(*sspreadsheet);

  if (table_id->type == SPREADSHEET_TABLE_ID_TYPE_GEOMETRY) {
    const auto &geo_id = *reinterpret_cast<const SpreadsheetTableIDGeometry *>(table_id);

    /* 显示组件类型 */
    layout.label("Component: Mesh", ICON_NONE);

    /* 显示域 */
    layout.label("Domain: Point", ICON_NONE);

    /* 显示对象名称 */
    ID *id = get_current_id(sspreadsheet);
    if (id) {
      layout.label(BKE_id_name(*id), ICON_OBJECT_DATA);
    }
  }
}
```

---

## 右侧面板处理 (过滤器)

### 侧边栏初始化

```cpp
/* 来源: space_spreadsheet.cc */
static void spreadsheet_sidebar_init(wmWindowManager *wm, ARegion *region)
{
  /* 1. 设置默认面板类别 */
  ui::panel_category_active_set_default(region, "Filters");

  /* 2. 初始化面板 */
  ED_region_panels_init(wm, region);

  /* 3. 添加键映射 */
  wmKeyMap *keymap = WM_keymap_ensure(
      wm->runtime->defaultconf, "Spreadsheet Generic", SPACE_SPREADSHEET, RGN_TYPE_WINDOW);
  WM_event_add_keymap_handler(®ion->runtime->handlers, keymap);
}
```

### 过滤器面板

```cpp
/* 来源: spreadsheet_row_filter_ui.cc */

/* 面板标题绘制 */
static void spreadsheet_filter_panel_draw_header(const bContext *C, Panel *panel)
{
  ui::Layout &layout = *panel->layout;
  SpaceSpreadsheet *sspreadsheet = CTX_wm_space_spreadsheet(C);
  PointerRNA *filter_ptr = ui::panel_custom_data_get(panel);
  const SpreadsheetRowFilter *filter = (SpreadsheetRowFilter *)filter_ptr->data;

  /* 查找列 */
  const SpreadsheetColumn *column = lookup_visible_column_for_filter(*sspreadsheet,
                                                                     filter->column_name);

  /* 检查启用状态 */
  if (!(sspreadsheet->filter_flag & SPREADSHEET_FILTER_ENABLE) ||
      (column == nullptr && filter->column_name[0]))
  {
    layout.active_set(false);
  }

  /* 启用复选框 */
  ui::Layout *row = &layout.row(true);
  row->emboss_set(ui::EmbossType::None);
  row->prop(filter_ptr, "enabled", ui::ITEM_R_ICON_ONLY, "", ICON_NONE);

  /* 显示过滤器描述 */
  if (filter->column_name[0] == '\0') {
    row->label(IFACE_("Filter"), ICON_NONE);
  }
  else if (column == nullptr) {
    row->label(filter->column_name, ICON_NONE);
  }
  else {
    const eSpreadsheetColumnValueType data_type = (eSpreadsheetColumnValueType)column->data_type;
    std::stringstream ss;
    ss << filter->column_name;
    ss << " ";
    ss << operation_string(data_type, (eSpreadsheetFilterOperation)filter->operation);
    ss << " ";
    ss << value_string(*filter, data_type);
    row->label(ss.str(), ICON_NONE);
  }

  /* 删除按钮 */
  row = &layout.row(true);
  row->emboss_set(ui::EmbossType::None);
  const int current_index = BLI_findindex(&sspreadsheet->row_filters, filter);
  PointerRNA op_ptr = row->op("SPREADSHEET_OT_remove_row_filter_rule", "", ICON_X);
  RNA_int_set(&op_ptr, "index", current_index);
}

/* 面板内容绘制 */
static void spreadsheet_filter_panel_draw(const bContext *C, Panel *panel)
{
  ui::Layout &layout = *panel->layout;
  SpaceSpreadsheet *sspreadsheet = CTX_wm_space_spreadsheet(C);
  PointerRNA *filter_ptr = ui::panel_custom_data_get(panel);
  SpreadsheetRowFilter *filter = (SpreadsheetRowFilter *)filter_ptr->data;

  const SpreadsheetColumn *column = lookup_visible_column_for_filter(*sspreadsheet,
                                                                     filter->column_name);

  /* 列选择器 */
  ui::Layout *row = &layout.row(true);
  row->prop(filter_ptr, "column_name", ui::ITEM_R_DEFAULT, "", ICON_NONE);

  /* 操作符选择器 */
  if (column) {
    const eSpreadsheetColumnValueType data_type = (eSpreadsheetColumnValueType)column->data_type;
    row = &layout.row(true);

    if (ELEM(data_type, SPREADSHEET_VALUE_TYPE_BOOL, SPREADSHEET_VALUE_TYPE_INSTANCES)) {
      row->prop(filter_ptr, "operation", ui::ITEM_R_DEFAULT, "", ICON_NONE);
    }
    else {
      row->prop(filter_ptr, "operation", ui::ITEM_R_DEFAULT, "", ICON_NONE);
    }
  }

  /* 值输入器 */
  if (column) {
    const eSpreadsheetColumnValueType data_type = (eSpreadsheetColumnValueType)column->data_type;

    switch (data_type) {
      case SPREADSHEET_VALUE_TYPE_BOOL:
        row = &layout.row(true);
        row->prop(filter_ptr, "flag", ui::ITEM_R_TOGGLE, IFACE_("Value"), ICON_NONE);
        break;

      case SPREADSHEET_VALUE_TYPE_INT8:
      case SPREADSHEET_VALUE_TYPE_INT32:
      case SPREADSHEET_VALUE_TYPE_INT64:
        row = &layout.row(true);
        row->prop(filter_ptr, "value_int", ui::ITEM_R_DEFAULT, IFACE_("Value"), ICON_NONE);
        break;

      case SPREADSHEET_VALUE_TYPE_FLOAT:
        row = &layout.row(true);
        row->prop(filter_ptr, "value_float", ui::ITEM_R_DEFAULT, IFACE_("Value"), ICON_NONE);
        row = &layout.row(true);
        row->prop(filter_ptr, "threshold", ui::ITEM_R_DEFAULT, IFACE_("Threshold"), ICON_NONE);
        break;

      case SPREADSHEET_VALUE_TYPE_FLOAT3:
        row = &layout.row(true);
        row->prop(filter_ptr, "value_float3", ui::ITEM_R_DEFAULT, IFACE_("Value"), ICON_NONE);
        break;

      case SPREADSHEET_VALUE_TYPE_COLOR:
      case SPREADSHEET_VALUE_TYPE_BYTE_COLOR:
        row = &layout.row(true);
        row->prop(filter_ptr, "value_color", ui::ITEM_R_DEFAULT, IFACE_("Value"), ICON_NONE);
        row = &layout.row(true);
        row->prop(filter_ptr, "threshold", ui::ITEM_R_DEFAULT, IFACE_("Threshold"), ICON_NONE);
        break;

      case SPREADSHEET_VALUE_TYPE_INSTANCES:
      case SPREADSHEET_VALUE_TYPE_STRING:
        row = &layout.row(true);
        row->prop(filter_ptr, "value_string", ui::ITEM_R_DEFAULT, IFACE_("Value"), ICON_NONE);
        break;

      default:
        break;
    }
  }
}
```

---

## 事件处理

### 光标更新

```cpp
/* 来源: space_spreadsheet.cc */
static void spreadsheet_cursor(wmWindow *win, ScrArea *area, ARegion *region)
{
  SpaceSpreadsheet &sspreadsheet = *static_cast<SpaceSpreadsheet *>(area->spacedata.first);

  const int2 cursor_re{win->runtime->eventstate->xy[0] - region->winrct.xmin,
                       win->runtime->eventstate->xy[1] - region->winrct.ymin};

  /* 检查是否悬停在列边缘 */
  if (find_hovered_column_header_edge(sspreadsheet, *region, cursor_re)) {
    WM_cursor_set(win, WM_CURSOR_X_MOVE);
    return;
  }

  /* 检查是否悬停在列头 */
  if (find_hovered_column_header(sspreadsheet, *region, cursor_re)) {
    WM_cursor_set(win, WM_CURSOR_HAND);
    return;
  }

  /* 默认光标 */
  WM_cursor_set(win, WM_CURSOR_DEFAULT);
}
```

### 通知监听器

```cpp
/* 来源: space_spreadsheet.cc */
static void spreadsheet_main_region_listener(const wmRegionListenerParams *params)
{
  ARegion *region = params->region;
  const wmNotifier *wmn = params->notifier;
  SpaceSpreadsheet *sspreadsheet = static_cast<SpaceSpreadsheet *>(params->area->spacedata.first);

  switch (wmn->category) {
    case NC_SCENE:
      switch (wmn->data) {
        case ND_MODE:
        case ND_FRAME:
        case ND_OB_ACTIVE:
          ED_region_tag_redraw(region);
          break;
      }
      break;

    case NC_OBJECT:
      ED_region_tag_redraw(region);
      break;

    case NC_SPACE:
      if (wmn->data == ND_SPACE_SPREADSHEET) {
        ED_region_tag_redraw(region);
      }
      break;

    case NC_TEXTURE:
    case NC_GEOM:
      ED_region_tag_redraw(region);
      break;

    case NC_GPENCIL:
      ED_region_tag_redraw(region);
      break;

    case NC_VIEWER_PATH:
      if (sspreadsheet->geometry_id.object_eval_state == SPREADSHEET_OBJECT_EVAL_STATE_VIEWER_NODE)
      {
        ED_region_tag_redraw(region);
      }
      break;
  }
}
```

---

## 键映射

### 通用键映射

```cpp
/* 来源: space_spreadsheet.cc */
static void spreadsheet_keymap(wmKeyConfig *keyconf)
{
  /* 通用电子表格键映射 */
  WM_keymap_ensure(keyconf, "Spreadsheet Generic", SPACE_SPREADSHEET, RGN_TYPE_WINDOW);
}
```

### View2D 键映射

```cpp
/* 在区域初始化时添加 */
wmKeyMap *keymap = WM_keymap_ensure(
    wm->runtime->defaultconf, "View2D Buttons List", SPACE_EMPTY, RGN_TYPE_WINDOW);
WM_event_add_keymap_handler(®ion->runtime->handlers, keymap);
```

---

## 鼠标事件处理

### 悬停检测

```cpp
/* 来源: spreadsheet_ops.cc */

/* 检查是否悬停在列边缘 */
SpreadsheetColumn *find_hovered_column_edge(SpaceSpreadsheet &sspreadsheet,
                                            ARegion ®ion,
                                            const int2 &cursor_re)
{
  SpreadsheetTable *table = get_active_table(sspreadsheet);
  if (!table) {
    return nullptr;
  }

  const float cursor_x_view = ui::view2d_region_to_view_x(®ion.v2d, cursor_re.x);

  for (SpreadsheetColumn *column : Span{table->columns, table->num_columns}) {
    if (column->flag & SPREADSHEET_COLUMN_FLAG_UNAVAILABLE) {
      continue;
    }

    if (std::abs(cursor_x_view - column->runtime->right_x) < SPREADSHEET_EDGE_ACTION_ZONE) {
      return column;
    }
  }

  return nullptr;
}

/* 检查是否悬停在列头 */
static bool is_hovering_header_row(const SpaceSpreadsheet &sspreadsheet,
                                   const ARegion ®ion,
                                   const int2 &cursor_re)
{
  const int region_height = BLI_rcti_size_y(®ion.winrct);
  return cursor_re.y >= region_height - sspreadsheet.runtime->top_row_height &&
         cursor_re.y <= region_height;
}
```

### 滚动处理

```cpp
/* View2D 自动处理滚动 */
region->v2d.scroll = V2D_SCROLL_RIGHT | V2D_SCROLL_BOTTOM |
                     V2D_SCROLL_VERTICAL_HIDE | V2D_SCROLL_HORIZONTAL_HIDE;

/* 滚动事件通过 View2D 系统自动处理 */
```

---

## UI 布局系统

### 面板系统

```cpp
/* 面板注册 */
void register_row_filter_panels(ARegionType &art)
{
  PanelType *pt = MEM_callocN<PanelType>(__func__);
  STRNCPY_UTF8(pt->idname, "SPREADSHEET_PT_filter");
  STRNCPY_UTF8(pt->label, "Filter");
  pt->draw_header = spreadsheet_filter_panel_draw_header;
  pt->draw = spreadsheet_filter_panel_draw;
  pt->flag = PANEL_TYPE_DEFAULT_OPEN;
  BLI_addtail(&art.paneltypes, pt);
}
```

### 布局创建

```cpp
/* 创建 UI 块 */
ui::Block *block = block_begin(C, region, __func__, ui::EmbossType::None);

/* 创建布局 */
ui::Layout &layout = ui::block_layout(block,
                                      ui::LayoutDirection::Horizontal,
                                      ui::LayoutType::Header,
                                      UI_HEADER_OFFSET,
                                      region->winy - (region->winy - UI_UNIT_Y) / 2.0f,
                                      region->winx,
                                      1,
                                      0,
                                      style);

/* 添加元素 */
layout.separator_spacer();
layout.alignment_set(ui::LayoutAlign::Right);
layout.label("Text", ICON_NONE);

/* 结束布局 */
ui::block_layout_resolve(block);
block_align_end(block);
block_end(C, block);
block_draw(C, block);
```

---

## 通知系统

### 通知类型

```cpp
/* 来源: WM_types.hh */

/* 类别 */
NC_SCENE = 1,
NC_OBJECT = 2,
NC_SPACE = 3,
NC_GEOM = 4,
NC_GPENCIL = 5,
NC_VIEWER_PATH = 6,

/* 子类型 */
ND_SPACE_SPREADSHEET = 1,  /* 电子表格空间更新 */
```

### 发送通知

```cpp
/* 操作完成后 */
WM_event_add_notifier(C, NC_SPACE | ND_SPACE_SPREADSHEET, sspreadsheet);

/* 区域重绘 */
ED_region_tag_redraw(region);

/* 全局重绘 */
WM_main_add_notifier(NC_SPACE | ND_SPACE_SPREADSHEET, nullptr);
```

### 接收通知

```cpp
/* 在监听器中 */
static void spreadsheet_main_region_listener(const wmRegionListenerParams *params)
{
  const wmNotifier *wmn = params->notifier;

  switch (wmn->category) {
    case NC_SPACE:
      if (wmn->data == ND_SPACE_SPREADSHEET) {
        ED_region_tag_redraw(region);
      }
      break;
  }
}
```

---

## 拖拽交互

### 列重排序

```cpp
/* 见 07_操作器系统.md 中的 reorder_columns 操作器 */

/* 关键事件:
   - invoke: 检查点击，初始化数据
   - modal: 处理 MOUSEMOVE 更新位置，LEFTMOUSE 完成，RIGHTMOUSE 取消
   - cleanup: 释放资源，重置光标
*/
```

### 列宽调整

```cpp
/* 见 07_操作器系统.md 中的 resize_column 操作器 */

/* 关键事件:
   - invoke: 检查边缘点击
   - modal: MOUSEMOVE 更新宽度，LEFTMOUSE 完成
*/
```

---

## 错误处理和反馈

### 空数据源

```cpp
std::unique_ptr<DataSource> data_source = get_data_source(*C);
if (!data_source) {
  data_source = std::make_unique<DataSource>();  /* 空数据源 */
}
```

### 无效操作

```cpp
SpreadsheetColumn *column = find_hovered_column_header_edge(...);
if (!column) {
  return OPERATOR_PASS_THROUGH;  /* 传递给其他操作器 */
}
```

### 用户反馈

```cpp
/* 状态栏更新 */
void spreadsheet_footer_region_draw(...) {
  /* 显示行数/列数统计 */
}

/* 工具提示 */
button_func_tooltip_set(but, tooltip_function, data, cleanup);
```

---

## 性能优化

### 1. 延迟重绘

```cpp
/* 只在需要时重绘 */
ED_region_tag_redraw(region);  /* 标记为需要重绘 */
/* 实际重绘在事件循环中进行 */
```

### 2. 事件过滤

```cpp
/* 只处理相关事件 */
switch (event->type) {
  case MOUSEMOVE:
  case LEFTMOUSE:
  case RIGHTMOUSE:
    /* 处理 */
    break;
  default:
    return OPERATOR_PASS_THROUGH;
}
```

### 3. 区域锁

```cpp
/* 防止并发访问 */
art->lock = REGION_DRAW_LOCK_ALL;
```

---

## 完整事件流程

### 1. 鼠标移动

```
鼠标移动
    ↓
WM_event_add_mousemove()
    ↓
查找区域和操作器
    ↓
调用 spreadsheet_cursor() 更新光标
    ↓
如果悬停列边缘 → WM_CURSOR_X_MOVE
如果悬停列头 → WM_CURSOR_HAND
否则 → WM_CURSOR_DEFAULT
```

### 2. 鼠标点击

```
鼠标点击
    ↓
WM_event_add_mouseclick()
    ↓
查找匹配的操作器
    ↓
调用操作器的 invoke()
    ↓
如果是模态操作器 → 添加到模态处理
    ↓
操作器 modal() 处理后续事件
```

### 3. 滚动

```
鼠标滚轮/滚动条
    ↓
View2D 系统处理
    ↓
更新 region->v2d.cur
    ↓
触发区域重绘
    ↓
draw_spreadsheet_in_region() 使用新偏移绘制
```

### 4. 键盘

```
按键
    ↓
键映射匹配
    ↓
调用对应操作器
    ↓
执行操作
    ↓
通知更新
```

---

## 总结

UI交互与事件处理系统的关键特点：

1. **区域分离**: 5个独立区域各司其职
2. **事件驱动**: 基于通知和回调的更新机制
3. **模态交互**: 支持复杂的拖拽和调整操作
4. **实时反馈**: 光标变化、状态栏更新
5. **键映射集成**: 标准 Blender 键映射系统

**核心组件**:
- `ARegionType`: 区域定义
- `wmRegionListenerParams`: 通知处理
- `wmEvent`: 事件数据
- `bContext`: 上下文信息

**交互模式**:
- **即时**: 按钮点击、菜单选择
- **模态**: 拖拽、调整
- **滚动**: View2D 系统
- **键盘**: 快捷键

**更新机制**:
- `ED_region_tag_redraw()`: 标记重绘
- `WM_event_add_notifier()`: 发送通知
- `spreadsheet_main_region_listener()`: 接收通知

---

**文档版本**: 1.0
**最后更新**: 2025-12-19
