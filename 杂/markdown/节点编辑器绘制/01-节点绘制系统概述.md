# Blender 节点绘制系统概述

## 1. 系统架构概览

Blender 的节点编辑器绘制系统是一个复杂而精密的图形渲染架构，负责将节点树以可视化的方式呈现给用户。该系统采用分层设计，将绘制逻辑划分为多个层次，每个层次负责特定的渲染任务。

```mermaid
flowchart TB
    subgraph 用户交互层["🖱️ 用户交互层"]
        A[鼠标事件处理] --> B[节点选择]
        A --> C[链接拖拽]
        A --> D[节点移动]
    end

    subgraph 绘制管理层["📐 绘制管理层"]
        E[节点排序] --> F[绘制顺序计算]
        F --> G[视口裁剪]
    end

    subgraph 核心绘制层["🎨 核心绘制层"]
        H[背景绘制] --> I[节点绘制]
        I --> J[链接绘制]
        J --> K[Socket绘制]
    end

    subgraph GPU渲染层["⚡ GPU渲染层"]
        L[批次渲染] --> M[着色器程序]
        M --> N[顶点缓冲]
    end

    用户交互层 --> 绘制管理层
    绘制管理层 --> 核心绘制层
    核心绘制层 --> GPU渲染层

    style 用户交互层 fill:#e1f5fe,stroke:#01579b,stroke-width:2px
    style 绘制管理层 fill:#fff3e0,stroke:#e65100,stroke-width:2px
    style 核心绘制层 fill:#f3e5f5,stroke:#6a1b9a,stroke-width:2px
    style GPU渲染层 fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
```

## 2. 核心文件结构

节点绘制系统主要由以下文件组成：

| 文件路径 | 主要职责 | 代码规模 |
|---------|---------|---------|
| `source/blender/editors/space_node/node_draw.cc` | 高级节点绘制逻辑 | ~2500 行 |
| `source/blender/editors/space_node/drawnode.cc` | 底层绘制辅助函数 | ~2440 行 |
| `source/blender/editors/space_node/node_intern.hh` | 内部接口定义 | ~827 行 |
| `source/blender/editors/space_node/space_node.cc` | 空间管理器 | ~400+ 行 |

```mermaid
flowchart LR
    subgraph 文件依赖关系["📁 文件依赖关系"]
        A[node_draw.cc<br/>高级绘制] --> C[node_intern.hh<br/>接口定义]
        B[drawnode.cc<br/>底层绘制] --> C
        D[space_node.cc<br/>空间管理] --> C
        E[其他模块] --> C
    end

    style A fill:#bbdefb,stroke:#1565c0,stroke-width:2px
    style B fill:#bbdefb,stroke:#1565c0,stroke-width:2px
    style C fill:#ffccbc,stroke:#d84315,stroke-width:3px
    style D fill:#bbdefb,stroke:#1565c0,stroke-width:2px
```

## 3. 关键数据结构

### 3.1 TreeDrawContext - 绘制上下文

`TreeDrawContext` 是节点绘制的核心上下文结构，贯穿整个绘制流程：

```cpp
struct TreeDrawContext {
    Main *bmain;                    // 主数据库
    wmWindow *window;               // 当前窗口
    Scene *scene;                   // 当前场景
    ARegion *region;                // 绘制区域
    Depsgraph *depsgraph;           // 依赖图
    
    // 几何节点特定数据
    const bNode *active_geometry_nodes_viewer = nullptr;
    geo_log::ContextualGeoTreeLogs tree_logs;
    
    // 预览和缓存
    NestedTreePreviews *nested_group_infos = nullptr;
    
    // 运行时计算数据
    Map<const bNode *, StringRef> reroute_auto_labels;
    Map<const bNode *, const bNode *> menu_switch_source_by_index_switch;
    Array<Vector<NodeExtraInfoRow>> extra_info_rows_per_node;
};
```

```mermaid
classDiagram
    class TreeDrawContext {
        +Main* bmain
        +wmWindow* window
        +Scene* scene
        +ARegion* region
        +Depsgraph* depsgraph
        +bNode* active_geometry_nodes_viewer
        +ContextualGeoTreeLogs tree_logs
        +NestedTreePreviews* nested_group_infos
        +Map reroute_auto_labels
        +Array extra_info_rows_per_node
        +~TreeDrawContext()
    }

    class bNode {
        +float2 location
        +float width
        +float height
        +int flag
        +int ui_order
        +bNodeRuntime* runtime
        +socket_by_decl()
        +input_sockets()
        +output_sockets()
    }

    class bNodeSocket {
        +int type
        +int flag
        +int in_out
        +float2 runtime~location
        +is_visible()
        +is_icon_visible()
    }

    class bNodeLink {
        +bNode* fromnode
        +bNodeSocket* fromsock
        +bNode* tonode
        +bNodeSocket* tosock
        +int flag
        +int multi_input_sort_id
    }

    TreeDrawContext --> bNode : 管理
    TreeDrawContext --> bNodeSocket : 管理
    bNode --> bNodeSocket : 包含
    bNodeLink --> bNode : 连接
    bNodeLink --> bNodeSocket : 连接
```

## 4. 绘制流程概览

节点绘制的完整流程遵循严格的顺序，确保视觉层次正确：

```mermaid
sequenceDiagram
    participant User as 用户
    participant WM as 窗口管理器
    participant SN as 节点空间
    participant ND as 节点绘制
    participant GPU as GPU

    User->>WM: 触发重绘
    WM->>SN: draw_nodespace_back_pix()
    
    rect rgb(225, 245, 254)
        Note over SN: 背景层绘制
        SN->>SN: 绘制网格背景
        SN->>SN: 绘制合成器背景
    end

    rect rgb(255, 243, 224)
        Note over ND: 节点层绘制
        SN->>ND: node_draw_space()
        ND->>ND: tree_draw_order_update()
        ND->>ND: 计算绘制顺序
    end

    rect rgb(243, 229, 245)
        Note over ND: 单个节点绘制
        ND->>ND: node_update_basis()
        ND->>ND: 布局计算
        ND->>ND: 绘制节点背景
        ND->>ND: 绘制标题栏
        ND->>ND: 绘制Socket
    end

    rect rgb(232, 245, 233)
        Note over ND: 链接层绘制
        ND->>ND: nodelink_batch_start()
        ND->>ND: 批次收集链接
        ND->>GPU: nodelink_batch_draw()
    end

    ND->>GPU: GPU_batch_draw()
    GPU->>User: 显示结果
```

## 5. 核心绘制函数

### 5.1 主绘制入口

```cpp
// node_draw.cc
void node_draw_space(const bContext &C, ARegion &region)
{
    // 1. 初始化绘制上下文
    TreeDrawContext tree_draw_ctx{...};
    
    // 2. 更新节点绘制顺序
    tree_draw_order_update(ntree);
    
    // 3. 计算节点布局
    for (bNode *node : nodes) {
        node_update_basis(C, tree_draw_ctx, ntree, *node, *block);
    }
    
    // 4. 绘制链接
    nodelink_batch_start(snode);
    for (bNodeLink *link : links) {
        node_draw_link(C, v2d, snode, *link, selected);
    }
    nodelink_batch_end(snode);
    
    // 5. 绘制节点
    for (bNode *node : nodes) {
        node_draw_basis(C, tree_draw_ctx, ntree, *node, *block);
    }
}
```

### 5.2 节点排序算法

节点排序确保正确的视觉层次 - 选中的节点显示在未选中节点之上：

```mermaid
flowchart TD
    A[开始排序] --> B{检查祖先关系}
    B -->|A是B的祖先| C[A在B后面]
    B -->|B是A的祖先| D[B在A后面]
    B -->|无祖先关系| E{检查背景标志}
    
    E -->|A是背景| F[A在B后面]
    E -->|B是背景| G[B在A后面]
    E -->|都不是| H{检查激活状态}
    
    H -->|A激活| I[A在前面]
    H -->|B激活| J[B在前面]
    H -->|都未激活| K{检查选中状态}
    
    K -->|A选中| L[A在前面]
    K -->|B选中| M[B在前面]
    K -->|都未选中| N[保持原顺序]

    style A fill:#e3f2fd,stroke:#1565c0
    style B fill:#fff8e1,stroke:#f57f17
    style H fill:#fce4ec,stroke:#c2185b
```

## 6. 坐标系统

节点编辑器使用多重坐标系统，需要精确的转换：

```mermaid
flowchart LR
    subgraph 世界坐标["🌍 世界坐标系"]
        A[node.location<br/>Blender单位]
    end

    subgraph 视图坐标["👁️ 视图坐标系"]
        B[View2D坐标<br/>缩放+偏移]
    end

    subgraph 像素坐标["📱 像素坐标系"]
        C[屏幕像素<br/>整数坐标]
    end

    A -->|node_to_view| B
    B -->|UI_SCALE_FAC| C
    C -->|node_from_view| A

    style A fill:#e8f5e9,stroke:#2e7d32
    style B fill:#e3f2fd,stroke:#1565c0
    style C fill:#fff3e0,stroke:#e65100
```

### 坐标转换函数

```cpp
// DPI 缩放的坐标转换
float2 node_to_view(const float2 &co) {
    return co * UI_SCALE_FAC;
}

float2 node_from_view(const float2 &co) {
    return co / UI_SCALE_FAC;
}

// 节点矩形计算
static rctf node_to_rect(const bNode &node) {
    rctf rect{};
    rect.xmin = node.location[0];
    rect.ymin = node.location[1] - node.height;
    rect.xmax = node.location[0] + node.width;
    rect.ymax = node.location[1];
    return rect;
}
```

## 7. 渲染优化策略

### 7.1 批次渲染

为了减少 GPU 调用次数，系统使用批次渲染：

```mermaid
flowchart TB
    subgraph 传统渲染["❌ 传统渲染"]
        A[绘制链接1] --> B[GPU调用]
        C[绘制链接2] --> D[GPU调用]
        E[绘制链接3] --> F[GPU调用]
    end

    subgraph 批次渲染["✅ 批次渲染"]
        G[收集链接1] --> J[合并数据]
        H[收集链接2] --> J
        I[收集链接3] --> J
        J --> K[单次GPU调用]
    end

    style 传统渲染 fill:#ffebee,stroke:#c62828
    style 批次渲染 fill:#e8f5e9,stroke:#2e7d32
```

### 7.2 视口裁剪

```cpp
// 链接可见性检测
static bool node_link_draw_is_visible(const View2D &v2d, 
                                       const std::array<float2, 4> &points) {
    if (min_ffff(points[0].x, points[1].x, points[2].x, points[3].x) > v2d.cur.xmax) {
        return false;
    }
    if (max_ffff(points[0].x, points[1].x, points[2].x, points[3].x) < v2d.cur.xmin) {
        return false;
    }
    return true;
}
```

## 8. 主题与样式

节点编辑器支持完整的主题定制：

```mermaid
flowchart TD
    A[主题系统] --> B[节点颜色]
    A --> C[Socket颜色]
    A --> D[链接颜色]
    A --> E[背景样式]

    B --> B1[输入节点]
    B --> B2[输出节点]
    B --> B3[转换节点]
    B --> B4[着色器节点]
    B --> B5[几何节点]

    C --> C1[浮点型]
    C --> C2[向量型]
    C --> C3[颜色型]
    C --> C4[几何型]
    C --> C5[布尔型]

    style A fill:#e1f5fe,stroke:#01579b
    style B fill:#f3e5f5,stroke:#6a1b9a
    style C fill:#e8f5e9,stroke:#2e7d32
```

### Socket 颜色定义

```cpp
// drawnode.cc - 标准Socket颜色映射
static const float std_node_socket_colors[][4] = {
    {0.63, 0.63, 0.63, 1.0}, // SOCK_FLOAT - 灰色
    {0.39, 0.39, 0.78, 1.0}, // SOCK_VECTOR - 蓝色
    {0.78, 0.78, 0.16, 1.0}, // SOCK_RGBA - 黄色
    {0.39, 0.78, 0.39, 1.0}, // SOCK_SHADER - 绿色
    {0.80, 0.65, 0.84, 1.0}, // SOCK_BOOLEAN - 紫色
    {0.35, 0.55, 0.36, 1.0}, // SOCK_INT - 深绿
    {0.44, 0.70, 1.00, 1.0}, // SOCK_STRING - 浅蓝
    {0.00, 0.84, 0.64, 1.0}, // SOCK_GEOMETRY - 青色
    // ... 更多类型
};
```

## 9. 与Blender其他系统的关系

```mermaid
flowchart TB
    subgraph 节点绘制系统["🎯 节点绘制系统"]
        ND[node_draw.cc]
        DN[drawnode.cc]
    end

    subgraph 核心系统["🔧 Blender核心"]
        BKE[BKE_node - 节点核心]
        GPU[GPU模块]
        UI[UI_interface - UI系统]
    end

    subgraph 渲染引擎["🎬 渲染引擎"]
        DRW[DRW_engine]
        SHD[着色器系统]
    end

    ND --> BKE
    ND --> GPU
    ND --> UI
    DN --> GPU
    DN --> SHD

    style 节点绘制系统 fill:#e1f5fe,stroke:#01579b,stroke-width:3px
    style 核心系统 fill:#fff3e0,stroke:#e65100,stroke-width:2px
    style 渲染引擎 fill:#f3e5f5,stroke:#6a1b9a,stroke-width:2px
```

## 10. 总结

Blender 的节点绘制系统展现了优秀的软件架构设计：

1. **分层架构**: 清晰的层次划分，职责明确
2. **性能优化**: 批次渲染、视口裁剪等优化手段
3. **可扩展性**: 支持自定义节点类型和绘制方式
4. **主题支持**: 完整的视觉定制能力
5. **多类型支持**: 统一架构支持着色器、合成器、几何节点等多种类型

这个系统不仅是 Blender 的核心组件，也为其他图形编辑器的设计提供了优秀的参考范例。
