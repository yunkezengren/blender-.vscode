# GIZMO_GT_cage2d 详细分析

## 目录
- [1. 概述](#1-概述)
- [2. 核心数据结构](#2-核心数据结构)
- [3. 部件定义 (Parts)](#3-部件定义-parts)
- [4. 样式 (Draw Styles)](#4-样式-draw-styles)
- [5. 绘制实现](#5-绘制实现)
  - [5.1. 盒形样式绘制](#51-盒形样式绘制)
  - [5.2. 圆形样式绘制](#52-圆形样式绘制)
  - [5.3. 边缘和角手柄](#53-边缘和角手柄)
- [6. 选择与测试](#6-选择与测试)
- [7. 交互逻辑](#7-交互逻辑)
  - [7.1. 平移](#71-平移)
  - [7.2. 旋转](#72-旋转)
  - [7.3. 缩放](#73-缩放)
  - [7.4. 修饰键处理](#74-修饰键处理)
- [8. 属性绑定](#8-属性绑定)
- [9. 完整 API 定义](#9-完整-api-定义)
- [10. 总结](#10-总结)

---

## 1. 概述

`GIZMO_GT_cage_2d` 是 Blender 中最核心的 2D 边界框 gizmo，用于在 2D 空间中进行平移、旋转和缩放操作。它被广泛应用于：

- **节点编辑器**：裁剪、遮罩、分割等
- **图像编辑器**：图像变换
- **3D 视图**：2D 辅助工具

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc`

**定义位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:1412-1472`

```cpp
void ED_gizmotypes_cage_2d()
{
  WM_gizmotype_append(GIZMO_GT_cage_2d);
}
```

<mark style="background-color: #2E7D32; color: white; padding: 2px 6px; border-radius: 3px;">★ Insight</mark>
Cage2d gizmo 是一个多功能工具，通过不同的标志组合可以实现从简单的平移到复杂的全功能变换。

---

## 2. 核心数据结构

### 2.1. 交互数据结构

```cpp
namespace {

struct RectTransformInteraction {
  float orig_mouse[2];                    // 原始鼠标位置
  float orig_matrix_offset[4][4];         // 原始偏移矩阵
  float orig_matrix_final_no_offset[4][4]; // 原始最终矩阵（无偏移）
  Dial *dial;                             // 旋转拨盘（用于旋转计算）
  bool use_temp_uniform;                  // 临时等比缩放标志
};

}  // namespace
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:1054-1064`

这个结构在交互过程中存储：
- **原始状态**：用于撤销和计算增量
- **鼠标位置**：用于计算相对移动
- **旋转拨盘**：用于精确的角度计算
- **临时标志**：处理 Shift 等修饰键

---

## 3. 部件定义 (Parts)

Cage2d gizmo 包含多个可交互部件，每个部件都有唯一的 ID：

```cpp
enum {
  ED_GIZMO_CAGE2D_PART_TRANSLATE = 0,          // 平移（中心）
  ED_GIZMO_CAGE2D_PART_SCALE,                  // 整体缩放
  ED_GIZMO_CAGE2D_PART_SCALE_MIN_X,            // 左边缘
  ED_GIZMO_CAGE2D_PART_SCALE_MAX_X,            // 右边缘
  ED_GIZMO_CAGE2D_PART_SCALE_MIN_Y,            // 下边缘
  ED_GIZMO_CAGE2D_PART_SCALE_MAX_Y,            // 上边缘
  ED_GIZMO_CAGE2D_PART_SCALE_MIN_X_MIN_Y,      // 左下角
  ED_GIZMO_CAGE2D_PART_SCALE_MIN_X_MAX_Y,      // 左上角
  ED_GIZMO_CAGE2D_PART_SCALE_MAX_X_MIN_Y,      // 右下角
  ED_GIZMO_CAGE2D_PART_SCALE_MAX_X_MAX_Y,      // 右上角
  ED_GIZMO_CAGE2D_PART_ROTATE,                 // 旋转
};
```

**源码位置**: `source/blender/editors/include/ED_gizmo_library.hh:152-163`

### 部件交互矩阵

| 部件 ID | 功能 | 适用变换标志 | 位置 |
|---------|------|-------------|------|
| `TRANSLATE` | 平移 | `TRANSLATE` | 中心 |
| `SCALE` | 整体缩放 | `SCALE` | 整个边界框 |
| `SCALE_MIN_X` | 左边缘缩放 | `SCALE` | 左边缘 |
| `SCALE_MAX_X` | 右边缘缩放 | `SCALE` | 右边缘 |
| `SCALE_MIN_Y` | 下边缘缩放 | `SCALE` | 下边缘 |
| `SCALE_MAX_Y` | 上边缘缩放 | `SCALE` | 上边缘 |
| `SCALE_MIN_X_MIN_Y` | 左下角缩放 | `SCALE` | 左下角 |
| `SCALE_MIN_X_MAX_Y` | 左上角缩放 | `SCALE` | 左上角 |
| `SCALE_MAX_X_MIN_Y` | 右下角缩放 | `SCALE` | 右下角 |
| `SCALE_MAX_X_MAX_Y` | 右上角缩放 | `SCALE` | 右上角 |
| `ROTATE` | 旋转 | `ROTATE` | 上方中心 |

---

## 4. 样式 (Draw Styles)

Cage2d gizmo 支持三种绘制样式：

```cpp
enum {
  ED_GIZMO_CAGE2D_STYLE_BOX = 0,              // 基础盒形（角落）
  ED_GIZMO_CAGE2D_STYLE_BOX_TRANSFORM,        // 变换盒形（带手柄）
  ED_GIZMO_CAGE2D_STYLE_CIRCLE,               // 圆形
};
```

**源码位置**: `source/blender/editors/include/ED_gizmo_library.hh:147-151`

### 样式对比

| 样式 | 视觉表现 | 使用场景 | 手柄支持 |
|------|----------|----------|----------|
| **BOX** | 仅角落线条 | 3D 视图辅助 | 无 |
| **BOX_TRANSFORM** | 完整边框 + 手柄 | 节点编辑器裁剪 | 边缘 + 角 |
| **CIRCLE** | 圆形边框 | 椭圆遮罩 | 边缘 + 角（可选）|

---

## 5. 绘制实现

### 5.1. 盒形样式绘制

#### 5.1.1. 角落绘制 (`cage2d_draw_box_corners`)

```cpp
static void cage2d_draw_box_corners(const rctf *r,
                                    const float margin[2],
                                    const float color[3],
                                    const float line_width)
{
  /* 绘制 4 个角落的短线段 */
  immBegin(GPU_PRIM_LINES, 16);

  // 左下角
  immVertex3f(pos, r->xmin, r->ymin + margin[1], 0.0f);
  immVertex3f(pos, r->xmin, r->ymin, 0.0f);
  immVertex3f(pos, r->xmin, r->ymin, 0.0f);
  immVertex3f(pos, r->xmin + margin[0], r->ymin, 0.0f);

  // ... 其他三个角落类似
  immEnd();
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:86-129`

**特点**：
- 使用 `GPU_SHADER_3D_POLYLINE_UNIFORM_COLOR` 着色器
- 绘制 16 条线段（每个角落 4 条）
- 支持轮廓线宽（outline_line_width = line_width + 3.0f）

#### 5.1.2. 交互区域绘制 (`cage2d_draw_box_interaction`)

```cpp
static void cage2d_draw_box_interaction(const float color[4],
                                        const int highlighted,
                                        const float size[2],
                                        const float margin[2],
                                        const float line_width,
                                        const bool is_solid,
                                        const int draw_options)
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:131-430`

这个函数根据 `highlighted` 参数绘制不同的交互区域：

```cpp
switch (highlighted) {
  case ED_GIZMO_CAGE2D_PART_SCALE_MIN_X:
    // 绘制左边缘区域
    rctf r;
    r.xmin = -size[0];
    r.xmax = -size[0] + margin[0];
    r.ymin = -size[1] + margin[1];
    r.ymax = size[1] - margin[1];
    // ... 绘制矩形
    break;

  case ED_GIZMO_CAGE2D_PART_SCALE_MIN_X_MIN_Y:
    // 绘制左下角区域
    rctf r;
    r.xmin = -size[0];
    r.xmax = -size[0] + margin[0];
    r.ymin = -size[1];
    r.ymax = -size[1] + margin[1];
    // ... 绘制三角形或矩形
    break;

  case ED_GIZMO_CAGE2D_PART_TRANSLATE:
    if (draw_options & ED_GIZMO_CAGE_DRAW_FLAG_XFORM_CENTER_HANDLE) {
      // 绘制中心手柄（小 X 形）
      ARRAY_SET_ITEMS(verts[0], -margin[0] / 2, -margin[1] / 2);
      ARRAY_SET_ITEMS(verts[1], margin[0] / 2, margin[1] / 2);
      // ...
    }
    else {
      // 整个内部区域用于选择
      ARRAY_SET_ITEMS(verts[0], -size[0], -size[1]);
      // ...
    }
    break;
}
```

**绘制模式**：
- **实心** (`is_solid=true`)：使用 `GPU_PRIM_TRI_FAN` 绘制填充区域
- **线框** (`is_solid=false`)：使用 `GPU_PRIM_LINE_STRIP` 绘制轮廓

### 5.2. 圆形样式绘制

#### 5.2.1. 圆形线框 (`cage2d_draw_circle_wire`)

```cpp
static void cage2d_draw_circle_wire(const float color[3],
                                    const float size[2],
                                    const float margin[2],
                                    const int transform_flag,
                                    const int draw_options,
                                    const float line_width)
{
  // 绘制主圆
  imm_draw_circle_wire_aspect_3d(pos, 0.0f, 0.0f, size[0], size[1], CIRCLE_RESOL);

  // 绘制旋转手柄线
  if (transform_flag & ED_GIZMO_CAGE_XFORM_FLAG_ROTATE) {
    immBegin(GPU_PRIM_LINES, 4);
    immVertex3f(pos, 0.0f, size[1], 0.0f);
    immVertex3f(pos, 0.0f, size[1] + margin[1], 0.0f);
    // ...
    immEnd();
  }

  // 绘制中心手柄
  if (transform_flag & ED_GIZMO_CAGE_XFORM_FLAG_TRANSLATE) {
    if (draw_options & ED_GIZMO_CAGE_DRAW_FLAG_XFORM_CENTER_HANDLE) {
      const float rad[2] = {margin[0] / 2, margin[1] / 2};
      // 绘制中心 X 形
    }
  }
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:548-603`

**特点**：
- 使用 `imm_draw_circle_wire_aspect_3d` 绘制椭圆（支持宽高比）
- 分辨率：`CIRCLE_RESOL = 32`
- 旋转手柄位于圆的顶部
- 中心手柄可选

### 5.3. 边缘和角手柄

#### 5.3.1. 边缘手柄 (`cage2d_draw_rect_edge_handles`)

```cpp
static void cage2d_draw_rect_edge_handles(const rctf *r,
                                          const int highlighted,
                                          const float size[2],
                                          const float margin[2],
                                          const float color[3],
                                          bool solid)
{
  switch (highlighted) {
    case ED_GIZMO_CAGE2D_PART_SCALE_MIN_X:
    case ED_GIZMO_CAGE2D_PART_SCALE_MAX_X:
      // 水平边缘：绘制垂直椭圆
      const float rad[2] = {0.2f * margin[0], 0.4f * size[1]};
      imm_draw_point_aspect_2d(pos, r->xmin, 0.0f, rad[0], rad[1], solid);
      imm_draw_point_aspect_2d(pos, r->xmax, 0.0f, rad[0], rad[1], solid);
      break;

    case ED_GIZMO_CAGE2D_PART_SCALE_MIN_Y:
    case ED_GIZMO_CAGE2D_PART_SCALE_MAX_Y:
      // 垂直边缘：绘制水平椭圆
      const float rad[2] = {0.4f * size[0], 0.2f * margin[1]};
      imm_draw_point_aspect_2d(pos, 0.0f, r->ymin, rad[0], rad[1], solid);
      imm_draw_point_aspect_2d(pos, 0.0f, r->ymax, rad[0], rad[1], solid);
      break;
  }
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:662-693`

**特点**：
- 仅在高亮时显示
- 形状根据边缘方向调整
- 使用 `imm_draw_point_aspect_2d` 绘制椭圆

#### 5.3.2. 角手柄 (`cage2d_draw_rect_corner_handles`)

```cpp
static void cage2d_draw_rect_corner_handles(const rctf *r,
                                            const float margin[2],
                                            const float color[3],
                                            bool solid)
{
  const float rad[2] = {margin[0] / 3, margin[1] / 3};

  // 绘制 4 个角的小圆点
  imm_draw_point_aspect_2d(pos, r->xmin, r->ymin, rad[0], rad[1], solid);
  imm_draw_point_aspect_2d(pos, r->xmax, r->ymin, rad[0], rad[1], solid);
  imm_draw_point_aspect_2d(pos, r->xmax, r->ymax, rad[0], rad[1], solid);
  imm_draw_point_aspect_2d(pos, r->xmin, r->ymax, rad[0], rad[1], solid);
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:639-660`

#### 5.3.3. 旋转手柄 (`cage2d_draw_rect_rotate_handle`)

```cpp
static void cage2d_draw_rect_rotate_handle(const rctf *r,
                                           const float margin[2],
                                           const float color[3],
                                           bool solid)
{
  const float rad[2] = {margin[0] / 3, margin[1] / 3};
  const float handle[2] = {
      BLI_rctf_cent_x(r),
      r->ymax + (margin[1] * GIZMO_MARGIN_OFFSET_SCALE),  // 1.5 倍边距
  };

  // 绘制小圆点
  circle_fn(pos, handle[0], handle[1], rad[0], rad[1], resolu);
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:614-637`

**特点**：
- 位于边界框上方 1.5 倍边距处
- 使用 `GIZMO_MARGIN_OFFSET_SCALE = 1.5f`

---

## 6. 选择与测试

### 6.1. 绘制选择缓冲区 (`gizmo_cage2d_draw_select`)

```cpp
static void gizmo_cage2d_draw_select(const bContext * /*C*/, wmGizmo *gz, int select_id)
{
  gizmo_cage2d_draw_intern(gz, true, false, select_id);
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:886-889`

**选择模式特点**：
- `select=true`：绘制选择区域（扩大热区）
- 使用 `GPU_select_load_id` 加载部件 ID
- 实心绘制用于点击检测

### 6.2. 测试选择 (`gizmo_cage2d_test_select`)

```cpp
static int gizmo_cage2d_test_select(bContext *C, wmGizmo *gz, const int mval[2])
{
  // 1. 投影鼠标到局部空间
  float point_local[2];
  gizmo_window_project_2d(C, gz, float2(int2(mval)), 2, true, point_local);

  // 2. 获取尺寸和边距
  float dims[2];
  RNA_float_get_array(gz->ptr, \"dimensions\", dims);
  const float size_real[2] = {dims[0] / 2.0f, dims[1] / 2.0f};
  float margin[2];
  gizmo_calc_rect_view_margin(gz, margin);

  // 3. 检查平移区域
  if (transform_flag & ED_GIZMO_CAGE_XFORM_FLAG_TRANSLATE) {
    rctf r;
    if (draw_options & ED_GIZMO_CAGE_DRAW_FLAG_XFORM_CENTER_HANDLE) {
      // 中心手柄
      r.xmin = -margin[0] / 2;
      r.ymin = -margin[1] / 2;
      r.xmax = margin[0] / 2;
      r.ymax = margin[1] / 2;
    }
    else {
      // 整个内部
      r.xmin = -size[0] + margin[0];
      r.ymin = -size[1] + margin[1];
      r.xmax = size[0] - margin[0];
      r.ymax = size[1] - margin[1];
    }
    if (BLI_rctf_isect_pt_v(&r, point_local)) {
      return ED_GIZMO_CAGE2D_PART_TRANSLATE;
    }
  }

  // 4. 检查缩放区域（边缘和角）
  if (transform_flag & (ED_GIZMO_CAGE_XFORM_FLAG_SCALE |
                        ED_GIZMO_CAGE_XFORM_FLAG_SCALE_UNIFORM)) {
    // 检查左边缘
    rctf r_xmin;
    r_xmin.xmin = -size[0];
    r_xmin.ymin = -size[1];
    r_xmin.xmax = -size[0] + margin[0];
    r_xmin.ymax = size[1];

    if (BLI_rctf_isect_pt_v(&r_xmin, point_local)) {
      // 检查是否在角区域
      if (draw_options & ED_GIZMO_CAGE_DRAW_FLAG_CORNER_HANDLES) {
        if (BLI_rctf_isect_pt_v(&r_ymin, point_local)) {
          return ED_GIZMO_CAGE2D_PART_SCALE_MIN_X_MIN_Y;
        }
        if (BLI_rctf_isect_pt_v(&r_ymax, point_local)) {
          return ED_GIZMO_CAGE2D_PART_SCALE_MIN_X_MAX_Y;
        }
      }
      return ED_GIZMO_CAGE2D_PART_SCALE_MIN_X;
    }
    // ... 其他边缘和角检查
  }

  // 5. 检查旋转区域
  if (transform_flag & ED_GIZMO_CAGE_XFORM_FLAG_ROTATE) {
    const float r_rotate_pt[2] = {0.0f, size_real[1] + (margin[1] * 1.5f)};
    rctf r_rotate;
    r_rotate.xmin = r_rotate_pt[0] - margin[0] / 2.0f;
    r_rotate.xmax = r_rotate_pt[0] + margin[0] / 2.0f;
    r_rotate.ymin = r_rotate_pt[1] - margin[1] / 2.0f;
    r_rotate.ymax = r_rotate_pt[1] + margin[1] / 2.0f;

    if (BLI_rctf_isect_pt_v(&r_rotate, point_local)) {
      return ED_GIZMO_CAGE2D_PART_ROTATE;
    }
  }

  return -1;  // 未选择
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:929-1052`

**选择逻辑**：
1. **投影**：将屏幕坐标转换为 gizmo 局部坐标
2. **扩大热区**：添加 `margin/2` 使点击更容易
3. **层级检查**：先检查角 → 边缘 → 内部
4. **返回部件 ID**：用于后续交互处理

---

## 7. 交互逻辑

### 7.1. 调用与初始化 (`gizmo_cage2d_invoke`)

```cpp
static wmOperatorStatus gizmo_cage2d_invoke(bContext *C, wmGizmo *gz, const wmEvent *event)
{
  // 分配交互数据
  RectTransformInteraction *data = MEM_callocN<RectTransformInteraction>(\"cage_interaction\");

  // 保存原始状态
  copy_m4_m4(data->orig_matrix_offset, gz->matrix_offset);
  WM_gizmo_calc_matrix_final_no_offset(gz, data->orig_matrix_final_no_offset);

  // 记录鼠标起始位置
  if (gizmo_window_project_2d(C, gz, float2(int2(event->mval)), 2, false, data->orig_mouse) == 0) {
    zero_v2(data->orig_mouse);
  }

  gz->interaction_data = data;

  return OPERATOR_RUNNING_MODAL;
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:1083-1099`

**初始化步骤**：
1. 分配 `RectTransformInteraction` 结构
2. 保存当前矩阵状态（用于撤销）
3. 计算无偏移的最终矩阵（用于旋转计算）
4. 投影鼠标位置到局部空间
5. 存储到 `gz->interaction_data`

### 7.2. 模态处理 (`gizmo_cage2d_modal`)

```cpp
static wmOperatorStatus gizmo_cage2d_modal(bContext *C,
                                           wmGizmo *gz,
                                           const wmEvent *event,
                                           eWM_GizmoFlagTweak /*tweak_flag*/)
{
  RectTransformInteraction *data = static_cast<RectTransformInteraction *>(gz->interaction_data);

  // 1. 处理 Shift 修饰键（临时等比缩放）
  int transform_flag = RNA_enum_get(gz->ptr, \"transform\");
  if ((transform_flag & ED_GIZMO_CAGE_XFORM_FLAG_SCALE_UNIFORM) == 0) {
    const bool use_temp_uniform = (event->modifier & KM_SHIFT) != 0;
    data->use_temp_uniform = use_temp_uniform;
    if (use_temp_uniform) {
      transform_flag |= ED_GIZMO_CAGE_XFORM_FLAG_SCALE_UNIFORM;
    }
  }

  // 2. 投影当前鼠标位置
  float point_local[2];
  // ... 投影代码

  // 3. 获取当前属性值
  wmGizmoProperty *gz_prop = WM_gizmo_target_property_find(gz, \"matrix\");
  if (gz_prop->type != nullptr) {
    WM_gizmo_target_property_float_get_array(gz, gz_prop, &gz->matrix_offset[0][0]);
  }

  // 4. 根据部件类型处理
  if (gz->highlight_part == ED_GIZMO_CAGE2D_PART_TRANSLATE) {
    // 7.2.1. 平移处理
    copy_m4_m4(gz->matrix_offset, data->orig_matrix_offset);
    gz->matrix_offset[3][0] = data->orig_matrix_offset[3][0] +
                              (point_local[0] - data->orig_mouse[0]);
    gz->matrix_offset[3][1] = data->orig_matrix_offset[3][1] +
                              (point_local[1] - data->orig_mouse[1]);
  }
  else if (gz->highlight_part == ED_GIZMO_CAGE2D_PART_ROTATE) {
    // 7.2.2. 旋转处理
    // ... 旋转计算
  }
  else {
    // 7.2.3. 缩放处理
    // ... 缩放计算
  }

  // 5. 更新属性
  if (gz_prop->type != nullptr) {
    WM_gizmo_target_property_float_set_array(C, gz, gz_prop, &gz->matrix_offset[0][0]);
  }

  // 6. 标记重绘
  ED_region_tag_redraw_editor_overlays(CTX_wm_region(C));

  return OPERATOR_RUNNING_MODAL;
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:1157-1365`

### 7.2.1. 平移处理

```cpp
if (gz->highlight_part == ED_GIZMO_CAGE2D_PART_TRANSLATE) {
  // 防止大小被钳制
  copy_m4_m4(gz->matrix_offset, data->orig_matrix_offset);

  // 计算增量：当前鼠标 - 原始鼠标
  gz->matrix_offset[3][0] = data->orig_matrix_offset[3][0] +
                            (point_local[0] - data->orig_mouse[0]);
  gz->matrix_offset[3][1] = data->orig_matrix_offset[3][1] +
                            (point_local[1] - data->orig_mouse[1]);
}
```

**逻辑**：
- 使用原始矩阵作为基础（避免累积误差）
- 计算鼠标位移增量
- 应用到位置分量（`matrix[3][0]` 和 `matrix[3][1]`）

### 7.2.2. 旋转处理

```cpp
else if (gz->highlight_part == ED_GIZMO_CAGE2D_PART_ROTATE) {
  // 初始化拨盘（仅第一次）
  if (data->dial == nullptr) {
    float test_co[3];
    // 计算原始位置在屏幕空间的坐标
    mul_v3_m4v3(test_co, data->orig_matrix_final_no_offset,
                float3{UNPACK2(data->orig_matrix_offset[3]), 0.0});
    data->dial = BLI_dial_init(test_co, FLT_EPSILON);

    // 计算鼠标起始位置
    mul_v3_m4v3(test_co, data->orig_matrix_final_no_offset,
                float3{UNPACK2(data->orig_mouse), 0.0});
    BLI_dial_angle(data->dial, test_co);
  }

  // 计算当前角度
  float test_co[3];
  mul_v3_m4v3(test_co, data->orig_matrix_final_no_offset,
              float3{UNPACK2(point_local), 0.0});
  const float angle = BLI_dial_angle(data->dial, test_co);

  // 构建旋转矩阵
  float matrix_space_inv[4][4];
  float matrix_rotate[4][4];
  float pivot[3];

  copy_v3_v3(pivot, data->orig_matrix_offset[3]);  // 旋转中心

  invert_m4_m4(matrix_space_inv, gz->matrix_space);

  unit_m4(matrix_rotate);
  mul_m4_m4m4(matrix_rotate, matrix_rotate, matrix_space_inv);
  rotate_m4(matrix_rotate, 'Z', -angle);  // Z 轴旋转
  mul_m4_m4m4(matrix_rotate, matrix_rotate, gz->matrix_space);

  zero_v3(matrix_rotate[3]);
  transform_pivot_set_m4(matrix_rotate, pivot);

  // 应用旋转
  mul_m4_m4m4(gz->matrix_offset, matrix_rotate, data->orig_matrix_offset);
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:1217-1256`

**旋转算法**：
1. **拨盘初始化**：`BLI_dial_init` 创建 2D 拨盘计算器
2. **角度计算**：`BLI_dial_angle` 计算相对于起始点的角度增量
3. **空间转换**：在 gizmo 的局部空间中旋转
4. **枢轴保持**：围绕原始位置旋转（保持中心不变）

### 7.2.3. 缩放处理

```cpp
else {
  /* scale */
  copy_m4_m4(gz->matrix_offset, data->orig_matrix_offset);

  // 1. 确定缩放枢轴
  float pivot[2];
  if (transform_flag & ED_GIZMO_CAGE_XFORM_FLAG_TRANSLATE) {
    gizmo_pivot_from_scale_part(gz->highlight_part, pivot);
    mul_v2_v2(pivot, dims);  // 转换为实际坐标
  }
  else {
    zero_v2(pivot);  // 无平移时以中心为枢轴
  }

  // 2. 旋转鼠标坐标（如果 gizmo 被旋转）
  float curr_mouse[2];
  copy_v2_v2(curr_mouse, data->orig_mouse);

  if (transform_flag & ED_GIZMO_CAGE_XFORM_FLAG_ROTATE) {
    float rot[3][3], loc[3], size[3];
    mat4_to_loc_rot_size(loc, rot, size, gz->matrix_offset);
    invert_m3(rot);

    sub_v2_v2(point_local, loc);
    mul_m3_v2(rot, point_local);
    add_v2_v2(point_local, loc);

    sub_v2_v2(curr_mouse, loc);
    mul_m3_v2(rot, curr_mouse);
    add_v2_v2(curr_mouse, loc);
  }

  // 3. 确定约束轴
  bool constrain_axis[2] = {false};
  gizmo_constrain_from_scale_part(gz->highlight_part, constrain_axis);

  // 4. 计算新尺寸
  float size_new[2], size_orig[2];
  for (int i = 0; i < 2; i++) {
    size_orig[i] = len_v3(data->orig_matrix_offset[i]);
    size_new[i] = size_orig[i];

    if (constrain_axis[i] == false) {
      const float delta_orig = curr_mouse[i] - data->orig_matrix_offset[3][i] -
                               pivot[i] * size_orig[i];
      const float delta_curr = point_local[i] - data->orig_matrix_offset[3][i] -
                               pivot[i] * size_orig[i];

      // 防止反向缩放（除非启用 signed）
      if ((transform_flag & ED_GIZMO_CAGE_XFORM_FLAG_SCALE_SIGNED) == 0) {
        if (signum_i(delta_orig) != signum_i(delta_curr)) {
          size_new[i] = 0.0f;
          continue;
        }
      }

      size_new[i] = delta_curr / (signf(delta_orig) * 0.5f * dims[i] - pivot[i]);
    }
  }

  // 5. 计算缩放比例
  float scale[2] = {1.0f, 1.0f};
  for (int i = 0; i < 2; i++) {
    if (size_orig[i] == 0) {
      size_orig[i] = 1.0f;
      gz->matrix_offset[i][i] = 1.0f;
    }
    scale[i] = size_new[i] / size_orig[i];
  }

  // 6. 处理等比缩放
  if (transform_flag & ED_GIZMO_CAGE_XFORM_FLAG_SCALE_UNIFORM) {
    if (constrain_axis[0] == false && constrain_axis[1] == false) {
      if (draw_style == ED_GIZMO_CAGE2D_STYLE_CIRCLE) {
        scale[1] = scale[0] = len_v2(scale);  // 圆形：保持半径
      }
      else {
        scale[1] = scale[0] = (scale[1] + scale[0]) / 2.0f;  // 平均值
      }
    }
    else if (constrain_axis[0] == false) {
      scale[1] = scale[0];  // X 轴缩放影响 Y
    }
    else if (constrain_axis[1] == false) {
      scale[0] = scale[1];  // Y 轴缩放影响 X
    }
  }

  // 7. 应用缩放（围绕枢轴）
  float matrix_scale[4][4];
  unit_m4(matrix_scale);
  mul_v3_fl(matrix_scale[0], scale[0]);
  mul_v3_fl(matrix_scale[1], scale[1]);

  transform_pivot_set_m4(matrix_scale, float3(UNPACK2(pivot), 0.0f));
  mul_m4_m4_post(gz->matrix_offset, matrix_scale);
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:1257-1355`

**缩放算法**：
1. **枢轴计算**：根据部件确定缩放中心
2. **坐标旋转**：如果 gizmo 被旋转，需要反向旋转鼠标坐标
3. **轴约束**：边缘缩放只影响一个轴，角缩放影响两个轴
4. **尺寸计算**：基于鼠标位移计算新尺寸
5. **比例计算**：新尺寸 / 原尺寸 = 缩放比例
6. **等比处理**：Shift 键启用时，两个轴使用相同比例
7. **矩阵应用**：围绕枢轴缩放

### 7.3. 修饰键处理

#### 7.3.1. Shift - 等比缩放

```cpp
// 在 modal 回调中
const bool use_temp_uniform = (event->modifier & KM_SHIFT) != 0;
data->use_temp_uniform = use_temp_uniform;
if (use_temp_uniform) {
  transform_flag |= ED_GIZMO_CAGE_XFORM_FLAG_SCALE_UNIFORM;
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:1167-1172`

**行为**：
- 按下 Shift 时临时启用 `SCALE_UNIFORM`
- 释放 Shift 时恢复原始状态
- 在 `gizmo_cage2d_transform_flag_get` 中动态应用

#### 7.3.2. 临时等比标志传递

```cpp
static int gizmo_cage2d_transform_flag_get(const wmGizmo *gz)
{
  RectTransformInteraction *data = static_cast<RectTransformInteraction *>(gz->interaction_data);
  int transform_flag = RNA_enum_get(gz->ptr, \"transform\");

  if (data) {
    if (data->use_temp_uniform) {
      transform_flag |= ED_GIZMO_CAGE_XFORM_FLAG_SCALE_UNIFORM;
    }
  }

  return transform_flag;
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:1066-1076`

**作用**：
- 在绘制和选择测试时动态获取正确的变换标志
- 确保 Shift 键效果实时生效

---

## 8. 属性绑定

### 8.1. 矩阵属性

Cage2d gizmo 使用 `matrix` 属性进行双向绑定：

```cpp
// 在 gizmo_cage2d_modal 中
wmGizmoProperty *gz_prop = WM_gizmo_target_property_find(gz, \"matrix\");

// 读取当前值
if (gz_prop->type != nullptr) {
  WM_gizmo_target_property_float_get_array(gz, gz_prop, &gz->matrix_offset[0][0]);
}

// 写入新值
if (gz_prop->type != nullptr) {
  WM_gizmo_target_property_float_set_array(C, gz, gz_prop, &gz->matrix_offset[0][0]);
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:1202-1207, 1357-1359`

### 8.2. 属性更新回调

```cpp
static void gizmo_cage2d_property_update(wmGizmo *gz, wmGizmoProperty *gz_prop)
{
  if (STREQ(gz_prop->type->idname, \"matrix\")) {
    if (WM_gizmo_target_property_array_length(gz, gz_prop) == 16) {
      WM_gizmo_target_property_float_get_array(gz, gz_prop, &gz->matrix_offset[0][0]);
    }
  }
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:1367-1380`

**作用**：当外部修改属性时，更新 gizmo 的矩阵显示。

### 8.3. 退出与撤销

```cpp
static void gizmo_cage2d_exit(bContext *C, wmGizmo *gz, const bool cancel)
{
  RectTransformInteraction *data = static_cast<RectTransformInteraction *>(gz->interaction_data);

  if (data->dial) {
    BLI_dial_free(data->dial);
  }

  wmGizmoProperty *gz_prop = WM_gizmo_target_property_find(gz, \"matrix\");

  if (!cancel) {
    // 确认：自动关键帧
    if (WM_gizmo_target_property_is_valid(gz_prop)) {
      WM_gizmo_target_property_anim_autokey(C, gz, gz_prop);
    }
    return;
  }

  // 取消：恢复原始值
  if (gz_prop->type != nullptr) {
    WM_gizmo_target_property_float_set_array(C, gz, gz_prop, &data->orig_matrix_offset[0][0]);
  }

  copy_m4_m4(gz->matrix_offset, data->orig_matrix_offset);
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:1382-1406`

**行为**：
- **确认**：保存修改，触发自动关键帧
- **取消**：恢复到交互前的状态

---

## 9. 完整 API 定义

```cpp
static void GIZMO_GT_cage_2d(wmGizmoType *gzt)
{
  /* identifiers */
  gzt->idname = \"GIZMO_GT_cage_2d\";

  /* API callbacks. */
  gzt->draw = gizmo_cage2d_draw;                      // 绘制
  gzt->draw_select = gizmo_cage2d_draw_select;        // 选择绘制
  gzt->test_select = gizmo_cage2d_test_select;        // 选择测试
  gzt->setup = gizmo_cage2d_setup;                    // 初始化
  gzt->invoke = gizmo_cage2d_invoke;                  // 调用
  gzt->property_update = gizmo_cage2d_property_update; // 属性更新
  gzt->modal = gizmo_cage2d_modal;                    // 模态处理
  gzt->exit = gizmo_cage2d_exit;                      // 退出
  gzt->cursor_get = gizmo_cage2d_get_cursor;          // 光标

  gzt->struct_size = sizeof(wmGizmo);

  /* rna */
  static const EnumPropertyItem rna_enum_draw_style[] = {
      {ED_GIZMO_CAGE2D_STYLE_BOX, \"BOX\", 0, \"Box\", \"\"},
      {ED_GIZMO_CAGE2D_STYLE_BOX_TRANSFORM, \"BOX_TRANSFORM\", 0, \"Box Transform\", \"\"},
      {ED_GIZMO_CAGE2D_STYLE_CIRCLE, \"CIRCLE\", 0, \"Circle\", \"\"},
      {0, nullptr, 0, nullptr, nullptr},
  };

  static const EnumPropertyItem rna_enum_transform[] = {
      {ED_GIZMO_CAGE_XFORM_FLAG_TRANSLATE, \"TRANSLATE\", 0, \"Move\", \"\"},
      {ED_GIZMO_CAGE_XFORM_FLAG_ROTATE, \"ROTATE\", 0, \"Rotate\", \"\"},
      {ED_GIZMO_CAGE_XFORM_FLAG_SCALE, \"SCALE\", 0, \"Scale\", \"\"},
      {ED_GIZMO_CAGE_XFORM_FLAG_SCALE_UNIFORM, \"SCALE_UNIFORM\", 0, \"Scale Uniform\", \"\"},
      {0, nullptr, 0, nullptr, nullptr},
  };

  static const EnumPropertyItem rna_enum_draw_options[] = {
      {ED_GIZMO_CAGE_DRAW_FLAG_XFORM_CENTER_HANDLE, \"XFORM_CENTER_HANDLE\", 0, \"Center Handle\", \"\"},
      {ED_GIZMO_CAGE_DRAW_FLAG_CORNER_HANDLES, \"CORNER_HANDLES\", 0, \"Corner Handles\", \"\"},
      {0, nullptr, 0, nullptr, nullptr},
  };

  static const float unit_v2[2] = {1.0f, 1.0f};

  // 定义 RNA 属性
  RNA_def_float_vector(gzt->srna, \"dimensions\", 2, unit_v2, 0, FLT_MAX, \"Dimensions\", \"\", 0.0f, FLT_MAX);
  RNA_def_enum_flag(gzt->srna, \"transform\", rna_enum_transform, 0, \"Transform Options\", \"\");
  RNA_def_enum(gzt->srna, \"draw_style\", rna_enum_draw_style, ED_GIZMO_CAGE2D_STYLE_BOX_TRANSFORM, \"Draw Style\", \"\");
  RNA_def_enum_flag(gzt->srna, \"draw_options\", rna_enum_draw_options, ED_GIZMO_CAGE_DRAW_FLAG_XFORM_CENTER_HANDLE, \"Draw Options\", \"\");

  // 定义目标属性
  WM_gizmotype_target_property_def(gzt, \"matrix\", PROP_FLOAT, 16);
}
```

**源码位置**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc:1412-1467`

### 9.1. RNA 属性说明

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `dimensions` | float[2] | `{1.0, 1.0}` | 边界框尺寸 |
| `transform` | enum flag | 0 | 变换能力标志 |
| `draw_style` | enum | `BOX_TRANSFORM` | 绘制样式 |
| `draw_options` | enum flag | `XFORM_CENTER_HANDLE` | 绘制选项 |
| `matrix` | float[16] | - | 绑定的变换矩阵 |

### 9.2. 目标属性

```cpp
WM_gizmotype_target_property_def(gzt, \"matrix\", PROP_FLOAT, 16);
```

这定义了 gizmo 支持的绑定属性类型：
- **名称**：`matrix`
- **类型**：`PROP_FLOAT`
- **长度**：16（4x4 矩阵）

---

## 10. 总结

### 10.1. 核心功能矩阵

| 功能 | 实现函数 | 关键算法 |
|------|----------|----------|
| **绘制** | `gizmo_cage2d_draw` | 根据样式调用不同绘制函数 |
| **选择** | `gizmo_cage2d_test_select` | 投影 + 矩形相交测试 |
| **调用** | `gizmo_cage2d_invoke` | 保存原始状态 |
| **模态** | `gizmo_cage2d_modal` | 平移/旋转/缩放计算 |
| **退出** | `gizmo_cage2d_exit` | 确认或撤销 |

### 10.2. 交互流程

```
用户点击
    ↓
gizmo_cage2d_test_select()  ← 确定点击的部件
    ↓
gizmo_cage2d_invoke()       ← 保存原始状态
    ↓
gizmo_cage2d_modal()        ← 循环处理鼠标移动
    ├─ 平移：计算位移增量
    ├─ 旋转：使用拨盘计算角度
    └─ 缩放：计算比例并应用
    ↓
gizmo_cage2d_exit()         ← 确认或撤销
```

### 10.3. 关键技术点

1. **空间转换**：所有计算在局部空间进行，通过 `matrix_space` 转换
2. **枢轴缩放**：支持围绕任意点缩放（边缘/角/中心）
3. **旋转处理**：使用 `BLI_dial_2d` 库进行精确角度计算
4. **修饰键**：Shift 临时启用等比缩放（通过 `use_temp_uniform`）
5. **选择优化**：扩大热区（margin/2）提高点击体验
6. **撤销支持**：保存原始矩阵，取消时恢复

### 10.4. 使用示例

```cpp
// 创建 gizmo
wmGizmo *gz = WM_gizmo_new(\"GIZMO_GT_cage_2d\", gzgroup, nullptr);

// 配置
RNA_enum_set(gz->ptr, \"transform\",
  ED_GIZMO_CAGE_XFORM_FLAG_TRANSLATE |
  ED_GIZMO_CAGE_XFORM_FLAG_ROTATE |
  ED_GIZMO_CAGE_XFORM_FLAG_SCALE);

RNA_enum_set(gz->ptr, \"draw_style\", ED_GIZMO_CAGE2D_STYLE_BOX_TRANSFORM);
RNA_enum_set(gz->ptr, \"draw_options\",
  ED_GIZMO_CAGE_DRAW_FLAG_XFORM_CENTER_HANDLE |
  ED_GIZMO_CAGE_DRAW_FLAG_CORNER_HANDLES);

// 设置尺寸
float dims[2] = {100.0f, 100.0f};
RNA_float_set_array(gz->ptr, \"dimensions\", dims);

// 绑定属性
wmGizmoPropertyFnParams params{};
params.value_get_fn = my_getter;
params.value_set_fn = my_setter;
params.user_data = my_data;
WM_gizmo_target_property_def_func(gz, \"matrix\", ¶ms);
```

---

**源码总览**: `source/blender/editors/gizmo_library/gizmo_types/cage2d_gizmo.cc` (1475 行)

**关键函数**:
- 绘制: `gizmo_cage2d_draw_intern` (697-881)
- 选择: `gizmo_cage2d_test_select` (929-1052)
- 模态: `gizmo_cage2d_modal` (1157-1365)
- 定义: `GIZMO_GT_cage_2d` (1412-1467)

**下一步**: 详见 `07_Gizmo_Modifier_Keys.md` 了解修饰键的详细处理机制。
