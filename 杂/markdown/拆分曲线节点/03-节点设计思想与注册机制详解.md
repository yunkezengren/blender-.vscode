# 几何节点设计思想与注册机制详解

## 一、整体架构设计思想

Blender 几何节点的设计遵循了**数据流编程 (Dataflow Programming)** 范式，其核心思想是：

```mermaid
flowchart TB
    subgraph 设计思想["💡 核心设计思想"]
        direction LR
        
        Dataflow["<span style='color:#FF6B6B'>数据流驱动</span><br/>节点 = 函数<br/>连线 = 数据传递"] 
        
        LazyEval["<span style='color:#4ECDC4'>延迟求值</span><br/>Field 系统<br/>按需计算"] 
        
        TypeSafe["<span style='color:#45B7D1'>类型安全</span><br/>模板 + 类型检查<br/>编译期验证"] 
        
        Modular["<span style='color:#96CEB4'>高度模块化</span><br/>声明与实现分离<br/>插件化注册"]
    end
    
    style Dataflow fill:#FFE5E5,stroke:#FF6B6B,stroke-width:3px
    style LazyEval fill:#E5F5F5,stroke:#4ECDC4,stroke-width:3px
    style TypeSafe fill:#E5F0FF,stroke:#45B7D1,stroke-width:3px
    style Modular fill:#E5F5E5,stroke:#96CEB4,stroke-width:3px
```

### 1.1 数据流驱动 (Dataflow)

在几何节点编辑器中：
- **节点 (Node)** = 一个纯函数，输入 → 处理 → 输出
- **连线 (Link)** = 数据依赖关系，决定执行顺序
- **节点树 (NodeTree)** = 一个有向无环图 (DAG)，系统自动拓扑排序

```mermaid
flowchart LR
    A["📦 输入几何"] --> B["⚙️ 拆分曲线节点"]
    C["🎯 选择字段"] --> B
    D["🔧 删除选项"] --> B
    B --> E["📤 输出几何"]
    
    style A fill:#FFE5E5,stroke:#FF6B6B
    style B fill:#E5F0FF,stroke:#45B7D1,stroke-width:3px
    style C fill:#E5F5F5,stroke:#4ECDC4
    style D fill:#FFF8E5,stroke:#FFEAA7
    style E fill:#E5F5E5,stroke:#96CEB4
```

### 1.2 延迟求值 (Lazy Evaluation)

**Field 系统**是 Blender 几何节点最核心的创新之一：

```mermaid
sequenceDiagram
    participant User as 👤 用户操作
    participant Editor as 🎨 节点编辑器
    participant Field as 🎯 Field 系统
    participant Eval as ⚙️ 求值引擎
    
    User->>Editor: 创建节点并连线
    Editor->>Field: 构建 Field 表达式树
    Note over Field: 此时仅构建表达式<br/>不执行实际计算
    
    User->>Editor: 查看结果/渲染
    Editor->>Eval: 请求输出
    Eval->>Field: 触发 Field 求值
    Field->>Field: 拓扑排序
    Field->>Field: 批量执行计算
    Field->>Eval: 返回结果
```

**Field 的优势**：
- 自动合并相同计算（去重）
- 批量处理数据（SIMD 优化）
- 延迟到真正需要时才计算

### 1.3 声明式编程 (Declarative)

节点开发者只需要**声明**节点有什么，而不是**命令**系统怎么做：

```cpp
// 声明式：声明节点有什么输入输出
static void node_declare(NodeDeclarationBuilder &b)
{
    b.add_input<decl::Geometry>("Curve"_ustr)     // 声明几何输入
         .supported_type({GeometryComponent::Type::Curve, 
                          GeometryComponent::Type::GreasePencil});
    b.add_input<decl::Bool>("Selection"_ustr)     // 声明布尔字段输入
         .default_value(true)
         .field_on_all();
    b.add_output<decl::Geometry>("Curve"_ustr);   // 声明几何输出
}

// 命令式：实现具体逻辑
static void node_geo_exec(GeoNodeExecParams params)
{
    // 实现数据处理逻辑
}
```

---

## 二、节点的三层结构

```mermaid
flowchart TB
    subgraph 三层结构["🏗️ 节点的三层结构"]
        direction TB
        
        Layer1["<span style='color:#FF6B6B'>① 声明层 (Declare)</span><br/>node_declare()<br/>静态元数据<br/>输入输出定义"] 
        
        Layer2["<span style='color:#4ECDC4'>② 执行层 (Execute)</span><br/>node_geo_exec()<br/>动态数据处理<br/>算法实现"] 
        
        Layer3["<span style='color:#45B7D1'>③ 注册层 (Register)</span><br/>node_register()<br/>系统注册<br/>类型绑定"]
    end
    
    Layer3 --> Layer1
    Layer3 --> Layer2
    
    style Layer1 fill:#FFE5E5,stroke:#FF6B6B,stroke-width:3px
    style Layer2 fill:#E5F5F5,stroke:#4ECDC4,stroke-width:3px
    style Layer3 fill:#E5F0FF,stroke:#45B7D1,stroke-width:3px
```

### 2.1 声明层 (Declaration Layer)

```cpp
static void node_declare(NodeDeclarationBuilder &b)
{
    b.use_custom_socket_order();           // 使用自定义 socket 顺序
    b.allow_any_socket_order();            // 允许任意 socket 顺序
    
    // 输入声明
    b.add_input<decl::Geometry>("Curve"_ustr)
        .supported_type({GeometryComponent::Type::Curve, 
                         GeometryComponent::Type::GreasePencil})
        .description("Curves to split");
    
    // 输出声明
    b.add_output<decl::Geometry>("Curve"_ustr)
        .propagate_all()                    // 传播所有属性
        .align_with_previous();             // 与上一个对齐
    
    // 字段输入声明
    b.add_input<decl::Bool>("Selection"_ustr)
        .default_value(true)
        .hide_value()                       // 隐藏默认值
        .field_on_all()                     // 在所有域上作为字段
        .description("Control points used to split curves");
}
```

**设计意图**：
- 将接口定义与实现分离
- 支持 UI 自动生成
- 支持链接搜索和类型检查

### 2.2 执行层 (Execution Layer)

```cpp
static void node_geo_exec(GeoNodeExecParams params)
{
    // 1. 提取输入
    GeometrySet geometry_set = params.extract_input<GeometrySet>("Curve"_ustr);
    const Field<bool> selection_field = params.extract_input<Field<bool>>("Selection"_ustr);
    const bool delete_segment = params.extract_input<bool>("Delete Segment"_ustr);
    
    // 2. 处理数据
    geometry::foreach_real_geometry(geometry_set, [&](GeometrySet &geometry_set) {
        // 处理曲线...
    });
    
    // 3. 设置输出
    params.set_output("Curve"_ustr, std::move(geometry_set));
}
```

**设计意图**：
- 纯函数式设计，无副作用
- 参数化输入输出
- 支持延迟求值

### 2.3 注册层 (Registration Layer)

这是本文重点，详见下一节。

---

## 三、注册机制深度解析 (node_register)

```mermaid
flowchart TB
    subgraph 注册流程["📝 节点注册流程"]
        direction TB
        
        Start(["开始"]) --> Define["<span style='color:#FF6B6B'>1. 定义 bNodeType</span><br/>static bke::bNodeType ntype"]
        
        Define --> Base["<span style='color:#4ECDC4'>2. 基础设置</span><br/>geo_node_type_base()<br/>设置 idname"]
        
        Base --> Meta["<span style='color:#45B7D1'>3. 元数据设置</span><br/>ui_name, ui_description<br/>nclass (类别)"]
        
        Meta --> Callback["<span style='color:#96CEB4'>4. 回调绑定</span><br/>declare = node_declare<br/>geometry_node_execute = node_geo_exec"]
        
        Callback --> Register["<span style='color:#FFEAA7'>5. 注册到系统</span><br/>bke::node_register_type()"]
        
        Register --> Macro["<span style='color:#DDA0DD'>6. 发现宏</span><br/>NOD_REGISTER_NODE()"]
        
        Macro --> End(["结束"])
    end
    
    style Define fill:#FFE5E5,stroke:#FF6B6B,stroke-width:2px
    style Base fill:#E5F5F5,stroke:#4ECDC4,stroke-width:2px
    style Meta fill:#E5F0FF,stroke:#45B7D1,stroke-width:2px
    style Callback fill:#E5F5E5,stroke:#96CEB4,stroke-width:2px
    style Register fill:#FFF8E5,stroke:#FFEAA7,stroke-width:2px
    style Macro fill:#F5E5F5,stroke:#DDA0DD,stroke-width:2px
```

### 3.1 完整注册代码解析

```cpp
static void node_register()              // ① 静态注册函数
{
    static bke::bNodeType ntype;         // ② 静态存储节点类型定义

    // ③ 基础初始化
    geo_node_type_base(&ntype, "GeometryNodeSplitCurve"_ustr);
    
    // ④ UI 元数据
    ntype.ui_name = "Split Curve";                           // 显示名称
    ntype.ui_description = "Split curves by selected control points";  // 描述
    ntype.nclass = NODE_CLASS_GEOMETRY;                      // 节点类别
    
    // ⑤ 功能回调绑定
    ntype.declare = node_declare;                            // 声明函数
    ntype.geometry_node_execute = node_geo_exec;            // 执行函数
    
    // ⑥ 注册到系统
    bke::node_register_type(ntype);
}

// ⑦ 发现宏 - 关键！
NOD_REGISTER_NODE(node_register)
```

### 3.2 逐行详解

#### ① `static void node_register()`

```mermaid
flowchart LR
    subgraph 静态函数["🔒 为什么用 static？"]
        direction TB
        
        A["<span style='color:#FF6B6B'>内部链接</span><br/>仅在当前翻译单元可见<br/>避免命名冲突"] 
        
        B["<span style='color:#4ECDC4'>自动执行</span><br/>通过 NOD_REGISTER_NODE<br/>在启动时调用"] 
        
        C["<span style='color:#45B7D1'>延迟初始化</span><br/>第一次使用时初始化<br/>控制初始化顺序"]
    end
    
    style A fill:#FFE5E5,stroke:#FF6B6B
    style B fill:#E5F5F5,stroke:#4ECDC4
    style C fill:#E5F0FF,stroke:#45B7D1
```

#### ② `static bke::bNodeType ntype`

```mermaid
classDiagram
    class bNodeType {
        <<struct>>
        +UString idname
        +string ui_name
        +string ui_description
        +int ui_icon
        +short nclass
        +short flag
        +bNodeSocketTemplate* inputs
        +bNodeSocketTemplate* outputs
        +NodeDeclareFunction declare
        +NodeGeometryExecFunction geometry_node_execute
        +bool(* poll)()
        +...
    }
    
    note for bNodeType "静态存储：
    1. 生命周期贯穿整个程序
    2. 全局唯一，通过 idname 区分
    3. 程序启动时注册，退出时销毁"
```

**为什么是 `static`**：
- 生命周期：从注册到程序结束
- 作用域：仅在当前文件可见
- 初始化：零初始化 + 后续代码填充

#### ③ `geo_node_type_base(&ntype, "GeometryNodeSplitCurve"_ustr)`

```mermaid
sequenceDiagram
    participant Node as node_register
    participant Base as geo_node_type_base
    participant BKE as BKE_node
    
    Node->>Base: 调用基础初始化
    Note over Base: 在 node_geometry_util.cc 中定义
    
    Base->>BKE: bke::node_type_base(*ntype, idname, legacy_type)
    Note over BKE: 设置基础属性
    
    Base->>Base: ntype->poll = geo_node_poll_default
    Note over Base: 设置默认轮询函数
    
    Base->>Base: ntype->insert_link = node_insert_link_default
    Note over Base: 设置默认链接插入
    
    Base->>Base: ntype->gather_link_search_ops = ...
    Note over Base: 设置链接搜索操作
```

**源码实现**（`source/blender/nodes/geometry/node_geometry_util.cc:136`）：

```cpp
void geo_node_type_base(bke::bNodeType *ntype,
                        UString idname,
                        const std::optional<int16_t> legacy_type)
{
    // 调用更底层的 base 函数
    bke::node_type_base(*ntype, idname, legacy_type);
    
    // 设置几何节点特有的默认值
    ntype->poll = geo_node_poll_default;                    // 默认轮询
    ntype->insert_link = node_insert_link_default;          // 默认链接处理
    ntype->gather_link_search_ops = nodes::search_link_ops_for_basic_node;
}
```

**设计模式**：**模板方法模式 (Template Method)**
- `node_type_base`：通用基础设置
- `geo_node_type_base`：几何节点特有设置
- `geo_cmp_node_type_base`：合成器+几何节点混合设置

#### ④ 元数据设置

```cpp
ntype.ui_name = "Split Curve";                           // 用户看到的名称
ntype.ui_description = "Split curves by selected...";    // 悬停提示
ntype.nclass = NODE_CLASS_GEOMETRY;                      // 节点类别（决定颜色）
```

**节点类别 (nclass)**：

```mermaid
flowchart LR
    subgraph 节点类别["🎨 节点类别与颜色"]
        direction TB
        
        Input["<span style='color:#6B8E23'>NODE_CLASS_INPUT</span><br/>输入 - 绿色"] 
        Output["<span style='color:#CD5C5C'>NODE_CLASS_OUTPUT</span><br/>输出 - 红色"] 
        Geometry["<span style='color:#4169E1'>NODE_CLASS_GEOMETRY</span><br/>几何 - 蓝色"] 
        Attribute["<span style='color:#9932CC'>NODE_CLASS_ATTRIBUTE</span><br/>属性 - 紫色"] 
        Material["<span style='color:#FF8C00'>NODE_CLASS_MATERIAL</span><br/>材质 - 橙色"] 
        Texture["<span style='color:#20B2AA'>NODE_CLASS_TEXTURE</span><br/>纹理 - 青色"] 
    end
    
    style Input fill:#F0FFF0,stroke:#6B8E23
    style Output fill:#FFF0F0,stroke:#CD5C5C
    style Geometry fill:#F0F5FF,stroke:#4169E1,stroke-width:3px
    style Attribute fill:#F5F0FF,stroke:#9932CC
    style Material fill:#FFF8F0,stroke:#FF8C00
    style Texture fill:#F0FFFA,stroke:#20B2AA
```

#### ⑤ 回调绑定

```cpp
ntype.declare = node_declare;                            // 声明回调
ntype.geometry_node_execute = node_geo_exec;            // 执行回调
```

**回调类型定义**（`BKE_node.hh:124, 370`）：

```cpp
// 声明回调：定义节点接口
using NodeDeclareFunction = void (*)(nodes::NodeDeclarationBuilder &builder);

// 执行回调：实现节点逻辑
using NodeGeometryExecFunction = void (*)(nodes::GeoNodeExecParams params);
```

**为什么用函数指针而不是虚函数？**

```mermaid
flowchart TB
    subgraph 函数指针优势["🎯 使用函数指针的原因"]
        direction TB
        
        CCompat["<span style='color:#FF6B6B'>C 兼容性</span><br/>Blender 核心部分是 C 语言<br/>bNodeType 需要在 C 代码中使用"] 
        
        Performance["<span style='color:#4ECDC4'>性能</span><br/>函数指针调用开销小<br/>虚函数有 vtable 间接寻址"] 
        
        Flexibility["<span style='color:#45B7D1'>灵活性</span><br/>运行时动态替换实现<br/>支持插件热更新"] 
        
        Clarity["<span style='color:#96CEB4'>清晰性</span><br/>明确的回调接口<br/>易于理解和调试"]
    end
    
    style CCompat fill:#FFE5E5,stroke:#FF6B6B,stroke-width:2px
    style Performance fill:#E5F5F5,stroke:#4ECDC4,stroke-width:2px
    style Flexibility fill:#E5F0FF,stroke:#45B7D1,stroke-width:2px
    style Clarity fill:#E5F5E5,stroke:#96CEB4,stroke-width:2px
```

| 特性 | 函数指针 | 虚函数 |
|------|----------|--------|
| C 兼容 | ✅ 可以 | ❌ 不行（C++ 特性） |
| 调用开销 | 直接跳转 | vtable 查找 + 跳转 |
| 动态替换 | ✅ 运行时修改 | ❌ 编译期固定 |
| 代码清晰 | 明确的函数签名 | 隐藏在继承体系中 |
| 调试友好 | 可以直接看到函数地址 | 需要查看 vtable |

#### 为什么函数指针语法这么怪？

**源码位置**：`source/blender/blenkernel/BKE_node.hh:120-125`

```cpp
/* Use `void *` for callbacks that require C++. This is rather ugly, but works well for now. This
 * would not be necessary if we would use bNodeSocketType and bNodeType only in C++ code.
 * However, achieving this requires quite a few changes currently. */
using NodeMultiFunctionBuildFunction = void (*)(nodes::NodeMultiFunctionBuilder &builder);
using NodeGeometryExecFunction = void (*)(nodes::GeoNodeExecParams params);
using NodeDeclareFunction = void (*)(nodes::NodeDeclarationBuilder &builder);
```

**"怪语法"解析**：

```mermaid
flowchart LR
    subgraph 语法解析["📝 函数指针类型别名语法"]
        direction TB
        
        Syntax["<span style='color:#FF6B6B'>using 别名 = 返回类型 (*)(参数列表);</span>"] 
        
        Example1["NodeGeometryExecFunction = void (*)(GeoNodeExecParams)"] 
        
        Example2["NodeDeclareFunction = void (*)(NodeDeclarationBuilder &)"] 
        
        ReadAs["<span style='color:#4ECDC4'>读作：</span><br/>这是一个函数指针<br/>指向返回 void<br/>接受指定参数的函数"]
    end
    
    Syntax --> Example1 --> Example2 --> ReadAs
    
    style Syntax fill:#FFE5E5,stroke:#FF6B6B,stroke-width:2px
    style Example1 fill:#E5F5F5,stroke:#4ECDC4,stroke-width:2px
    style Example2 fill:#E5F0FF,stroke:#45B7D1,stroke-width:2px
    style ReadAs fill:#E5F5E5,stroke:#96CEB4,stroke-width:2px
```

**语法拆解**：

```cpp
using NodeGeometryExecFunction = void (*)(nodes::GeoNodeExecParams params);
//     ^^^^^^^^^^^^^^^^^^^^^^^^   ^^^^  ^  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
//            类型别名             返回   |           参数列表
//                                    指针标记
```

**为什么不用更简单的语法？**

| 替代方案 | 代码 | 缺点 |
|----------|------|------|
| `std::function` | `std::function<void(GeoNodeExecParams)>` | 有类型擦除开销，不适合高频调用 |
| 模板 | `template<typename F> void set_callback(F f)` | 需要在头文件暴露实现，编译依赖增加 |
| 虚函数 | `virtual void execute(GeoNodeExecParams)` | 不兼容 C，vtable 开销 |
| **函数指针** | `void (*func)(GeoNodeExecParams)` | ✅ 简单、高效、兼容 C |

**注释中的 `"rather ugly"` 指什么？**

```cpp
/* Use `void *` for callbacks that require C++. This is rather ugly, but works well for now. */
```

这里的 `"ugly"` 指的是：**C++ 代码需要使用 `void*` 来兼容 C 代码的回调**，而不是函数指针语法本身。

```cpp
// C++ 中的理想写法（但无法直接在 C 中使用）
using NodeGeometryExecFunction = void (*)(nodes::GeoNodeExecParams params);

// 为了 C 兼容，有时需要这样写
void* callback;  // "ugly"：丢失了类型安全，需要强制转换
```

**实际使用时的简洁性**：

```cpp
// 定义时：使用类型别名，清晰
ntype.geometry_node_execute = node_geo_exec;

// 调用时：像普通函数一样使用
ntype.geometry_node_execute(params);  // 简洁直观
```

#### ⑥ `bke::node_register_type(ntype)`

```mermaid
sequenceDiagram
    participant Register as node_register
    participant BKE as bke::node_register_type
    participant Map as 全局类型映射
    
    Register->>BKE: 传入 ntype
    BKE->>BKE: 验证 idname 唯一性
    BKE->>BKE: 生成 static_declaration（如果 declare 不为空）
    BKE->>Map: 插入到全局类型映射
    Note over Map: idname → bNodeType*
    BKE->>Register: 返回
```

**全局注册表**：
- 存储位置：`bke::nodetypes` 映射表
- 键：节点的 `idname`（如 `"GeometryNodeSplitCurve"`）
- 值：`bNodeType*` 指针

#### ⑦ `NOD_REGISTER_NODE(node_register)` - 最关键的部分

```mermaid
flowchart TB
    subgraph 发现机制["🔍 NOD_REGISTER_NODE 发现机制"]
        direction TB
        
        Source["<span style='color:#FF6B6B'>源代码</span><br/>NOD_REGISTER_NODE(node_register)"] 
        
        Script["<span style='color:#4ECDC4'>discover_nodes.py</span><br/>构建时扫描所有 .cc 文件<br/>查找 NOD_REGISTER_NODE 宏"] 
        
        Generate["<span style='color:#45B7D1'>生成代码</span><br/>生成 register_nodes.cc<br/>包含所有注册函数调用"] 
        
        Runtime["<span style='color:#96CEB4'>运行时</span><br/>启动时调用 register_nodes()<br/>执行所有 node_register 函数"]
    end
    
    Source --> Script
    Script --> Generate
    Generate --> Runtime
    
    style Source fill:#FFE5E5,stroke:#FF6B6B,stroke-width:2px
    style Script fill:#E5F5F5,stroke:#4ECDC4,stroke-width:2px
    style Generate fill:#E5F0FF,stroke:#45B7D1,stroke-width:2px
    style Runtime fill:#E5F5E5,stroke:#96CEB4,stroke-width:2px
```

**宏定义**（`NOD_register.hh:37-42`）：

```cpp
#define NOD_REGISTER_NODE(REGISTER_FUNC) \
  void REGISTER_FUNC##_discover(); \           // ① 声明发现函数
  void REGISTER_FUNC##_discover() \            // ② 定义发现函数
  { \
    REGISTER_FUNC(); \                         // ③ 调用实际注册函数
  }
```

**展开后的实际代码**：

```cpp
// 原始代码
NOD_REGISTER_NODE(node_register)

// 预处理器展开后
void node_register_discover();           // 声明oid node_register_discover()            // 定义
{
    node_register();                      // 调用注册
}
```

**为什么需要这个宏？**

1. **发现标记**：`discover_nodes.py` 脚本搜索 `NOD_REGISTER_NODE` 字符串来找到所有需要注册的节点
2. **包装函数**：创建非静态的包装函数 `xxx_discover()`，可以被外部调用
3. **避免魔法**：让代码中明显看出"这里有注册逻辑"，而不是隐式地搜索所有 `node_register` 函数

**构建时流程**：

```mermaid
sequenceDiagram
    participant Dev as 开发者
    participant Build as 构建系统
    participant Script as discover_nodes.py
    participant Generated as 生成的代码
    participant Compiler as 编译器
    
    Dev->>Build: 添加新节点文件
    Build->>Script: 运行发现脚本
    Script->>Script: 扫描所有 .cc 文件
    Script->>Script: 提取 NOD_REGISTER_NODE 调用
    Script->>Generated: 生成 register_nodes.cc
    Note over Generated: 包含所有 discover 函数调用
    Build->>Compiler: 编译所有源文件
    Compiler->>Generated: 链接生成的代码
```

---

## 四、运行时行为

### 4.1 启动时注册

```mermaid
sequenceDiagram
    participant Main as main()
    participant Register as register_nodes()
    participant Generated as 生成的代码
    participant Node1 as node_register_discover()
    participant Node2 as node_register_discover()
    participant BKE as bke::node_register_type()
    
    Main->>Register: 启动时调用
    Register->>Generated: 调用生成的注册函数
    
    Generated->>Node1: 调用第一个节点
    Node1->>Node1: node_register()
    Node1->>BKE: bke::node_register_type(ntype)
    
    Generated->>Node2: 调用第二个节点
    Node2->>Node2: node_register()
    Node2->>BKE: bke::node_register_type(ntype)
    
    Note over BKE: 所有节点类型注册完成
```

### 4.2 节点创建时

```mermaid
sequenceDiagram
    participant User as 用户
    participant Editor as 节点编辑器
    participant Type as 全局类型表
    participant Instance as bNode 实例
    
    User->>Editor: 添加节点
    Editor->>Type: 查找 idname
    Type-->>Editor: 返回 bNodeType*
    
    Editor->>Instance: 创建 bNode 实例
    Editor->>Instance: 复制 bNodeType 信息
    
    alt declare 不为空
        Editor->>Instance: 调用 declare()
        Instance->>Instance: 生成 sockets
    end
    
    alt initfunc 不为空
        Editor->>Instance: 调用 initfunc()
    end
```

### 4.3 节点执行时

```mermaid
sequenceDiagram
    participant Graph as 节点图
    participant Eval as 求值引擎
    participant Node as 节点实例
    participant Type as bNodeType
    participant Exec as node_geo_exec()
    
    Graph->>Eval: 请求输出
    Eval->>Eval: 拓扑排序
    
    loop 按顺序执行每个节点
        Eval->>Node: 执行节点
        Node->>Type: 获取 geometry_node_execute
        Type-->>Node: 返回函数指针
        Node->>Exec: 调用执行函数
        Exec->>Exec: 处理数据
        Exec-->>Node: 返回结果
    end
    
    Eval-->>Graph: 最终结果
```

---

## 五、设计模式总结

```mermaid
mindmap
  root((节点系统<br/>设计模式))
    工厂模式
      bke::node_register_type
      根据 idname 创建实例
    模板方法模式
      geo_node_type_base
      基础设置 + 特化设置
    策略模式
      函数指针回调
      declare / execute
    观察者模式
      NOD_REGISTER_NODE 发现
      构建时收集
    单例模式
      static bNodeType
      全局唯一类型定义
    依赖注入
      GeoNodeExecParams
      参数化执行
```

---

## 六、关键代码回顾

### 6.1 最小节点模板

```cpp
/* SPDX-FileCopyrightText: 2026 Blender Authors
 *
 * SPDX-License-Identifier: GPL-2.0-or-later */

#include "node_geometry_util.hh"

namespace blender::nodes::node_geo_example_cc {

// 1. 声明层：定义接口
static void node_declare(NodeDeclarationBuilder &b)
{
    b.add_input<decl::Geometry>("Geometry"_ustr);
    b.add_output<decl::Geometry>("Geometry"_ustr);
}

// 2. 执行层：实现逻辑
static void node_geo_exec(GeoNodeExecParams params)
{
    GeometrySet geometry = params.extract_input<GeometrySet>("Geometry"_ustr);
    // ... 处理 ...
    params.set_output("Geometry"_ustr, std::move(geometry));
}

// 3. 注册层：绑定到系统
static void node_register()
{
    static bke::bNodeType ntype;
    geo_node_type_base(&ntype, "GeometryNodeExample"_ustr);
    ntype.ui_name = "Example";
    ntype.ui_description = "Example node";
    ntype.nclass = NODE_CLASS_GEOMETRY;
    ntype.declare = node_declare;
    ntype.geometry_node_execute = node_geo_exec;
    bke::node_register_type(ntype);
}
NOD_REGISTER_NODE(node_register)

}  // namespace blender::nodes::node_geo_example_cc
```

### 6.2 核心要点

1. **三层分离**：声明、执行、注册各司其职
2. **静态存储**：`bNodeType` 静态存储，程序生命周期内有效
3. **函数指针**：使用 C 风格回调，兼容性和性能兼顾
4. **宏发现**：`NOD_REGISTER_NODE` 实现自动发现
5. **延迟求值**：Field 系统实现按需计算

---

## 七、延伸阅读

- `source/blender/nodes/NOD_register.hh` - 注册宏定义
- `source/blender/nodes/geometry/node_geometry_util.cc` - 几何节点工具函数
- `source/blender/blenkernel/BKE_node.hh` - 节点类型定义
- `source/blender/nodes/intern/node_common.cc` - 通用节点逻辑
- `build_files/cmake/modules/discover_nodes.py` - 节点发现脚本
