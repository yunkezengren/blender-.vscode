# Spreadsheet 绘制系统

> 📍 目标：理解UI绘制流程、GPU渲染和性能优化策略

---

## 1. 整体架构

### 1.1 绘制层次

```mermaid
flowchart TB
    subgraph View层
        V2D[View2D] --> SC[bContext]
    end

    subgraph 布局层
        SL[SpreadsheetLayout] --> CD[ColumnLayout]
        CD --> CV[ColumnValues]
    end

    subgraph 绘制抽象层
        SSD[SpreadsheetDrawer] --> SSI[SpreadsheetDrawer实现]
        SSI --> DPC[CellDrawParams]
    end

    subgraph GPU层
        GPU[GPU_immediate] --> IMM[immRectf/immBegin/immEnd]
        BLF[BLF_api] --> 文字渲染
    end

    subgraph UI层
        UI[UI_interface] --> BUT[按钮/控件]
    end

    SC --> SL
    SL --> SSD
    SSD --> GPU
    SSD --> UI
```

### 1.2 核心组件关系

```mermaid
classDiagram
    class SpreadsheetDrawer {
        <<abstract>>
        +int left_column_width
        +int top_row_height
        +int row_height
        +int tot_rows
        +int tot_columns
        +SpreadsheetDrawer()
        +~SpreadsheetDrawer() virtual
        +draw_top_row_cell(column_index, params)*
        +draw_left_column_cell(row_index, params)*
        +draw_content_cell(row_index, column_index, params)*
        +column_width(column_index)* int
    }

    class CellDrawParams {
        +Block* block
        +int xmin, ymin
        +int width, height
    }

    class SpreadsheetDrawer_impl {
        -Vector~ColumnLayout~ columns_
        -IndexMask row_mask_
        +draw_top_row_cell() override
        +draw_left_column_cell() override
        +draw_content_cell() override
        +column_width() override
    }

    class SpreadsheetLayout {
        +Vector~ColumnLayout~ columns
        +IndexMask row_indices
        +int index_column_width
    }

    class ColumnLayout {
        +ColumnValues* values
        +int width
    }

    SpreadsheetDrawer <|-- SpreadsheetDrawer_impl
    SpreadsheetDrawer ..> CellDrawParams : 使用
    SpreadsheetDrawer_impl --> SpreadsheetLayout : 布局数据
    SpreadsheetLayout --> ColumnLayout : 包含
    ColumnLayout --> ColumnValues : 引用
```

---

## 2. 绘制流程

### 2.1 主绘制流程

```mermaid
sequenceDiagram
    participant WM as WindowManager
    participant Region as ARegion
    participant SSD as SpreadsheetDrawer
    participant Draw as draw_spreadsheet_in_region

    WM->>Region: 触发重绘事件
    Region->>Draw: draw_spreadsheet_in_region(C, region, drawer)

    Draw->>Draw: 获取可见区域(clip rectangle)
    Draw->>Draw: 计算可见行范围
    Draw->>Draw: 计算可见列范围

    Draw->>SSD: 绘制索引列背景
    SSD->>Draw: draw_index_column_background()

    Draw->>SSD: 绘制交替行背景
    SSD->>Draw: draw_alternating_row_overlay()

    Draw->>SSD: 绘制表头背景
    SSD->>Draw: draw_top_row_background()

    Draw->>SSD: 绘制分隔线
    SSD->>Draw: draw_separator_lines()

    Draw->>SSD: 绘制列拖拽视觉效果
    alt 正在拖拽列
        SSD->>Draw: draw_reorder_column_visualization()
    end

    Draw->>SSD: 绘制表头单元格
    loop 每个可见列
        SSD->>SSD: draw_top_row_cell()
    end

    Draw->>SSD: 绘制内容单元格
    loop 每个可见行
        SSD->>SSD: draw_left_column_cell()
        loop 每个可见列
            SSD->>SSD: draw_content_cell()
        end
    end
```

### 2.2 详细绘制步骤

```mermaid
flowchart TB
    subgraph 步骤1：背景
        A[draw_index_column_background] --> B[灰色背景]
        C[draw_alternating_row_overlay] --> D[交替行颜色]
        E[draw_top_row_background] --> F[表头背景]
    end

    subgraph 步骤2：网格
        G[draw_separator_lines] --> H[列分隔线]
        G --> I[行分隔线]
    end

    subgraph 步骤3：单元格
        J[draw_top_row_cell] --> K[表头文字]
        L[draw_left_column_cell] --> M[行索引]
        N[draw_content_cell] --> O[单元格值]
    end

    subgraph 步骤4：特效
        P[draw_reorder_column_visualization] --> Q[列拖拽效果]
    end

    A --> C --> E --> G --> J --> L --> N --> P
```

---

## 3. GPU绘制实现

### 3.1 Immediate Mode绘制

```mermaid
flowchart TB
    subgraph GPU立即模式
        A[immBegin] --> B[设置顶点格式]
        B --> C[immAttr* 设置属性]
        C --> D[immVertex* 提交顶点]
        D --> E[immEnd]
    end

    subgraph 矩形绘制示例
        F[immUniformThemeColorShade] --> G[设置颜色]
        G --> H[immRectf pos, x1, y1, x2, y2]
    end
```

### 3.2 背景绘制代码分析

```cpp
// 索引列背景
static void draw_index_column_background(const uint pos,
                                         const ARegion *region,
                                         const SpreadsheetDrawer &drawer)
{
    // 使用主题颜色(背景色+11亮度)
    immUniformThemeColorShade(TH_BACK, 11);

    // 绘制左侧索引列矩形
    immRectf(pos,
             0,                                      // 左边界
             region->winy - drawer.top_row_height,   // 上边界(排除表头)
             drawer.left_column_width,               // 右边界
             0);                                      // 下边界
}

// 交替行覆盖
static void draw_alternating_row_overlay(const uint pos,
                                          const int scroll_offset_y,
                                          const ARegion *region,
                                          const SpreadsheetDrawer &drawer)
{
    immUniformThemeColor(TH_ROW_ALTERNATE);
    GPU_blend(GPU_BLEND_ALPHA);  // 启用透明混合

    const int row_pair_height = drawer.row_height * 2;
    // 计算第一个可见行的位置
    const int row_top_y = region->winy - drawer.top_row_height -
                          (scroll_offset_y % row_pair_height);

    // 绘制交替的半透明行
    for (const int i : IndexRange(region->winy / row_pair_height + 1)) {
        // 计算当前矩形坐标
        int y_top = row_top_y - i * row_pair_height - drawer.row_height;
        int y_bottom = y_top - drawer.row_height;

        // 限制在内容区域
        y_top = std::min(y_top, region->winy - drawer.top_row_height);
        y_bottom = std::min(y_bottom, region->winy - drawer.top_row_height);

        immRectf(pos, 0, y_top, region->winx, y_bottom);
    }

    GPU_blend(GPU_BLEND_NONE);  // 恢复混合状态
}
```

---

## 4. 单元格绘制

### 4.1 表头单元格绘制

```mermaid
flowchart TB
    A[draw_top_row_cell] --> B[获取列信息]
    B --> C[创建uiBlock]
    C --> D[添加uiDefBut]
    D --> E{列是否拖拽中?}
    E -->|是| F[应用偏移量]
    E -->|否| G[正常位置]
    F --> H[设置样式]
    G --> H
    H --> I[渲染按钮]
```

### 4.2 内容单元格绘制

```cpp
void SpreadsheetDrawer_impl::draw_content_cell(int row_index,
                                                  int column_index,
                                                  const CellDrawParams &params) const
{
    const ColumnLayout &column = columns_[column_index];
    const ColumnValues &values = *column.values;

    // 1. 获取该行的值
    const GVArray &data = values.data();
    const CPPType &type = data.type();

    // 2. 根据类型渲染
    switch (cpp_type_to_column_type(type)) {
        case SPREADSHEET_VALUE_TYPE_BOOL:
            draw_bool_cell(params, data.get<bool>(row_index));
            break;
        case SPREADSHEET_VALUE_TYPE_INT32:
            draw_int_cell(params, data.get<int32_t>(row_index));
            break;
        case SPREADSHEET_VALUE_TYPE_FLOAT:
            draw_float_cell(params, data.get<float>(row_index));
            break;
        case SPREADSHEET_VALUE_TYPE_FLOAT3:
            draw_float3_cell(params, data.get<float3>(row_index));
            break;
        case SPREADSHEET_VALUE_TYPE_COLOR:
            draw_color_cell(params, data.get<ColorGeometry4f>(row_index));
            break;
        // ... 更多类型
    }
}
```

### 4.3 各类型绘制方式

```mermaid
flowchart TB
    subgraph 类型到渲染
        A[bool] --> B[复选框图标]
        C[int] --> D[右对齐数字]
        E[float] --> F[固定精度浮点]
        G[float2] --> H[(x, y)]
        I[float3] --> J[(x, y, z)]
        K[float4] --> L[(x, y, z, w)]
        M[color] --> N[色块+RGBA数值]
        O[string] --> P[左对齐文本]
        Q[instance] --> R[图标+名称]
        S[quaternion] --> T[(w, x, y, z)]
        U[float4x4] --> V[4x4矩阵]
    end
```

---

## 5. 布局计算

### 5.1 SpreadsheetLayout构建

```mermaid
sequenceDiagram
    participant Space as SpaceSpreadsheet
    participant DS as DataSource
    participant SL as SpreadsheetLayout
    participant UI as UI绘制

    Space->>DS: get_data_source(C)
    DS-->>Space: unique_ptr~DataSource~

    Space->>SL: spreadsheet_drawer_from_layout(*data_source)

    SL->>DS: foreach_default_column_ids()
    loop 每个列ID
        SL->>DS: get_column_values(column_id)
        DS-->>SL: unique_ptr~ColumnValues~
        SL->>SL: 创建ColumnLayout
        SL->>SL: 计算列宽
    end

    SL->>Space: get_active_table()
    Space-->>SL: SpreadsheetTable*

    SL->>SL: 应用用户列宽设置
    SL->>DS: tot_rows()
    SL->>SL: 创建IndexMask(应用行筛选)

    SL-->>UI: 返回SpreadsheetLayout
```

### 5.2 列宽计算逻辑

```mermaid
flowchart TB
    A[计算布局] --> B[遍历所有ColumnLayout]
    B --> C{用户手动设置过宽度?}
    C -->|是| D[使用用户设置]
    C -->|否| E[使用fit_column_width_px]
    E --> F[采样计算最佳宽度]
    D --> G[累加总宽度]
    F --> G
    G --> H[设置View2D范围]
```

### 5.3 可见区域计算

```cpp
// 计算可见行范围
const int row_start = std::max(0, int(-scroll_offset_y / row_height));
const int row_end = std::min(
    layout.row_indices.size(),
    row_start + int(std::ceil(view_height / row_height)) + 1
);

// 计算可见列范围
float x_offset = drawer.left_column_width;
for (const int column_index : layout.columns.index_range()) {
    const ColumnLayout &column = layout.columns[column_index];
    const float column_x_start = x_offset;
    const float column_x_end = x_offset + column.width;

    if (column_x_end > clip_x_min && column_x_start < clip_x_max) {
        // 该列可见
        visible_columns.append(column_index);
    }
    x_offset = column_x_end;
}
```

---

## 6. 性能优化

### 6.1 按需渲染策略

```mermaid
flowchart TB
    subgraph 优化策略
        A[只渲染可见区域] --> B[计算clip rectangle]
        B --> C[跳过屏幕外单元格]
        D[缓存ColumnValues] --> E[避免重复计算]
        F[采样计算列宽] --> G[max_sample_size限制]
    end
```

### 6.2 渲染复杂度分析

| 操作 | 复杂度 | 优化策略 |
|------|-------|---------|
| 背景绘制 | O(1) | GPU批量绘制 |
| 行数计算 | O(1) | 缓存tot_rows |
| 可见行数 | O(1) | 只渲染屏幕内 |
| 列宽计算 | O(N) 采样 | max_sample_size限制 |
| 单元格绘制 | O(可见行×可见列) | 跳过屏幕外 |
| 文本渲染 | O(字符数) | 缓存字形 |

### 6.3 大数据集处理

```mermaid
flowchart TB
    subgraph 大数据优化
        A[行数>10000] --> B[IndexMask应用]
        B --> C[只显示筛选后行]
        C --> D[延迟列值计算]
        D --> E[滚动时实时计算]
    end
```

---

## 7. View2D 坐标系统

### 7.1 坐标转换

```mermaid
flowchart TB
    subgraph 坐标空间
        A[视图空间(View Space)] --> B[区域空间(Region Space)]
        B --> C[屏幕空间(Screen Space)]
    end

    subgraph 转换函数
        D[ui::view2d_region_to_view_x] --> E[region_x -> view_x]
        F[ui::view2d_view_to_region_x] --> G[view_x -> region_x]
    end
```

### 7.2 列位置计算

```cpp
// 计算列的视图空间位置
float x_offset = drawer.left_column_width;
for (SpreadsheetColumn *column : columns) {
    column->runtime->left_x = x_offset;
    x_offset += column->width * SPREADSHEET_WIDTH_UNIT;
    column->runtime->right_x = x_offset;
}

// 鼠标坐标转换
const float cursor_x_view = ui::view2d_region_to_view_x(&region.v2d, cursor_re.x);

// 检测鼠标悬停列
for (SpreadsheetColumn *column : columns) {
    if (cursor_x_view > column->runtime->left_x &&
        cursor_x_view <= column->runtime->right_x) {
        // 找到悬停列
    }
}
```

---

## 8. 特效和交互视觉反馈

### 8.1 列拖拽视觉效果

```mermaid
flowchart TB
    subgraph 拖拽状态
        A[按下鼠标] --> B[启动reorder_columns操作符]
        B --> C[设置visualization_data]
        C --> D[每帧更新位置偏移]
        D --> E[绘制半透明列]
        F[释放鼠标] --> G[交换列位置]
    end

    subgraph 绘制效果
        H[半透明列] --> I[高亮放置位置]
        J[列边缘指示器] --> K[用户反馈]
    end
```

### 8.2 行高亮效果

```mermaid
flowchart TB
    subgraph 悬停效果
        A[鼠标移动] --> B[计算悬停行]
        B --> C[标记redraw]
        C --> D[绘制高亮行]
    end

    subgraph 选中效果
        E[点击行] --> F[设置selected_index]
        F --> G[持久化高亮]
    end
```

---

## 9. 颜色主题

### 9.1 主题常量

```mermaid
flowchart TB
    subgraph 颜色使用
        A[TH_BACK] --> B[背景色]
        C[TH_BACK+11] --> D[索引列背景]
        E[TH_ROW_ALTERNATE] --> F[交替行颜色]
        G[TH_HEADER] --> H[表头颜色]
        I[TH_GRID] --> J[分隔线颜色]
        K[TH_TEXT] --> L[文字颜色]
        M[TH_SELECT] --> N[选中高亮]
    end
```

### 9.2 主题适配

```cpp
// 根据主题调整
void apply_theme_colors() {
    // 获取当前主题颜色
    const uchar *color = UI_GetThemeColor4ubv(TH_BACK);

    // 索引列背景稍亮
    immUniformThemeColorShade(TH_BACK, 11);

    // 交替行使用预设颜色
    immUniformThemeColor(TH_ROW_ALTERNATE);

    // 分隔线
    immUniformThemeColorShade(TH_GRID, -20);
}
```

---

## 10. 关键代码清单

### 10.1 绘制函数

| 函数 | 文件 | 职责 |
|------|------|------|
| `draw_spreadsheet_in_region()` | draw.cc | 主绘制入口 |
| `draw_index_column_background()` | draw.cc | 索引列背景 |
| `draw_alternating_row_overlay()` | draw.cc | 交替行 |
| `draw_top_row_background()` | draw.cc | 表头背景 |
| `draw_separator_lines()` | draw.cc | 网格线 |
| `spreadsheet_drawer_from_layout()` | layout.cc | 创建绘制器 |

### 10.2 绘制类

| 类 | 文件 | 职责 |
|----|------|------|
| `SpreadsheetDrawer` | draw.hh | 绘制抽象基类 |
| `CellDrawParams` | draw.hh | 单元格绘制参数 |
| `SpreadsheetLayout` | layout.hh | 布局信息 |
| `ColumnLayout` | layout.hh | 单列布局 |

---

## 11. 性能调优建议

### 11.1 渲染优化

1. **减少draw call**：批量绘制背景
2. **文字缓存**：避免每帧重新计算字形
3. **延迟加载**：列值按需计算
4. **裁剪**：只渲染可见区域

### 11.2 内存优化

1. **对象池**：重用CellDrawParams
2. **智能指针**：unique_ptr自动释放
3. **临时内存**：ResourceScope管理

---

*文档创建: 2025年*
*基于 spreadsheet_draw*.hh/cc, spreadsheet_layout*.hh/cc 分析*
