# 21. 调用堆栈详解 DRW_text_cache_add

## 概述

本文档详细分析 Blender 绘制系统中 `DRW_text_cache_add` 函数的调用堆栈和实现机制。该函数负责将文本添加到渲染缓存中，是文本渲染系统的核心入口点，为 3D 视图中的文本叠加提供高效的缓存管理。

## 调用堆栈流程图

```mermaid
sequenceDiagram
    participant App as 应用层
    participant Overlay as Overlay引擎
    participant Comp as 具体组件
    participant Cache as DRW_text_cache_add
    participant Store as DRWTextStore
    participant Mem as BLI_memiter
    
    App->>Overlay: 渲染3D视图
    Overlay->>Comp: 同步组件数据
    Comp->>Cache: DRW_text_cache_add()
    
    Cache->>Store: 获取文本存储
    Store->>Mem: 创建内存迭代器
    Mem-->>Store: 返回迭代器
    
    Cache->>Cache: 计算字符串长度
    Cache->>Cache: 分配缓存空间
    Cache->>Mem: BLI_memiter_alloc()
    Mem-->>Cache: 返回内存块
    
    Cache->>Cache: 填充ViewCachedString
    Cache->>Cache: 设置位置、颜色、标志
    Cache-->>Comp: 添加完成
    
    Comp-->>Overlay: 组件同步完成
    Overlay-->>App: 渲染准备完成
```

## 类型系统图

```mermaid
classDiagram
    class DRW_text_cache_add {
        +DRWTextStore* dt
        +const float vec[3]
        +const char* str
        +int str_len
        +short xoffs
        +short yoffs
        +int flag
        +const uchar col[4]
        +add_text_to_cache()
    }
    
    class DRWTextStore {
        +BLI_memiter* cache_strings
        +create_text_store()
        +destroy_text_store()
    }
    
    class ViewCachedString {
        +float vec[3]
        +union col
        +short sco[2]
        +short xoffs, yoffs
        +short flag
        +int str_len
        +bool shadow
        +bool align_center
        +char str[0]
    }
    
    class ColorUnion {
        +uchar ub[4]
        +uint pack
    }
    
    class TextFlags {
        +DRW_TEXT_CACHE_GLOBALSPACE
        +DRW_TEXT_CACHE_LOCALCLIP
        +DRW_TEXT_CACHE_STRING_PTR
        +DRW_TEXT_CACHE_ASCII
    }
    
    DRW_text_cache_add --> DRWTextStore
    DRW_text_cache_add --> ViewCachedString
    DRWTextStore --> ViewCachedString
    ViewCachedString --> ColorUnion
    ViewCachedString --> TextFlags
    
    note for DRW_text_cache_add "文本缓存添加接口"
    note for ViewCachedString "变长结构体，字符串存储在末尾"
    note for ColorUnion "支持RGBA和打包格式"
```

## 事件处理图

```mermaid
stateDiagram-v2
    [*] --> 初始化文本存储
    初始化文本存储 --> 检查输入参数
    检查输入参数 --> 计算字符串长度: 有效参数
    检查输入参数 --> 错误处理: 无效参数
    
    计算字符串长度 --> 分配内存块
    分配内存块 --> 填充结构体数据
    填充结构体数据 --> 设置3D位置
    设置3D位置 --> 设置颜色信息
    设置颜色信息 --> 设置偏移量
    设置偏移量 --> 设置标志位
    设置标志位 --> 复制字符串数据
    复制字符串数据 --> 添加到缓存
    添加到缓存 --> [*]
    
    错误处理 --> [*]
    
    note right of 填充结构体数据 : 填充ViewCachedString结构
    note right of 设置标志位 : 设置对齐、阴影等选项
```

## 文本缓存图

```mermaid
flowchart TD
    A[DRW_text_cache_add调用] --> B{输入验证}
    B -->|有效| C[获取DRWTextStore]
    B -->|无效| D[返回错误]
    
    C --> E[检查内存迭代器]
    E -->|未初始化| F[创建内存迭代器]
    E -->|已初始化| G[继续处理]
    F --> G
    
    G --> H[计算字符串长度]
    H --> I[计算总分配大小]
    I --> J[分配内存块]
    
    J --> K[获取ViewCachedString指针]
    K --> L[填充基础数据]
    
    L --> M[设置3D坐标 vec3]
    M --> N[设置颜色信息]
    N --> O[设置偏移量 xoffs/yoffs]
    O --> P[设置标志位 flag]
    P --> Q[设置字符串长度 str_len]
    
    Q --> R{字符串类型}
    R -->|直接字符串| S[复制字符串内容]
    R -->|字符串指针| T[设置指针标志]
    
    S --> U[添加到缓存列表]
    T --> U
    U --> V[返回成功]
    
    D --> W[返回失败]
    
    style A fill:#e3f2fd
    style C fill:#f3e5f5
    style J fill:#fff3e0
    style U fill:#e8f5e8
    style V fill:#c8e6c9
    style W fill:#ffcdd2
```

## 核心实现分析

### 1. 函数签名和参数

```cpp
// source/blender/draw/intern/draw_manager_text.cc:56
void DRW_text_cache_add(DRWTextStore *dt,
                        const float vec[3],
                        const char *str,
                        int str_len,
                        const short xoffs,
                        const short yoffs,
                        const int flag,
                        const uchar col[4])
```

### 2. 内存分配策略

```cpp
// 计算分配大小
size_t alloc_len = (str_len + 1) * sizeof(char);

// 分配变长结构体内存
ViewCachedString *vos = static_cast<ViewCachedString *>(
    BLI_memiter_alloc(dt->cache_strings, sizeof(ViewCachedString) + alloc_len));
```

### 3. 数据填充过程

```cpp
// 填充基础数据
copy_v3_v3(vos->vec, vec);
vos->xoffs = xoffs;
vos->yoffs = yoffs;
vos->flag = flag;
vos->str_len = str_len;

// 设置颜色
if (col) {
  vos->col.ub[0] = col[0];
  vos->col.ub[1] = col[1];
  vos->col.ub[2] = col[2];
  vos->col.ub[3] = col[3];
} else {
  vos->col.pack = 0xFFFFFFFF; // 默认白色
}

// 复制字符串
if (flag & DRW_TEXT_CACHE_STRING_PTR) {
  *((const char **)vos->str) = str;
} else {
  memcpy(vos->str, str, str_len);
  vos->str[str_len] = '\0';
}
```

## 调用关系详解

### 1. 主要调用路径

```mermaid
graph TD
    A[应用层调用] --> B[Overlay组件同步]
    B --> C[具体组件处理]
    
    C --> D[网格组件]
    C --> E[骨骼组件]
    C --> F[曲线组件]
    C --> G[文本组件]
    
    D --> H[DRW_text_cache_add - 边长]
    D --> I[DRW_text_cache_add - 面积]
    E --> J[DRW_text_cache_add - 骨骼名称]
    F --> K[DRW_text_cache_add - 控制点]
    G --> L[DRW_text_cache_add - 文本对象]
    
    H --> M[ViewCachedString创建]
    I --> M
    J --> M
    K --> M
    L --> M
    
    M --> N[添加到DRWTextStore]
    N --> O[等待渲染阶段]
    
    style A fill:#e3f2fd
    style C fill:#f3e5f5
    style M fill:#fff3e0
    style N fill:#e8f5e8
```

### 2. 组件级调用示例

```cpp
// 网格边长测量
if (v3d->overlay.edit_flag & V3D_OVERLAY_EDIT_EDGE_LEN) {
  const float *co = mid_v3_v3v3(v1, v2);
  char numstr[64];
  const size_t numstr_len = SNPRINTF_RLEN(numstr, "%.3f", len_v3v3(v1, v2));
  
  DRW_text_cache_add(dt, co, numstr, numstr_len, 0, edge_tex_sep, txt_flag, col);
}

// 骨骼名称显示
if (arm->drawtype == ARM_ENVELOPE) {
  DRW_text_cache_add(dt, bone->head, bone->name, strlen(bone->name), 
                     0, 12, txt_flag, col);
}
```

## 性能优化机制

### 1. 内存池管理

```mermaid
flowchart LR
    A[BLI_memiter_create] --> B[内存池初始化]
    B --> C[批量分配]
    C --> D[减少malloc调用]
    D --> E[提高缓存局部性]
    E --> F[性能提升]
    
    G[16KB初始大小] --> H[动态扩展]
    H --> I[内存重用]
    I --> J[减少碎片]
    
    style A fill:#e3f2fd
    style C fill:#fff3e0
    style F fill:#c8e6c9
    style J fill:#c8e6c9
```

### 2. 字符串存储优化

```cpp
// 指针模式 - 避免字符串复制
if (flag & DRW_TEXT_CACHE_STRING_PTR) {
  *((const char **)vos->str) = str;
}

// 直接复制模式 - 适用于临时字符串
else {
  memcpy(vos->str, str, str_len);
  vos->str[str_len] = '\0';
}
```

## 应用场景分析

### 1. 3D编辑模式

```mermaid
pie title 文本缓存使用场景分布
    "边长测量" : 35
    "角度显示" : 25
    "物体名称" : 20
    "坐标信息" : 15
    "其他信息" : 5
```

### 2. 具体使用案例

```cpp
// 1. 边长测量文本
DRW_text_cache_add(dt, mid_point, length_str, len_str, 0, 5, 
                   DRW_TEXT_CACHE_ASCII, white_color);

// 2. 角度测量文本  
DRW_text_cache_add(dt, vertex_pos, angle_str, angle_len, 0, -8,
                   DRW_TEXT_CACHE_ASCII, yellow_color);

// 3. 物体名称文本
DRW_text_cache_add(dt, object_center, object->id.name + 2, strlen(object->id.name + 2),
                   0, 15, DRW_TEXT_CACHE_ASCII, name_color);
```

## 错误处理机制

### 1. 输入验证

```cpp
// 参数有效性检查
if (!dt || !vec || !str || str_len <= 0) {
  return;
}

// 内存分配失败检查
ViewCachedString *vos = static_cast<ViewCachedString *>(
    BLI_memiter_alloc(dt->cache_strings, sizeof(ViewCachedString) + alloc_len));
if (!vos) {
  return;
}
```

### 2. 内存保护

```mermaid
stateDiagram-v2
    [*] --> 分配内存
    分配内存 --> 检查分配结果
    检查分配结果 --> 分配成功: 内存有效
    检查分配结果 --> 分配失败: 内存无效
    
    分配成功 --> 填充数据
    填充数据 --> 验证数据完整性
    验证数据完整性 --> 数据有效: 填充正确
    验证数据完整性 --> 数据无效: 填充错误
    
    数据有效 --> 添加到缓存
    数据无效 --> 回滚操作
    添加到缓存 --> [*]
    回滚操作 --> [*]
    分配失败 --> [*]
```

## 调试和分析工具

### 1. 调试宏定义

```cpp
#ifdef DEBUG_TEXT_CACHE
#define DEBUG_TEXT_CACHE_ADD(dt, vec, str, len) \
  printf("Adding text: '%s' at (%.2f, %.2f, %.2f)\n", str, vec[0], vec[1], vec[2])
#else
#define DEBUG_TEXT_CACHE_ADD(dt, vec, str, len)
#endif
```

### 2. 性能监控

```cpp
// 添加性能计时
static double total_add_time = 0.0;
static int add_count = 0;

double start = PIL_check_seconds_timer();
DRW_text_cache_add(dt, vec, str, str_len, xoffs, yoffs, flag, col);
double end = PIL_check_seconds_timer();

total_add_time += (end - start);
add_count++;

if (add_count % 1000 == 0) {
  printf("Average add time: %.4f ms\n", (total_add_time / add_count) * 1000.0);
}
```

## 总结

`DRW_text_cache_add` 函数是 Blender 文本渲染系统的核心组件，通过高效的内存管理和批量处理机制，为 3D 视图中的文本叠加提供性能优化。理解其调用堆栈和实现机制对于开发渲染功能和性能优化具有重要意义。

该函数的设计体现了现代渲染引擎的优化原则：内存池管理、批量处理、状态缓存等，为高性能的文本渲染奠定了坚实基础。