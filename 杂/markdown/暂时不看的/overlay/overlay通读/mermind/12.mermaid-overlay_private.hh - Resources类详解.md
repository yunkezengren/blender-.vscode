# 12. overlay_private.hh - Resources类详解

## 概述

`Resources` 类是Overlay引擎的核心资源管理器，继承自 `select::SelectMap`，负责管理所有渲染资源，包括着色器、帧缓冲、纹理、几何体缓存等。它是连接渲染管线各个组件的中央枢纽。

## Resources类架构图

```mermaid
classDiagram
    class Resources {
        -ShaderModule* shaders
        -Framebuffer overlay_fb
        -Framebuffer overlay_line_fb
        -Framebuffer overlay_in_front_fb
        -TextureRef depth_tx
        -TextureRef color_overlay_tx
        -TextureFromPool line_tx
        -TextureFromPool overlay_tx
        -ShapeCache& shapes
        -UniformBuffer~UniformData~ globals_buf
        -Vector~MovieClip*~ bg_movie_clips
        
        +Resources(selection_type, shapes)
        +~Resources()
        +init(clipping_enabled)
        +acquire(draw_ctx, state)
        +release()
        +begin_sync(clipping_plane_count)
        +update_theme_settings(ctx, state)
        +update_clip_planes(state)
        +object_wire_theme_id(ob_ref, state)
        +object_wire_color(ob_ref, state)
        +is_selection()
    }
    
    class SelectMap {
        <<inherit>>
        +SelectionType selection_type
        +begin_sync()
        +select_append()
        +select_clear()
    }
    
    class ShaderModule {
        <<composition>>
        +StaticShader anti_aliasing
        +StaticShader grid
        +StaticShader mesh_edit_*
    }
    
    class ShapeCache {
        <<reference>>
        +BatchPtr bone_box
        +BatchPtr grid
        +BatchPtr camera_frame
    }
    
    SelectMap <|-- Resources
    Resources *-- ShaderModule
    Resources *-- ShapeCache
```

## 资源管理流程图

```mermaid
flowchart TD
    A[Resources创建] --> B[初始化着色器模块]
    B --> C[预编译关键着色器]
    C --> D[等待渲染帧]
    
    D --> E[begin_sync]
    E --> F[清理MovieClip纹理]
    F --> G[重置选择映射]
    
    G --> H[acquire]
    H --> I[获取视口纹理]
    I --> J[配置X射线深度缓冲]
    J --> K[分配渲染纹理]
    K --> L[设置帧缓冲]
    
    L --> M[渲染阶段]
    M --> N[使用资源进行绘制]
    
    N --> O[release]
    O --> P[释放纹理资源]
    P --> Q[清理MovieClip纹理]
    Q --> R[等待下一帧]
    
    R --> E
```

## GPU资源生命周期图

```mermaid
stateDiagram-v2
    [*] --> Uninitialized
    Uninitialized --> Initialized : init()
    
    Initialized --> Acquired : acquire()
    Acquired --> Ready : 资源分配完成
    
    Ready --> Rendering : 开始渲染
    Rendering --> Ready : 渲染完成
    
    Ready --> Released : release()
    Released --> Initialized : 资源释放
    
    Initialized --> Destroyed : 析构函数
    Destroyed --> [*]
    
    state Rendering {
        [*] --> Bound
        Bound --> Drawing
        Drawing --> Bound
    }
```

## 帧缓冲管理系统

### 帧缓冲类型层次

```mermaid
graph TB
    subgraph "Framebuffer Hierarchy"
        A[Framebuffer Base] --> B[Overlay Framebuffers]
        A --> C[Output Framebuffers]
        A --> D[Render Framebuffers]
        
        B --> B1[overlay_color_only_fb]
        B --> B2[overlay_line_only_fb]
        B --> B3[overlay_fb]
        B --> B4[overlay_line_fb]
        B --> B5[overlay_in_front_fb]
        B --> B6[overlay_line_in_front_fb]
        
        C --> C1[overlay_output_color_only_fb]
        C --> C2[overlay_output_fb]
        
        D --> D1[render_fb]
        D --> D2[render_in_front_fb]
    end
```

### 帧缓冲配置流程

```mermaid
sequenceDiagram
    participant Resources as Resources
    participant Viewport as Viewport Textures
    participant GPU as GPU Driver
    
    Resources->>Viewport: 获取默认纹理
    Viewport-->>Resources: depth_tx, color_tx
    
    alt X射线模式
        Resources->>GPU: 创建xray_depth_tx
        Resources->>GPU: 创建xray_depth_in_front_tx
    else 普通模式
        Resources->>GPU: 检查depth_in_front_tx
        alt 无有效纹理
            Resources->>GPU: 分配depth_in_front_alloc_tx
        end
    end
    
    Resources->>GPU: 分配line_tx
    Resources->>GPU: 分配overlay_tx
    
    Resources->>GPU: 配置overlay_fb
    Resources->>GPU: 配置overlay_line_fb
    Resources->>GPU: 配置overlay_in_front_fb
    Resources->>GPU: 配置overlay_line_in_front_fb
```

## 纹理管理架构

### 纹理分类体系

```mermaid
mindmap
  root((Texture Management))
    输入纹理
      depth_tx
      depth_in_front_tx
      color_overlay_tx
      color_render_tx
    渲染目标纹理
      line_tx
      overlay_tx
      xray_depth_tx
      xray_depth_in_front_tx
    分配纹理
      depth_in_front_alloc_tx
      color_overlay_alloc_tx
      color_render_alloc_tx
    特殊纹理
      dummy_depth_tx
      weight_ramp_tx
    MovieClip纹理
      bg_movie_clips
```

### 纹理生命周期管理

```mermaid
graph LR
    subgraph "Texture Lifecycle"
        A[TextureFromPool] --> B[acquire]
        B --> C[GPU分配]
        C --> D[使用阶段]
        D --> E[release]
        E --> F[返回池中]
        F --> B
    end
    
    subgraph "TextureRef"
        G[TextureRef] --> H[wrap]
        H --> I[引用管理]
        I --> J[自动清理]
    end
    
    subgraph "Special Textures"
        K[dummy_depth_tx] --> L[1px最大深度]
        M[weight_ramp_tx] --> N[权重渐变]
        O[MovieClip纹理] --> P[帧后清理]
    end
```

## 着色器资源管理

### 着色器初始化流程

```mermaid
flowchart TD
    A[Resources::init] --> B[获取ShaderModule]
    B --> C[设置clipping_enabled]
    
    C --> D[异步编译着色器]
    D --> E[抗锯齿着色器]
    D --> F[骨骼着色器]
    D --> G[网格编辑着色器]
    D --> H[额外几何体着色器]
    D --> I[流体着色器]
    D --> J[网格着色器]
    D --> K[轮廓检测着色器]
    D --> L[粒子着色器]
    D --> M[绘制着色器]
    D --> N[UV编辑着色器]
    
    E --> O[编译完成]
    F --> O
    G --> O
    H --> O
    I --> O
    J --> O
    K --> O
    L --> O
    M --> O
    N --> O
```

## 主题和颜色管理

### 颜色主题系统

```mermaid
classDiagram
    class ThemeColorID {
        <<enumeration>>
        TH_WIRE
        TH_WIRE_EDIT
        TH_ACTIVE
        TH_SELECT
        TH_TRANSFORM
        TH_LIGHT
        TH_CAMERA
        TH_SPEAKER
        TH_EMPTY
    }
    
    class Resources {
        +object_wire_theme_id(ob_ref, state)
        +object_wire_color(ob_ref, theme_id)
        +background_blend_color(theme_id)
        +background_color_get(state)
    }
    
    class UniformData {
        +theme colors
        +wire float4
        +wire_edit float4
        +active_object float4
        +object_select float4
    }
    
    Resources --> ThemeColorID : uses
    Resources --> UniformData : updates
```

### 对象线框颜色决策树

```mermaid
flowchart TD
    A[object_wire_theme_id] --> B{编辑模式?}
    B -->|是| C[TH_WIRE_EDIT]
    B -->|否| D{变换中?}
    
    D -->|是且选中| E[TH_TRANSFORM]
    D -->|否| F{选中?}
    
    F -->|是| G{活动对象?}
    G -->|是| H[TH_ACTIVE]
    G -->|否| I[TH_SELECT]
    
    F -->|否| J{对象类型}
    J -->|LAMP| K[TH_LIGHT]
    J -->|SPEAKER| L[TH_SPEAKER]
    J -->|CAMERA| M[TH_CAMERA]
    J -->|EMPTY| N[TH_EMPTY]
    J -->|其他| O[TH_WIRE]
```

## 选择系统集成

### 选择映射机制

```mermaid
graph TB
    subgraph "Selection System Integration"
        A[SelectMap基类] --> B[SelectionType]
        A --> C[begin_sync]
        A --> D[select_append]
        A --> E[select_clear]
        
        F[Resources] --> A
        F --> G[is_selection]
        F --> H[选择ID管理]
        
        I[Primitive Buffers] --> J[select::SelectBuf]
        J --> K[选择缓冲区]
        K --> L[GPU选择渲染]
    end
```

## 内存管理策略

### 资源分配模式

```mermaid
sequenceDiagram
    participant Pool as TexturePool
    participant Resources as Resources
    participant GPU as GPU
    
    Resources->>Pool: request_texture(size, format)
    alt 池中有可用纹理
        Pool-->>Resources: 返回现有纹理
    else 池中无可用纹理
        Pool->>GPU: 创建新纹理
        GPU-->>Pool: 纹理句柄
        Pool-->>Resources: 返回新纹理
    end
    
    Resources->>Resources: 使用纹理渲染
    
    Resources->>Pool: release_texture(texture)
    Pool->>Pool: 标记为可用
```

## 性能优化特性

### 关键优化策略

```mermaid
mindmap
  root((Performance Optimizations))
    资源复用
      TextureFromPool
      帧缓冲复用
      几何体缓存共享
    延迟分配
      按需纹理分配
      条件性帧缓冲创建
      最小化GPU状态切换
    异步编译
      着色器预编译
      后台着色器加载
      非阻塞资源准备
    内存优化
      智能指针管理
      RAII模式
      自动资源清理
    批量操作
      统一资源更新
      批量纹理释放
      集中状态设置
```

## 错误处理和恢复

### 资源管理错误处理

```mermaid
flowchart TD
    A[资源操作] --> B{成功?}
    B -->|是| C[继续执行]
    B -->|否| D{错误类型}
    
    D -->|纹理分配失败| E[使用dummy纹理]
    D -->|帧缓冲创建失败| F[回退到默认帧缓冲]
    D -->|着色器编译失败| G[使用备用着色器]
    D -->|内存不足| H[释放非关键资源]
    
    E --> I[记录警告]
    F --> I
    G --> I
    H --> I
    
    I --> J[继续渲染]
    J --> K[降级模式]
```

## 扩展点和自定义

### 自定义资源扩展

```mermaid
classDiagram
    class Resources {
        <<extendable>>
        +acquire_custom_texture()
        +register_custom_shader()
        +add_custom_framebuffer()
        +custom_resource_cleanup()
    }
    
    class CustomResourceProvider {
        <<interface>>
        +create_custom_resources()
        +update_custom_data()
        +cleanup_custom_resources()
    }
    
    class ResourceExtension {
        <<abstract>>
        +on_resource_init()
        +on_frame_begin()
        +on_frame_end()
    }
    
    Resources --> CustomResourceProvider : uses
    Resources --> ResourceExtension : extends
```

## 调试和诊断

### 资源监控工具

```mermaid
graph TB
    subgraph "Resource Debugging"
        A[Resource Tracker] --> B[纹理使用统计]
        A --> C[帧缓冲状态]
        A --> D[内存使用量]
        A --> E[着色器编译状态]
        
        F[Debug Commands] --> G[dump_resource_info]
        F --> H[validate_resources]
        F --> I[check_leaks]
        
        J[Performance Metrics] --> K[GPU内存占用]
        J --> L[资源分配次数]
        J --> M[帧缓冲切换次数]
    end
```

## 最佳实践指南

### 资源使用建议

1. **及时释放**: 确保在每帧结束时调用 `release()`
2. **避免重复分配**: 利用 `TextureFromPool` 的复用机制
3. **批量更新**: 在 `begin_sync()` 中集中更新状态
4. **错误处理**: 始终检查资源分配的返回值
5. **内存监控**: 定期检查资源使用情况

## 总结

`Resources` 类是Overlay引擎的资源管理核心，具有以下特点：

- **统一管理**: 集中管理所有渲染资源
- **高性能**: 多层次的缓存和优化策略
- **内存安全**: RAII和智能指针保证资源安全
- **可扩展**: 灵活的扩展点支持自定义功能
- **错误恢复**: 完善的错误处理和降级机制

该类为Overlay引擎提供了稳定、高效的资源管理基础，确保渲染管线的顺畅运行。