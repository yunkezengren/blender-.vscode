# draw_pass.hh 详解

## 概述

`draw_pass.hh` 是Blender绘制系统的核心组件，负责管理渲染通道(Pass)的创建、记录和执行。Pass系统是Blender GPU渲染架构的基础，提供了高效的命令录制和执行机制。

## 核心概念

### Pass系统架构

```mermaid
graph TB
    subgraph "Pass系统架构"
        Manager[Manager<br/>渲染管理器]
        
        subgraph "Pass类型"
            PassMain[PassMain<br/>主通道]
            PassSimple[PassSimple<br/>简单通道]
            PassSortable[PassSortable<br/>可排序通道]
        end
        
        subgraph "命令缓冲区"
            DrawCommandBuf[DrawCommandBuf<br/>绘制命令缓冲区]
            DrawMultiBuf[DrawMultiBuf<br/>多重绘制缓冲区]
        end
        
        subgraph "子通道系统"
            SubPass[SubPass<br/>子通道]
            SubPassVector[SubPassVector<br/>子通道容器]
        end
    end
    
    Manager --> PassMain
    Manager --> PassSimple
    Manager --> PassSortable
    
    PassMain --> DrawCommandBuf
    PassSimple --> DrawCommandBuf
    PassSortable --> DrawMultiBuf
    
    PassMain --> SubPass
    PassSimple --> SubPass
    PassSortable --> SubPass
    
    SubPass --> SubPassVector
```

*图1: Pass系统整体架构，展示了不同类型的Pass及其相互关系*

### Pass类型关系图

```mermaid
classDiagram
    class PassBase {
        +Vector~Header~ headers_
        +Vector~Undetermined~ commands_
        +DrawCommandBufType draw_commands_buf_
        +SubPassVector sub_passes_
        +gpu::Shader* shader_
        +init()
        +is_empty()
        +sub(name)
        +state_set(state)
        +draw(batch, ...)
        +dispatch(...)
        +clear_color(color)
    }
    
    class Pass {
        +SubPassVector sub_passes_main_
        +DrawCommandBufType draw_commands_buf_main_
        +init()
    }
    
    class PassMain {
        +init()
    }
    
    class PassSimple {
        +init()
    }
    
    class PassSortable {
        +Vector~float~ sorting_values_
        +bool sorted_
        +sub(name, sorting_value)
        +sort()
    }
    
    PassBase <|-- Pass
    Pass <|-- PassMain
    Pass <|-- PassSimple
    PassMain <|-- PassSortable
    
    note for PassBase "基础Pass类，包含所有核心功能"
    note for Pass "模板类，支持不同命令缓冲区类型"
    note for PassMain "优化用于大量绘制调用的主通道"
    note for PassSimple "简单通道，无批处理优化"
    note for PassSortable "支持排序的通道，用于透明对象渲染"
```

*图2: Pass类型继承关系图*

## 渲染流程

### 渲染流程图

```mermaid
flowchart TD
    Start([开始渲染]) --> CreatePass[创建Pass]
    CreatePass --> InitPass[初始化Pass]
    InitPass --> RecordCommands[记录命令]
    
    subgraph "命令记录阶段"
        RecordCommands --> SetState[设置渲染状态]
        SetState --> BindShader[绑定着色器]
        BindShader --> BindResources[绑定资源]
        BindResources --> RecordDraw[记录绘制命令]
        RecordDraw --> RecordDispatch[记录计算命令]
        RecordDispatch --> RecordClear[记录清除命令]
    end
    
    RecordClear --> SubmitPass[提交Pass]
    
    subgraph "命令执行阶段"
        SubmitPass --> WarmShader[预热着色器]
        WarmShader --> ExecuteCommands[执行命令]
        ExecuteCommands --> SubPasses[处理子通道]
        SubPasses --> GPUCommands[执行GPU命令]
    end
    
    GPUCommands --> End([结束])
    
    style RecordCommands fill:#e1f5fe
    style ExecuteCommands fill:#f3e5f5
```

*图3: Pass渲染完整流程，从创建到执行*

### 命令缓冲区管理

```mermaid
sequenceDiagram
    participant App as 应用程序
    participant Pass as Pass
    participant CmdBuf as 命令缓冲区
    participant GPU as GPU
    
    App->>Pass: 创建Pass
    Pass->>CmdBuf: 初始化命令缓冲区
    
    loop 记录命令
        App->>Pass: 添加绘制命令
        Pass->>CmdBuf: append_draw()
        Note over CmdBuf: 存储命令到缓冲区
    end
    
    App->>Pass: 提交Pass
    Pass->>CmdBuf: generate_commands()
    Note over CmdBuf: 生成最终命令序列
    
    Pass->>GPU: 执行命令
    loop 执行循环
        GPU->>GPU: 处理每个命令
        GPU->>GPU: 状态变更检测
        GPU->>GPU: 优化批处理
    end
    
    GPU-->>Pass: 执行完成
    Pass-->>App: 渲染完成
```

*图4: 命令缓冲区管理时序图*

## 核心组件详解

### PassBase模板类

`PassBase`是所有Pass类型的基础模板类，提供了以下核心功能：

#### 命令管理
- **headers_**: 存储命令头部的向量，按类型分组
- **commands_**: 存储实际命令数据的向量
- **draw_commands_buf_**: 绘制命令缓冲区引用
- **sub_passes_**: 子通道容器

#### 状态管理
- **shader_**: 当前绑定的着色器
- **manager_fingerprint_**: 管理器指纹，用于检测变更
- **view_fingerprint_**: 视图指纹，用于检测变更

### SubPassVector容器

`SubPassVector`是一个特殊的容器，用于高效管理子通道：

```mermaid
graph LR
    subgraph "SubPassVector结构"
        %% 修复1: 标签中有特殊字符建议加引号
        Blocks["Vector&lt;unique_ptr&gt; blocks_"]
        
        subgraph "Block 0"
            B0["Vector&lt;T&gt; block_size=16"]
        end
        
        subgraph "Block 1"
            B1["Vector&lt;T&gt; block_size=16"]
        end
        
        subgraph "Block N"
            BN["Vector&lt;T&gt; block_size=16"]
        end
    end
    
    Blocks --> B0
    Blocks --> B1
    Blocks --> BN
    
    %% 修复2: 流程图没有 "note for"，
    %% 这里使用 "折角矩形" 形状 [/.../] 并通过虚线 -.- 连接来模拟注释
    N1("块指针向量") -.- Blocks
    N2{"第一个数据块"} -.- B0
    N3(("第二个数据块")) -.- B1
    N4[/"第N个数据块"/] -.- BN
```

*图5: SubPassVector内存布局*

### 命令类型系统

Pass系统支持多种命令类型：

```mermaid
graph TD
    Commands[命令类型]
    
    subgraph "绘制命令"
        Draw[Draw<br/>单次绘制]
        DrawMulti[DrawMulti<br/>多重绘制]
        DrawIndirect[DrawIndirect<br/>间接绘制]
    end
    
    subgraph "计算命令"
        Dispatch[Dispatch<br/>计算调度]
        DispatchIndirect[DispatchIndirect<br/>间接计算]
    end
    
    subgraph "状态命令"
        StateSet[StateSet<br/>状态设置]
        StencilSet[StencilSet<br/>模板设置]
        ShaderBind[ShaderBind<br/>着色器绑定]
    end
    
    subgraph "资源命令"
        ResourceBind[ResourceBind<br/>资源绑定]
        PushConstant[PushConstant<br/>常量推送]
        SpecializeConstant[SpecializeConstant<br/>特化常量]
    end
    
    subgraph "帧缓冲命令"
        FramebufferBind[FramebufferBind<br/>帧缓冲绑定]
        SubPassTransition[SubPassTransition<br/>子通道转换]
        Clear[Clear<br/>清除操作]
        ClearMulti[ClearMulti<br/>多重清除]
    end
    
    subgraph "同步命令"
        Barrier[Barrier<br/>内存屏障]
    end
    
    Commands --> Draw
    Commands --> DrawMulti
    Commands --> DrawIndirect
    Commands --> Dispatch
    Commands --> DispatchIndirect
    Commands --> StateSet
    Commands --> StencilSet
    Commands --> ShaderBind
    Commands --> ResourceBind
    Commands --> PushConstant
    Commands --> SpecializeConstant
    Commands --> FramebufferBind
    Commands --> SubPassTransition
    Commands --> Clear
    Commands --> ClearMulti
    Commands --> Barrier
```

*图6: Pass系统支持的命令类型*

## 性能优化特性

### 批处理优化

PassMain类型支持自动批处理优化：

```mermaid
flowchart LR
    subgraph "批处理流程"
        Input[输入绘制命令] --> Group[按状态分组]
        Group --> Merge[合并相同状态]
        Merge --> Batch[创建批处理]
        Batch --> GPU[GPU执行]
    end
    
    subgraph "优化策略"
        StateSort[状态排序]
        InstanceMerge[实例合并]
        ResourceReuse[资源重用]
    end
    
    Group --> StateSort
    Merge --> InstanceMerge
    Batch --> ResourceReuse
```

*图7: 批处理优化流程*

### 内存管理

Pass系统采用高效的内存管理策略：

1. **预分配**: 向量容器预分配内存空间
2. **块分配**: SubPassVector使用块分配策略
3. **引用计数**: 资源使用引用计数管理
4. **延迟清理**: 延迟清理未使用的内存

## 使用示例

### 基本使用模式

```cpp
// 创建主Pass
PassMain main_pass("MyPass");
main_pass.init();

// 设置渲染状态
main_pass.state_set(DRW_STATE_WRITE_COLOR | DRW_STATE_WRITE_DEPTH);
main_pass.shader_set(shader);

// 绑定资源
main_pass.bind_texture("colorTex", texture);
main_pass.bind_ubo("uniforms", ubo);

// 添加绘制命令
main_pass.draw(batch, instance_count);

// 提交执行
main_pass.submit(state);
```

### 子通道使用

```cpp
// 创建子通道
auto& sub_pass = main_pass.sub("SubPass1");
sub_pass.state_set(DRW_STATE_BLEND_ALPHA);
sub_pass.draw(transparent_batch, instance_count);

// 子通道会自动插入到父Pass的适当位置
```

## 总结

`draw_pass.hh` 提供了一个强大而灵活的渲染通道管理系统，具有以下特点：

1. **模块化设计**: 支持多种Pass类型，满足不同渲染需求
2. **高效批处理**: 自动优化绘制调用，减少GPU状态切换
3. **灵活的命令系统**: 支持丰富的GPU命令类型
4. **内存优化**: 采用高效的内存管理策略
5. **易于扩展**: 模板化设计便于添加新功能

这个系统是Blender现代渲染架构的核心，为高性能实时渲染提供了坚实的基础。