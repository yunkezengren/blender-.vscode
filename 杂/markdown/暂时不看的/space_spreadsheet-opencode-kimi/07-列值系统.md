# Spreadsheet 列值系统

> 📍 目标：理解列值抽象、类型系统和数据显示

---

## 1. ColumnValues 类设计

### 1.1 类结构

```mermaid
classDiagram
    class ColumnValues {
        -string name_
        -string description_
        -GVArray data_
        -ColumnValueDisplayHint display_hint_
        +ColumnValues(name, data, description, hint)
        +~ColumnValues() virtual
        +type() eSpreadsheetColumnValueType
        +name() StringRefNull
        +size() int
        +data() GVArray&
        +display_hint() ColumnValueDisplayHint
        +fit_column_width_px() float
        +fit_column_values_width_px() float
    }

    class GVArray {
        <<Generic Virtual Array>>
        -const CPPType* type_
        +type() CPPType
        +size() int
        +is_empty() bool
        +get(index) T
    }

    class ColumnValueDisplayHint {
        <<enum>>
        None
        Bytes
    }

    class SpreadsheetExtraColumnValues {
        <<子类>>
        用于特殊列
    }

    ColumnValues --> GVArray
    ColumnValues --> ColumnValueDisplayHint
    ColumnValues <|-- SpreadsheetExtraColumnValues
```

### 1.2 设计意图

```mermaid
flowchart TB
    subgraph 设计目标
        A[类型擦除] --> B[统一接口处理不同类型]
        C[延迟计算] --> D[按需计算而非立即]
        E[内存高效] --> F[GVArray避免拷贝]
    end
```

---

## 2. GVArray 详解

### 2.1 GVArray是什么

```mermaid
flowchart TB
    subgraph GVArray概念
        A[Generic Virtual Array] --> B[泛型虚拟数组]
        B --> C[类型擦除]
        C --> D[运行时类型信息]
    end

    subgraph 实现方式
        E[虚函数表] --> F[多态访问]
        G[类型指针] --> H[运行时类型检查]
    end
```

### 2.2 GVArray使用场景

```mermaid
flowchart TB
    subgraph GVArray应用场景
        A[几何属性] --> B[顶点位置float3]
        C[实例数据] --> D[实例引用InstanceReference]
        E[自定义属性] --> F[任意类型]
        G[计算结果] --> H[节点输出]
    end
```

### 2.3 代码示例

```cpp
// 创建GVArray
std::unique_ptr<ColumnValues> GeometryDataSource::get_column_values(
    const SpreadsheetColumnID &column_id) const
{
    // 获取属性读取器
    const std::optional<bke::AttributeReader> reader =
        attributes->lookup(column_id.name);

    if (!reader) {
        return {};  // 属性不存在
    }

    // 转换为GVArray
    GVArray gvarray = *reader;

    // 创建ColumnValues
    return std::make_unique<ColumnValues>(
        column_id.name,
        std::move(gvarray)
    );
}
```

---

## 3. 类型映射系统

### 3.1 C++类型到列类型

```mermaid
flowchart TB
    subgraph 类型映射
        A[bool] --> B[SPREADSHEET_VALUE_TYPE_BOOL]
        C[int8] --> D[SPREADSHEET_VALUE_TYPE_INT8]
        E[int32] --> F[SPREADSHEET_VALUE_TYPE_INT32]
        G[int64] --> H[SPREADSHEET_VALUE_TYPE_INT64]
        I[float] --> J[SPREADSHEET_VALUE_TYPE_FLOAT]
        K[float2] --> L[SPREADSHEET_VALUE_TYPE_FLOAT2]
        M[float3] --> N[SPREADSHEET_VALUE_TYPE_FLOAT3]
        O[float4] --> P[SPREADSHEET_VALUE_TYPE_FLOAT4]
        Q[ColorGeometry4f] --> R[SPREADSHEET_VALUE_TYPE_COLOR]
        S[string] --> T[SPREADSHEET_VALUE_TYPE_STRING]
        U[InstanceReference] --> V[SPREADSHEET_VALUE_TYPE_INSTANCES]
        W[Quaternion] --> X[SPREADSHEET_VALUE_TYPE_QUATERNION]
        Y[float4x4] --> Z[SPREADSHEET_VALUE_TYPE_FLOAT4X4]
    end
```

### 3.2 映射实现

```cpp
// spreadsheet_column.cc
eSpreadsheetColumnValueType cpp_type_to_column_type(const CPPType &type) {
    if (type.is<bool>()) {
        return SPREADSHEET_VALUE_TYPE_BOOL;
    }
    if (type.is<int8_t>()) {
        return SPREADSHEET_VALUE_TYPE_INT8;
    }
    if (type.is<int>()) {
        return SPREADSHEET_VALUE_TYPE_INT32;
    }
    if (type.is<int64_t>()) {
        return SPREADSHEET_VALUE_TYPE_INT64;
    }
    if (type.is_any<short2, int2>()) {
        return SPREADSHEET_VALUE_TYPE_INT32_2D;
    }
    if (type.is_any<int3>()) {
        return SPREADSHEET_VALUE_TYPE_INT32_3D;
    }
    if (type.is<float>()) {
        return SPREADSHEET_VALUE_TYPE_FLOAT;
    }
    if (type.is<float2>()) {
        return SPREADSHEET_VALUE_TYPE_FLOAT2;
    }
    if (type.is<float3>()) {
        return SPREADSHEET_VALUE_TYPE_FLOAT3;
    }
    if (type.is<float4>()) {
        return SPREADSHEET_VALUE_TYPE_FLOAT4;
    }
    if (type.is<ColorGeometry4f>()) {
        return SPREADSHEET_VALUE_TYPE_COLOR;
    }
    if (type.is<std::string>() || type.is<MStringProperty>()) {
        return SPREADSHEET_VALUE_TYPE_STRING;
    }
    if (type.is<bke::InstanceReference>()) {
        return SPREADSHEET_VALUE_TYPE_INSTANCES;
    }
    if (type.is<ColorGeometry4b>()) {
        return SPREADSHEET_VALUE_TYPE_BYTE_COLOR;
    }
    if (type.is<math::Quaternion>()) {
        return SPREADSHEET_VALUE_TYPE_QUATERNION;
    }
    if (type.is<float4x4>()) {
        return SPREADSHEET_VALUE_TYPE_FLOAT4X4;
    }
    if (type.is<nodes::BundleItemValue>()) {
        return SPREADSHEET_VALUE_TYPE_BUNDLE_ITEM;
    }
    return SPREADSHEET_VALUE_TYPE_UNKNOWN;
}
```

---

## 4. 列宽自适应

### 4.1 自适应算法

```mermaid
flowchart TB
    subgraph 自适应流程
        A[开始] --> B[计算列名宽度]
        B --> C{需要采样?}
        C -->|是| D[采样max_sample_size行]
        C -->|否| E[遍历所有行]
        D --> F[计算每行值宽度]
        E --> F
        F --> G[取最大值]
        G --> H[加上padding]
        H --> I[返回宽度]
    end
```

### 4.2 代码实现

```cpp
float ColumnValues::fit_column_width_px(
    const std::optional<int64_t> &max_sample_size) const
{
    // 1. 计算列名宽度
    float name_width = 0;
    if (!name_.empty()) {
        name_width = BLF_width(font_id, name_.c_str(), name_.size());
    }

    // 2. 计算值宽度
    float values_width = this->fit_column_values_width_px(max_sample_size);

    // 3. 取最大值并加边距
    float width = std::max(name_width, values_width);
    width += CELL_RIGHT_PADDING * 2;

    return width;
}

float ColumnValues::fit_column_values_width_px(
    const std::optional<int64_t> &max_sample_size) const
{
    if (data_.is_empty()) {
        return 0;
    }

    // 采样大小
    const int64_t size = data_.size();
    const int64_t sample_size = max_sample_size ?
        std::min(*max_sample_size, size) : size;

    float max_width = 0;

    // 遍历采样
    for (int64_t i = 0; i < sample_size; i++) {
        float value_width = this->compute_value_width(i);
        max_width = std::max(max_width, value_width);
    }

    // 如果是采样，确保最小宽度
    if (max_sample_size && sample_size < size) {
        max_width = std::max(max_width, MIN_COLUMN_WIDTH);
    }

    return max_width;
}
```

---

## 5. 特殊列处理

### 5.1 特殊列识别

```mermaid
flowchart TB
    subgraph 特殊列判断
        A[foreach_default_column_ids] --> B{is_extra?}
        B -->|true| C[特殊列]
        B -->|false| D[普通属性列]
        C --> E[索引列]
        C --> F[名称列]
        C --> G[ID列]
    end
```

### 5.2 特殊列实现

```cpp
// GeometryDataSource中的特殊列处理
void GeometryDataSource::foreach_default_column_ids(
    FunctionRef<void(const SpreadsheetColumnID &, bool is_extra)> fn) const
{
    // 1. 特殊列（is_extra = true）
    SpreadsheetColumnID index_column;
    STRNCPY(index_column.name, "index");
    fn(index_column, true);  // 标记为extra

    SpreadsheetColumnID name_column;
    STRNCPY(name_column.name, "name");
    fn(name_column, true);   // 标记为extra

    // 2. 普通属性列（is_extra = false）
    attributes->foreach_attribute([&](const bke::AttributeIDRef &id, ...) {
        SpreadsheetColumnID column_id;
        column_id.name = id.name();
        fn(column_id, false);
    });
}
```

### 5.3 特殊列值生成

```mermaid
flowchart TB
    subgraph 特殊列值生成
        A[get_column_values] --> B{列名}
        B -->|index| C[生成索引GVArray]
        B -->|name| D[从实例引用获取名称]
        B -->|其他| E[从属性获取]
    end
```

---

## 6. 显示提示系统

### 6.1 ColumnValueDisplayHint

```mermaid
classDiagram
    class ColumnValueDisplayHint {
        <<enum>>
        None
        Bytes
    }

    class ColumnValues {
        +display_hint() ColumnValueDisplayHint
    }
```

### 6.2 应用场景

| Hint值 | 作用 | 示例 |
|-------|------|------|
| None | 默认显示 | 大多数类型 |
| Bytes | 以字节格式显示 | 内存大小、文件大小 |

### 6.3 代码实现

```cpp
// 设置显示提示
ColumnValues values("memory", data, "", ColumnValueDisplayHint::Bytes);

// 绘制时使用
if (column_values.display_hint() == ColumnValueDisplayHint::Bytes) {
    draw_bytes_cell(params, value);
} else {
    draw_default_cell(params, value);
}
```

---

## 7. 性能考虑

### 7.1 惰性计算

```mermaid
flowchart TB
    subgraph 惰性计算
        A[DataSource创建] --> B[不立即构建列值]
        C[UI需要时] --> D[调用get_column_values]
        D --> E[按需计算]
        E --> F[缓存结果]
    end
```

### 7.2 采样策略

```cpp
// 大数据集优化
void draw_spreadsheet(...) {
    // 绘制时只采样计算列宽
    const std::optional<int64_t> max_sample_size = 100;

    for (const auto &column : columns) {
        float width = column.values->fit_column_width_px(max_sample_size);
        // ...
    }
}
```

### 7.3 内存管理

| 技术 | 说明 |
|-----|------|
| unique_ptr | 自动管理ColumnValues生命周期 |
| ResourceScope | 管理临时计算内存 |
| move语义 | 避免不必要拷贝 |

---

## 8. 扩展指南

### 8.1 添加新列类型步骤

```mermaid
flowchart TB
    subgraph 扩展步骤
        A[1. DNA_space_types.h] --> B[在enum添加类型]
        C[2. spreadsheet_column.cc] --> D[添加cpp_type_to_column_type映射]
        E[3. spreadsheet_draw.cc] --> F[实现draw函数]
        G[4. 测试] --> H[验证显示正确]
    end
```

### 8.2 扩展示例

```cpp
// 步骤1: 在enum中添加
// DNA_space_types.h
typedef enum eSpreadsheetColumnValueType {
    // ... 现有类型
    SPREADSHEET_VALUE_TYPE_NEW_TYPE,
} eSpreadsheetColumnValueType;

// 步骤2: 添加类型映射
// spreadsheet_column.cc
eSpreadsheetColumnValueType cpp_type_to_column_type(const CPPType &type) {
    // ... 现有映射
    if (type.is<NewType>()) {
        return SPREADSHEET_VALUE_TYPE_NEW_TYPE;
    }
    return SPREADSHEET_VALUE_TYPE_UNKNOWN;
}

// 步骤3: 实现绘制函数
// spreadsheet_draw.cc
void draw_content_cell(int row, int col, const CellDrawParams &params) {
    switch (column_type) {
        // ... 现有case
        case SPREADSHEET_VALUE_TYPE_NEW_TYPE:
            draw_new_type_cell(params, data.get<NewType>(row));
            break;
    }
}
```

---

## 9. 关键源码分析

### 9.1 ColumnValues构造函数

```cpp
ColumnValues::ColumnValues(std::string name,
                           GVArray data,
                           std::string description,
                           const ColumnValueDisplayHint display_hint)
    : name_(std::move(name)),
      description_(std::move(description)),
      data_(std::move(data)),
      display_hint_(display_hint)
{
    // 断言：数组不能为空
    BLI_assert(data_);
}
```

### 9.2 关键方法

```cpp
// 获取类型
eSpreadsheetColumnValueType type() const {
    return cpp_type_to_column_type(data_.type());
}

// 获取名称
StringRefNull name() const {
    return name_;
}

// 获取大小
int size() const {
    return data_.size();
}

// 获取数据引用
const GVArray &data() const {
    return data_;
}
```

---

*文档创建: 2025年*
*基于 spreadsheet_column_values.hh, spreadsheet_column.cc 分析*
