# Spreadsheet 列和表格系统

> 📍 目标：理解列管理、表格状态存储和Blend文件持久化

---

## 1. SpreadsheetColumn 类层次

### 1.1 三层结构

```mermaid
classDiagram
    class SpreadsheetColumnID {
        +char name[64]
        --
        列的唯一标识符
        存储列名
    }

    class SpreadsheetColumn {
        +SpreadsheetColumnID* id
        +SpreadsheetColumnRuntime* runtime
        +float width
        +int flag
        --
        列的持久化数据
        保存用户配置
    }

    class SpreadsheetColumnRuntime {
        +int left_x
        +int right_x
        --
        运行时计算的位置
        不保存到文件
    }

    SpreadsheetColumn --> SpreadsheetColumnID : id
    SpreadsheetColumn --> SpreadsheetColumnRuntime : runtime
```

### 1.2 设计意图

```mermaid
flowchart TB
    subgraph 分离关注点
        A[SpreadsheetColumnID] -->|唯一标识| B[持久化数据]
        C[SpreadsheetColumn] -->|用户配置| B
        D[SpreadsheetColumnRuntime] -->|计算属性| E[运行时数据]
    end

    subgraph 生命周期
        B -->|保存到.blend| F[磁盘文件]
        E -->|每次打开重新计算| G[内存中]
    end
```

---

## 2. 列值系统 (ColumnValues)

### 2.1 类设计

```mermaid
classDiagram
    class ColumnValues {
        -string name_
        -string description_
        -GVArray data_
        -ColumnValueDisplayHint display_hint_
        --
        +ColumnValues(name, data, description, hint)
        +~ColumnValues() virtual
        +type() eSpreadsheetColumnValueType
        +name() StringRefNull
        +size() int
        +data() GVArray&
        +fit_column_width_px() float
        +fit_column_values_width_px() float
    }

    class GVArray {
        <<Generic Virtual Array>>
        +type() CPPType
        +size() int
        +get(index) T
        --
        泛型虚拟数组
        类型擦除但保留类型信息
    }

    class ColumnValueDisplayHint {
        <<enum>>
        None
        Bytes
    }

    ColumnValues --> GVArray : data_
    ColumnValues --> ColumnValueDisplayHint : display_hint_
```

### 2.2 数据流

```mermaid
sequenceDiagram
    participant DS as DataSource
    participant CV as ColumnValues
    participant GVA as GVArray
    participant UI as UI绘制

    DS->>CV: new ColumnValues(name, gvarray)
    CV->>GVA: 保存数据引用
    CV-->>DS: return unique_ptr
    DS-->>UI: 传递ColumnValues
    UI->>CV: type()
    CV->>GVA: type()
    GVA-->>CV: CPPType
    CV-->>UI: eSpreadsheetColumnValueType
    UI->>CV: data()
    CV-->>UI: 返回GVArray
    UI->>GVA: get(index)
    GVA-->>UI: 返回值
```

---

## 3. SpreadsheetTable 表格管理

### 3.1 数据结构

```mermaid
classDiagram
    class SpreadsheetTable {
        +SpreadsheetTableID* id
        +SpreadsheetColumn** columns
        +int num_columns
        +int flag
        --
        表格的完整状态
        每列可独立配置
    }

    class SpreadsheetTableID {
        <<抽象>>
        +int type
    }

    class SpreadsheetTableIDGeometry {
        +SpreadsheetTableID base
        +int object_session_uid
        +uint8_t geometry_component_type
        +uint8_t attribute_domain
        +int layer_index
        --
        几何表格ID
    }

    class SpreadsheetTableIDClosure {
        +SpreadsheetTableID base
        +int closure_session_uid
        +SpreadsheetClosureInputOutput in_out
        --
        闭包表格ID
    }

    class SpaceSpreadsheet {
        +ListBase tables
        +SpreadsheetTableID* active_table_id
        --
        Space可包含多个表格
    }

    SpreadsheetTable --> SpreadsheetTableID : id
    SpreadsheetTableID <|-- SpreadsheetTableIDGeometry
    SpreadsheetTableID <|-- SpreadsheetTableIDClosure
    SpreadsheetTableID <|-- SpreadsheetTableIDBundle
    SpreadsheetTableID <|-- SpreadsheetTableIDList
    SpaceSpreadsheet --> SpreadsheetTable : tables
    SpaceSpreadsheet --> SpreadsheetTableID : active_table_id
```

### 3.2 表格查找机制

```mermaid
flowchart TB
    subgraph 表格匹配
        A[当前数据状态] --> B[生成SpreadsheetTableID]
        B --> C[在Space中查找]
        C --> D{找到?}
        D -->|是| E[返回现有表格]
        D -->|否| F[创建新表格]
        F --> G[添加到Space]
    end
```

**代码对应**:
```cpp
// 匹配逻辑在 spreadsheet_table_id_match()
bool spreadsheet_table_id_match(const SpreadsheetTableID &a, const SpreadsheetTableID &b) {
    if (a.type != b.type) return false;
    switch (a.type) {
        case SPREADSHEET_TABLE_ID_TYPE_GEOMETRY:
            // 比较几何组件类型、属性域等
            // 忽略迭代索引
            break;
        // ...
    }
}
```

---

## 4. Blend文件持久化

### 4.1 写入流程

```mermaid
flowchart TB
    subgraph Blend写入
        A[blend_write] --> B[遍历所有tables]
        B --> C[写入SpreadsheetTableID]
        C --> D[根据type写入特定数据]
        D --> E[写入SpreadsheetTable]
        E --> F[遍历所有columns]
        F --> G[写入SpreadsheetColumn]
        G --> H[写入SpreadsheetColumnID]
    end
```

### 4.2 读取流程

```mermaid
flowchart TB
    subgraph Blend读取
        A[blend_read] --> B[读取SpreadsheetTableID]
        B --> C[根据type读取特定数据]
        C --> D[读取SpreadsheetTable]
        D --> E[读取num_columns]
        E --> F[分配columns数组]
        F --> G[循环读取每个column]
        G --> H[读取SpreadsheetColumnID]
    end
```

### 4.3 DNA结构版本控制

```mermaid
flowchart LR
    subgraph DNA版本管理
        A[SDNA] -->|定义结构| B[SDNAindex]
        B --> C[字段偏移量]
        C --> D[兼容旧版本]
    end
```

---

## 5. 内存管理

### 5.1 分配策略

```mermaid
flowchart TB
    subgraph 内存分配
        A[spreadsheet_column_id_new] -->|MEM_new| B[堆分配]
        C[spreadsheet_column_new] -->|MEM_new| D[堆分配]
        E[spreadsheet_table_new] -->|MEM_new| F[堆分配]
    end

    subgraph 数组分配
        G[SpreadsheetTable.columns] -->|MEM_malloc_array| H[指针数组]
    end
```

### 5.2 释放策略

```mermaid
flowchart TB
    subgraph 内存释放
        A[spreadsheet_column_id_free] --> B[MEM_free]
        C[spreadsheet_column_free] --> D[释放id和runtime]
        E[spreadsheet_table_free] --> F[释放所有列]
        F --> G[释放id]
    end
```

### 5.3 拷贝语义

```mermaid
flowchart TB
    subgraph 深拷贝
        A[spreadsheet_column_id_copy] --> B[新分配内存]
        B --> C[复制name字段]
        D[spreadsheet_column_copy] --> E[复制所有字段]
        E --> F[深拷贝id]
        G[spreadsheet_table_copy] --> H[深拷贝所有列]
    end
```

---

## 6. 行筛选系统

### 6.1 筛选器结构

```mermaid
classDiagram
    class SpreadsheetRowFilter {
        +int flag
        +char column_name[64]
        +int data_type
        +int operation
        +Value value
    }

    class Value {
        <<union>>
        +int8_t int8_value
        +int32_t int32_value
        +int64_t int64_value
        +float float_value
        +char string_value[64]
    }

    class IndexMask {
        <<返回结果>>
        +IndexRange ranges[]
        +bool[] indices
    }

    SpreadsheetRowFilter --> Value : value
```

### 6.2 筛选流程

```mermaid
flowchart TB
    A[原始数据] --> B[应用行筛选器]
    B --> C[遍历所有筛选器]
    C --> D{AND/OR模式}
    D -->|AND| E[取交集]
    D -->|OR| F[取并集]
    E --> G[生成IndexMask]
    F --> G
    G --> H[仅显示匹配行]
```

### 6.3 筛选操作

| 操作 | 含义 | 适用类型 |
|-----|------|---------|
| EQUAL | 等于 | 所有类型 |
| NOT_EQUAL | 不等于 | 所有类型 |
| GREATER_THAN | 大于 | 数值类型 |
| LESS_THAN | 小于 | 数值类型 |
| CONTAINS | 包含 | 字符串 |
| STARTS_WITH | 开头匹配 | 字符串 |
| ENDS_WITH | 结尾匹配 | 字符串 |
| IS_EMPTY | 为空 | 字符串 |
| IS_NOT_EMPTY | 非空 | 字符串 |

---

## 7. 列宽调整系统

### 7.1 列宽计算

```mermaid
flowchart TB
    subgraph 列宽来源
        A[默认值] --> B[5 * UI_UNIT_X]
        C[用户手动调整] --> D[column->width]
        E[自适应宽度] --> F[fit_column_width_px()]
    end

    subgraph 计算逻辑
        F --> G[采样部分行计算文本宽度]
        G --> H[取列名和值的最大宽度]
        H --> I[加上边距]
    end
```

### 7.2 自适应宽度实现

```cpp
float ColumnValues::fit_column_width_px(
    const std::optional<int64_t>& max_sample_size) const
{
    // 1. 计算列名宽度
    float width = BLF_width(font_id, name_.c_str(), name_.size());

    // 2. 计算值宽度（可选采样）
    if (max_sample_size) {
        // 只采样前N行
        for (int i = 0; i < std::min(*max_sample_size, size()); i++) {
            width = std::max(width, get_cell_width(i));
        }
        // 确保最小宽度
        width = std::max(width, minimum_column_width);
    } else {
        // 遍历所有行
        for (int i = 0; i < size(); i++) {
            width = std::max(width, get_cell_width(i));
        }
    }

    return width + padding;
}
```

---

## 8. 表格状态管理

### 8.1 状态标志

```mermaid
flowchart TB
    subgraph SpreadsheetTable.flags
        A[SPREADSHEET_TABLE_FLAG_MANUALLY_EDITED] --> B[用户手动编辑过列顺序/宽度]
    end

    subgraph SpreadsheetColumn.flags
        C[SPREADSHEET_COLUMN_FLAG_UNAVAILABLE] --> D[该列在当前数据中不可用]
    end

    subgraph SpaceSpreadsheet.filter_flag
        E[SPREADSHEET_FILTER_ENABLE] --> F[启用行筛选]
        G[SPREADSHEET_FILTER_SHOW_SELECTED] --> H[仅显示选中项]
    end
```

### 8.2 状态转换

```mermaid
stateDiagram-v2
    [*] --> 初始状态
    初始状态 --> 自动列宽: 无用户数据
    自动列宽 --> 手动调整列宽: resize_column操作
    手动调整列宽 --> 手动调整列宽: resize_column操作
    手动调整列宽 --> [*]: 保存到Blend
    手动调整列宽 --> 自适应列宽: fit_column操作
    自适应列宽 --> 手动调整列宽: 用户调整
```

---

## 9. 实例ID处理

### 9.1 实例导航

```mermaid
flowchart TB
    subgraph 实例ID层级
        A[SpreadsheetInstanceID] --> B[InstanceReference]
        B --> C[GeometrySet]
        C --> D[Component]
        D --> E[Attributes]
    end

    subgraph 递归展示
        F[选中实例] --> G[将该实例作为根]
        G --> H[展示其属性]
    end
```

---

## 10. 关键函数清单

### 10.1 列管理函数

| 函数 | 文件 | 职责 |
|------|------|------|
| `spreadsheet_column_id_new()` | column.cc | 创建列ID |
| `spreadsheet_column_id_copy()` | column.cc | 复制列ID |
| `spreadsheet_column_id_free()` | column.cc | 释放列ID |
| `spreadsheet_column_new()` | column.cc | 创建列 |
| `spreadsheet_column_copy()` | column.cc | 复制列 |
| `spreadsheet_column_free()` | column.cc | 释放列 |

### 10.2 表格管理函数

| 函数 | 文件 | 职责 |
|------|------|------|
| `spreadsheet_table_id_new_geometry()` | table.cc | 创建几何表格ID |
| `spreadsheet_table_id_match()` | table.cc | 判断表格是否匹配 |
| `spreadsheet_table_new()` | table.cc | 创建表格 |
| `spreadsheet_table_find()` | table.cc | 查找表格 |
| `spreadsheet_table_add()` | table.cc | 添加表格到Space |
| `spreadsheet_table_remove_unused()` | table.cc | 清理未使用表格 |

### 10.3 筛选函数

| 函数 | 文件 | 职责 |
|------|------|------|
| `spreadsheet_row_filter_new()` | row_filter.cc | 创建筛选器 |
| `spreadsheet_row_filter_copy()` | row_filter.cc | 复制筛选器 |
| `spreadsheet_filter_rows()` | row_filter.cc | 应用筛选 |

---

## 11. 学习要点总结

### 11.1 设计模式应用

1. **Pimpl模式**：通过Runtime分离实现细节
2. **工厂模式**：所有create函数统一管理内存
3. **策略模式**：ColumnValues隐藏具体数据类型
4. **标识模式**：TableID区分不同表格类型

### 11.2 C++技巧

1. **智能指针**：`unique_ptr<ColumnValues>`自动管理
2. **类型擦除**：GVArray提供泛型接口
3. **内存池**：MEM_new/MEM_free统一分配
4. **字符串视图**：StringRef避免拷贝

### 11.3 文件持久化要点

1. **分离运行时数据**：runtime不保存
2. **版本兼容**：通过SDNA处理结构变化
3. **深拷贝**：copy函数处理指针成员
4. **ID重映射**：foreach_id处理库依赖

---

*文档创建: 2025年*
*基于 spreadsheet_column*.hh/cc, spreadsheet_table*.hh/cc 分析*
