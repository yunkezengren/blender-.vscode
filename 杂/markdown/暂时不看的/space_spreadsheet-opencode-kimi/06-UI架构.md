# Spreadsheet UI架构

> 📍 目标：理解面板系统、数据集UI和Blender UI框架集成

---

## 1. 整体UI结构

### 1.1 Region布局

```mermaid
flowchart TB
    subgraph SpaceSpreadsheet空间
        direction TB

        subgraph Header [Header Region - 顶部栏]
            H1[数据对象名称]
            H2[对象类型: 网格/点云/体积...]
            H3[行数/列数统计]
        end

        subgraph MainWindow [Window Region - 主窗口]
            direction TB
            W1[表头行] --> W2[数据区域]
            W2 --> W3[水平滚动条]
        end

        subgraph Dataset [Dataset Region - 数据集面板]
            D1[对象树]
            D2[组件选择]
            D3[域选择]
        end

        subgraph Properties [Properties Region - 属性面板]
            P1[行筛选器列表]
            P2[添加筛选按钮]
        end

        subgraph Footer [Footer Region - 底部栏]
            F1[筛选开关]
            F2[仅显示选中]
        end

        Header --- MainWindow
        MainWindow --- Footer
        Dataset --- MainWindow
        MainWindow --- Properties
    end
```

### 1.2 Region注册

```mermaid
sequenceDiagram
    participant Space as SpaceSpreadsheet
    participant BKE as BKE_area_region_new
    participant List as ListBase regionbase

    Space->>BKE: 创建Header
    BKE-->>Space: 返回ARegion*
    Space->>Space: region->regiontype = RGN_TYPE_HEADER
    Space->>Space: region->alignment = TOP/BOTTOM
    Space->>List: BLI_addtail(&regionbase, region)

    Space->>BKE: 创建Footer
    Space->>List: 添加到regionbase

    Space->>BKE: 创建Dataset
    Space->>Space: region->regiontype = RGN_TYPE_TOOLS
    Space->>Space: region->alignment = RGN_ALIGN_LEFT
    Space->>List: 添加到regionbase

    Space->>BKE: 创建Properties
    Space->>Space: region->regiontype = RGN_TYPE_UI
    Space->>Space: region->alignment = RGN_ALIGN_RIGHT
    Space->>Space: region->flag = RGN_FLAG_HIDDEN (默认隐藏)
    Space->>List: 添加到regionbase

    Space->>BKE: 创建Window
    Space->>Space: region->regiontype = RGN_TYPE_WINDOW
    Space->>List: 添加到regionbase
```

---

## 2. 数据集面板

### 2.1 面板注册

```mermaid
flowchart TB
    subgraph 面板注册流程
        A[space_spreadsheet.cc] --> B[注册RegionTypes]
        B --> C[spreadsheet_data_set_region_panels_register]
        C --> D[创建PanelType]
        D --> E[设置idname/label/draw]
        E --> F[BLI_addtail添加到region]
    end
```

### 2.2 数据结构

```mermaid
classDiagram
    class ARegionType {
        +int regiontype
        +ListBase paneltypes
        +draw()
    }

    class PanelType {
        +char idname[BKE_ST_MAXNAME]
        +char label[BKE_ST_MAXNAME]
        +char translation_context[BKE_ST_MAXNAME]
        +int flag
        +draw()
    }

    class SpreadsheetPanelData {
        <<上下文数据>>
        +SpaceSpreadsheet* spreadsheet
        +DataSource* data_source
    }

    ARegionType --> PanelType : paneltypes
```

### 2.3 面板绘制

```cpp
// spreadsheet_panels.cc
void spreadsheet_data_set_region_panels_register(ARegionType &region_type) {
    PanelType *panel_type = MEM_new_zeroed<PanelType>(__func__);

    // 设置面板标识
    STRNCPY_UTF8(panel_type->idname, "SPREADSHEET_PT_data_set");
    STRNCPY_UTF8(panel_type->label, N_("Data Set"));
    STRNCPY_UTF8(panel_type->translation_context, BLT_I18NCONTEXT_DEFAULT_BPYRNA);

    // 无header，直接显示内容
    panel_type->flag = PANEL_TYPE_NO_HEADER;

    // 绑定绘制函数
    panel_type->draw = spreadsheet_data_set_panel_draw;

    // 添加到region
    BLI_addtail(&region_type.paneltypes, panel_type);
}
```

### 2.4 数据集树形结构

```mermaid
flowchart TB
    subgraph 树形视图
        A[Object: Cube] --> B[Mesh Component]
        A --> C[Instances Component]
        A --> D[Volume Component]

        B --> E[Vertices Domain]
        B --> F[Edges Domain]
        B --> G[Faces Domain]

        E --> H[position]
        E --> I[normal]
        E --> J[custom_attr]

        C --> K[Instances]
        K --> L[Instance 0]
        K --> M[Instance 1]
        K --> N[Instance 2]
    end

    subgraph 选中逻辑
        O[点击Domain] --> P[设置geometry_id]
        Q[点击Instance] --> R[设置instance_id数组]
    end
```

---

## 3. 数据集绘制实现

### 3.1 绘制流程

```mermaid
flowchart TB
    subgraph 数据集绘制流程
        A[spreadsheet_data_set_panel_draw] --> B[获取SpaceSpreadsheet]
        B --> C[创建临时块uiBlock]
        C --> D[遍历所有组件类型]
        D --> E{是否有该组件?}
        E -->|是| F[创建可折叠项]
        F --> G[展开/折叠状态]
        G --> H[遍历所有Domain]
        H --> I[创建Domain按钮]
        I --> J[绑定change_data_source操作符]
        E -->|否| K[跳过]
    end
```

### 3.2 组件选择UI

```cpp
// 简化的绘制逻辑
void spreadsheet_data_set_panel_draw(const bContext *C, Panel *panel) {
    uiBlock *block = uiLayoutGetBlock(layout);

    // 获取当前Space
    SpaceSpreadsheet *sspreadsheet = CTX_wm_space_spreadsheet(C);

    // 绘制对象选择器
    uiItemO(layout, nullptr, ICON_OBJECT_DATA, "SPREADSHEET_OT_select_object");

    // 绘制组件列表
    for (auto component_type : component_types) {
        // 检查组件是否存在
        if (!geometry_set.has(component_type)) continue;

        // 创建折叠面板
        uiLayout *box = uiLayoutPanel(
            C, layout, component_type_to_name(component_type));

        // 绘制域选择
        for (auto domain : supported_domains(component_type)) {
            uiItemFullO(box,
                       "SPREADSHEET_OT_change_spreadsheet_data_source",
                       component_type, domain);
        }
    }
}
```

### 3.3 实例导航

```mermaid
flowchart TB
    subgraph 实例层级展示
        A[Instances Component] --> B[Instances文件夹]
        B --> C[Instance 0: Cube]
        B --> D[Instance 1: Sphere]
        B --> E[Instance 2: Cylinder]

        C --> F[点击] --> G[进入实例视图]
        D --> H[点击] --> I[进入实例视图]
        E --> J[点击] --> K[进入实例视图]
    end

    subgraph 面包屑导航
        L[返回上一级] <-- M[Object: Cube]
        M <-- N[Instances/Instance 0]
        N <-- O[Mesh Component]
    end
```

---

## 4. 行筛选器UI

### 4.1 筛选器布局

```mermaid
flowchart TB
    subgraph 筛选器面板
        A[属性面板] --> B[筛选器列表]

        B --> C[筛选器1: position.x > 0]
        B --> D[筛选器2: material_index == 1]
        B --> E[+ 添加筛选器]

        C --> F[列选择: position.x]
        C --> G[操作符: >]
        C --> H[值: 0]
        C --> I[删除按钮]

        D --> J[列选择: material_index]
        D --> K[操作符: ==]
        D --> L[值: 1]
        D --> M[删除按钮]
    end

    subgraph 组合模式
        N[所有条件(AND)] --> O[任一条件(OR)]
    end
```

### 4.2 筛选器代码结构

```cpp
// spreadsheet_row_filter_ui.cc
void spreadsheet_row_filter_panel_draw(const bContext *C, Panel *panel) {
    SpaceSpreadsheet *sspreadsheet = CTX_wm_space_spreadsheet(C);
    uiLayout *layout = panel->layout;

    // 遍历所有筛选器
    int index = 0;
    LISTBASE_FOREACH (SpreadsheetRowFilter *, filter, &sspreadsheet->row_filters) {
        uiLayout *row = uiLayoutRow(layout, true);

        // 列名选择
        uiItemR(row, filter_ptr, "column_name", UI_ITEM_R_EXPAND_EVENT, "", ICON_NONE);

        // 操作符选择
        uiItemR(row, filter_ptr, "operation", UI_ITEM_R_EXPAND_EVENT, "", ICON_NONE);

        // 值输入
        uiItemR(row, filter_ptr, "value", UI_ITEM_R_EXPAND_EVENT, "", ICON_NONE);

        // 删除按钮
        uiItemO(row, "", ICON_X,
                "SPREADSHEET_OT_remove_row_filter_rule");

        // 传递索引参数
        uiItemIntO(row, "index", index);

        index++;
    }

    // 添加筛选器按钮
    uiItemO(layout, "Add Filter", ICON_PLUS,
            "SPREADSHEET_OT_add_row_filter_rule");
}
```

### 4.3 动态类型切换

```mermaid
flowchart TB
    subgraph 根据列类型切换UI
        A[选择列] --> B{列类型}
        B -->|bool| C[复选框]
        B -->|int/float| D[数值输入]
        B -->|string| E[文本输入]
        B -->|enum| F[下拉选择]
    end
```

---

## 5. 表头UI

### 5.1 表头结构

```mermaid
flowchart TB
    subgraph 表头行
        A[索引列标题] --> B[列1标题]
        B --> C[列2标题]
        C --> D[列3标题]
        D --> E[...]

        B --> F[拖拽手柄]
        C --> G[拖拽手柄]
        D --> H[拖拽手柄]

        F --> I[调整列宽]
        G --> I
        H --> I
    end
```

### 5.2 表头按钮

```cpp
void draw_header_cell(uiBlock *block,
                      const SpreadsheetColumn *column,
                      float x, float y,
                      float width, float height) {
    // 创建按钮
    uiBut *but = uiDefBut(block, UI_BTYPE_LABEL, 0,
                          column->id->name,
                          x, y, width, height,
                          nullptr, 0, 0, 0, 0, "");

    // 设置按钮属性
    but->flag = UI_BUT_TEXT_LEFT;
    but->drawflag = UI_BUT_TEXT_LEFT;

    // 添加工具提示
    but->tooltip = column_tooltip(column);
}
```

---

## 6. Blender UI框架集成

### 6.1 UI层级

```mermaid
flowchart TB
    subgraph Blender UI层级
        A[Screen] --> B[ScrArea]
        B --> C[ARegion]
        C --> D[uiBlock]
        D --> E[uiBut]
    end

    subgraph SpaceSpreadsheet层级
        F[SpaceSpreadsheet] --> G[Header Region]
        F --> H[Dataset Region]
        F --> I[Properties Region]
        F --> J[Window Region]
        F --> K[Footer Region]
    end
```

### 6.2 上下文获取

```mermaid
flowchart TB
    subgraph 上下文宏
        A[CTX_wm_space_spreadsheet] --> B[获取SpaceSpreadsheet]
        C[CTX_wm_region] --> D[获取ARegion]
        E[CTX_data] --> F[获取Main/blend文件数据]
        G[CTX_wm_window] --> H[获取wmWindow]
    end
```

### 6.3 布局系统

```mermaid
classDiagram
    class uiLayout {
        +int w, h
        +int root_w, root_h
        +int space
        +int align
        +ListBase items
        --
        +row() uiLayout*
        +column() uiLayout*
        +box() uiLayout*
        +panel() uiLayout*
    }

    class uiBlock {
        +ListBase buttons
        +float winmat[4][4]
        --
        +begin() void
        +end() void
    }

    class uiBut {
        +char str[UI_MAX_DRAW_STR]
        +int type
        +float x1, y1, x2, y2
        +float hardmin, hardmax
        --
        +draw() void
    }

    uiLayout --> uiBlock
    uiBlock --> uiBut
```

---

## 7. RNA属性绑定

### 7.1 RNA定义

```mermaid
flowchart TB
    subgraph RNA属性定义
        A[定义StructRNA] --> B[定义PropertyRNA]
        B --> C[设置getter/setter]
        C --> D[绑定到DNA结构]
    end

    subgraph DNA到UI
        E[DNA属性] --> F[RNA包装]
        F --> G[uiItemR自动创建控件]
    end
```

### 7.2 Spreadsheet RNA属性

```cpp
// DNA属性定义
// DNA_space_types.h
struct SpreadsheetRowFilter {
    int flag;
    char column_name[64];
    int data_type;  // eSpreadsheetColumnValueType
    int operation;  // eSpreadsheetFilterOperation
    SpreadsheetFilterValue value;
};

// RNA定义（简化）
void RNA_def_spreadsheet_row_filter(BlenderRNA *brna) {
    StructRNA *srna;
    PropertyRNA *prop;

    srna = RNA_def_struct(brna, "SpreadsheetRowFilter", nullptr);
    RNA_def_struct_sdna(srna, "SpreadsheetRowFilter");
    RNA_def_struct_ui_text(srna, "Spreadsheet Row Filter", "");

    // column_name属性
    prop = RNA_def_property(srna, "column_name", PROP_STRING, PROP_NONE);
    RNA_def_property_string_funcs(prop, "rna_SpreadsheetRowFilter_column_name_get",
                                  "rna_SpreadsheetRowFilter_column_name_length",
                                  "rna_SpreadsheetRowFilter_column_name_set");
    RNA_def_property_ui_text(prop, "Column", "Column to filter");

    // operation属性
    prop = RNA_def_property(srna, "operation", PROP_ENUM, PROP_NONE);
    RNA_def_property_enum_items(prop, rna_enum_spreadsheet_filter_operation);
    RNA_def_property_ui_text(prop, "Operation", "Filter operation");
}
```

---

## 8. 国际化支持

### 8.1 翻译标记

```mermaid
flowchart TB
    subgraph 翻译使用
        A[N_("Data Set")] --> B[标记需要翻译]
        B --> C[提取到po文件]
        C --> D[翻译人员翻译]
        D --> E[运行时加载.mo文件]
    end
```

### 8.2 代码示例

```cpp
// 定义时的翻译标记
STRNCPY_UTF8(panel_type->label, N_("Data Set"));
STRNCPY_UTF8(panel_type->translation_context, BLT_I18NCONTEXT_DEFAULT_BPYRNA);

// RNA定义中的翻译
RNA_def_property_ui_text(prop, N_("Column"), N_("Column to filter"));

// 操作符描述
ot->description = N_("Add a filter to remove rows from the displayed data");
```

---

## 9. 主题系统

### 9.1 主题集成

```mermaid
flowchart TB
    subgraph 主题使用
        A[用户定义主题] --> B[UserDef.uiflag]
        B --> C[THEME_COLOR值]
        C --> D[UI绘制颜色]
    end
```

### 9.2 主题常量

```cpp
// 在绘制代码中使用
immUniformThemeColor(TH_BACK);           // 背景色
immUniformThemeColorShade(TH_BACK, 11);   // 背景色+11亮度
immUniformThemeColor(TH_ROW_ALTERNATE);   // 交替行色
immUniformThemeColor(TH_TEXT);            // 文字颜色
immUniformThemeColor(TH_HEADER);          // 表头颜色
```

---

## 10. 事件处理流程

### 10.1 鼠标事件传递

```mermaid
sequenceDiagram
    participant WM as WindowManager
    participant Region as ARegion
    participant Block as uiBlock
    participant But as uiBut
    participant Space as SpaceHandler

    WM->>WM: 处理鼠标移动
    WM->>Region: 分发到Region
    Region->>Block: 分发到Block
    Block->>But: 检测悬停按钮
    But->>But: 处理hover事件
    But-->>Block: 消耗事件?

    alt 未消耗
        Block->>Space: 传递给Space处理器
        Space->>Space: 检测列边缘悬停
        Space->>Space: 设置光标样式
    end
```

### 10.2 操作符触发

```mermaid
flowchart TB
    subgraph 事件到操作符
        A[鼠标按下] --> B[WM_operator_invoke]
        B --> C[查找操作符类型]
        C --> D[执行poll]
        D -->|true| E[执行invoke]
        D -->|false| F[忽略]
        E --> G[OPERATOR_RUNNING_MODAL]
        G --> H[进入modal循环]
    end
```

---

## 11. 关键UI函数

### 11.1 常用UI函数

| 函数 | 作用 | 示例 |
|-----|------|------|
| `uiLayoutRow` | 创建水平布局 | `uiLayoutRow(layout, true)` |
| `uiLayoutColumn` | 创建垂直布局 | `uiLayoutColumn(layout, true)` |
| `uiLayoutBox` | 创建带边框的布局 | `uiLayoutBox(layout)` |
| `uiLayoutPanel` | 创建可折叠面板 | `uiLayoutPanel(C, layout, label)` |
| `uiItemR` | 添加属性控件 | `uiItemR(layout, ptr, propname)` |
| `uiItemO` | 添加操作符按钮 | `uiItemO(layout, label, icon, opid)` |
| `uiDefBut` | 定义按钮 | `uiDefBut(block, type, retval, str...)` |

### 11.2 面板函数

| 函数 | 文件 | 作用 |
|------|------|------|
| `spreadsheet_data_set_region_panels_register` | panels.cc | 注册数据集面板 |
| `spreadsheet_data_set_panel_draw` | dataset_draw.cc | 绘制数据集面板 |
| `spreadsheet_row_filter_panel_draw` | row_filter_ui.cc | 绘制筛选器面板 |

---

## 12. UI设计原则

### 12.1 可用性考虑

```mermaid
flowchart TB
    subgraph 设计原则
        A[反馈] --> B[悬停高亮]
        B --> C[拖拽视觉反馈]
        D[一致性] --> E[遵循Blender风格]
        E --> F[使用标准图标]
        G[效率] --> H[快捷键支持]
        H --> I[快速自适应列宽]
    end
```

### 12.2 可访问性

| 考虑点 | 实现 |
|-------|------|
| 键盘导航 | Tab切换焦点 |
| 高对比度 | 使用主题系统 |
| 工具提示 | 所有按钮提供tooltip |
| 图标+文字 | 重要按钮同时使用 |

---

*文档创建: 2025年*
*基于 spreadsheet_panels.cc, spreadsheet_row_filter_ui.hh/cc, spreadsheet_dataset_draw.hh/cc 分析*
