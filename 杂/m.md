### 首先判断当前选中的节点是否具备“插入”的资格。


* **寻找“终点end node”**:

  * 遍历所有选中节点的输出接口的连线，如果有一个link连到选中节点外, 则当前选中节点不具备插入资格。
  * 当只存在一个节点的输出接口全没连线才判定为有效.
  * 区域输入和区域输出之间没连线的话,可以存在两个end node.

---

### 获取输入接口和输出接口候选

---

### 最后,根据选中节点总体边界框相交的连线类型,再候选里查找合适的起点终点接口.
